# META — космология · наблюдатель · эпистемология

**Не SSOT физики.** Канон **`g`**, аксиомы, M→T→SM — только [`MODEL.md`](MODEL.md) hub + [`model/`](model/).

**Meta** = следствия и рамки **над** уже принятой физикой M: циклы, UI наблюдателя, «почему не подсмотреть будущее», генезис **наблюдаемой** Вселенной. **Не добавляет knobs в `g`.**

---

## §0. Граница MODEL / Meta

| | **MODEL** (M + T + SM) | **META** (этот файл) |
|---|------------------------|----------------------|
| **Объект** | `g`, `Z_N[i]`, аксиомы A1–A16, SI, SM-числа | космология, observer UI, access |
| **Истина** | **да** — SSOT | **нет** — интерпретация поверх MODEL |
| **Verify** | `verify_principles.py` (M) | логика закрыта; long-run sim — leaf |
| **Примеры §** | §1–§8 MODEL | §1–§3 META |

**Правило чтения:** спор о **`g`** → MODEL. Спор «что видит мозг / был ли t=0» → META, но **не** может отменить A3/A13.

**Карта:**

| тема | MODEL | META |
|------|-------|------|
| детерминизм, Born | §2.1 | — |
| будущее/прошлое, observer lock | stub §2.2 | §1 |
| `system_quanta`, Compton fractal | §4.8 | §2.2 (UI смысл) |
| мозг, Bekenstein, кадр 15 ms | stub §4.8 | §2 |
| циклы, BB, baryon asymmetry narrative | refs | §3 |
| seeds / T-tests | §3, §4, §10 | §3.7 |

---

## §1. Эпистемология access (будущее / прошлое)

**Следствие MODEL §2.1 + A3 + A13:** на M будущее **уже определено** — развёртывание одной `g` тик за тиком. Прошлое **тоже** — унитарный шаг **обратим**: `g⁻¹` (A13); информация **не стирается**.

| | M | T (наблюдатель) |
|---|-----|-----------------|
| Будущее | фиксировано `g` | **не предсказуемо** аналитически |
| Прошлое | в `z` сейчас | **не читается** до бита (entropy veil) |
| «Знаем будущее» | да — **траектория** | нет — **кино событий** |

**Вычислительная несжимаемость:** `z(t+N)` только прогоном CA (chaos gate), не формулой macro-T. Вселенная = калькулятор своего кода.

**Замок наблюдателя:** мы внутри M; опередить ε-окрестность на N тиков изнутри **нельзя** — нужно больше тактов, чем вмещает подсистема.

**Прошлое:** A6 размазывает micro-info в фон; macro = шум. A13 ⇒ при `g⁻¹` история **разматывается** (§3 циклы). A9: локальная ε связана с полем — прошлое **капиллярно** в текущем `z`.

**Тор (sim):** конечное пространство состояний → **Пуанкаре** (возврат). Не Turing-halting — `g` обратима, без произвольных веток.

**GPU sim:** **не** наш BB с ~10^180 амплитуд; **да** — проверка **правил** (T1/T2) + **структурный** перебор зародышей (§3.7), не raw brute force.

---

## §2. Наблюдатель и UI (T над T)

**Provenance:** 2026-09-22 — сшивка автомата M и **наблюдаемой** Вселенной.

**Переворот (meta):** **13–15 ms** на «кадр» — не баг биохимии, а **неизбежность** readout при дискретном **M + ДА** (MODEL §3.9): мозг **отражает такт** на fractal-этаже T.

### 2.1 Мозг как macro-узел

Мозг = конгломерат **`hV`** (MODEL §5, §3.10). **~10¹¹** нейронов — macro-сеть поверх **`v_p`**. Физика узла — MODEL; **сознание как UI** — meta.

### 2.2 Ёмкость кадра и fractal quanta

**Формулы (вычислимы из MODEL §4.8)** — канон в MODEL; здесь **смысл UI:**

| pool | порядок | роль (meta) |
|------|---------|-------------|
| **`N_cell`** | **~10¹⁰¹** | геометрия; **не** читается (Гейзенберг-шум) |
| **`N_tick`** | **~10⁴¹** | сырой M-budget на worldline |
| **`I_max`** | **~10⁵⁰ bit** | Bekenstein-cap упорядоченного UI (MODEL §4.8) |
| **фенomenальный кадр** | **≪ I_max** | invariants: траектории + интерференция |

**Coarse-graining:** **`N_cell` → invariants**; один кадр = сжатие combinatorial budget в **«комната, 4 утра, небо»**.

**Fractal-закон:** **`Δx_sys/l_P = Δt_sys/hT = m_P/M`** — чем легче система, тем ближе её «личные» кванты к глобальным **`l_P`, `hT`**. Пример: **дрозофила** **`Δx/l_P ~ 2%`** vs человек **~10⁻¹⁰** — разная **UI-геометрия**, не другой `g`.

```
M  (hT, g, Z_N[i])  →  T  (поля, SM)  →  UI  (кадр, τ_frame)
```

**Impl:** `mt_ca.si_constants.system_quanta` — **мост M→T**; интерпретация UI — meta.

### 2.3 Наблюдаемая Вселенная

**Шаги генезиса UI** (не новые параметры `g`):

1. **Сепарация** — хаос → vortex **`v_p`** (MODEL §5)
2. **Зигзаг → mass → расстояние** — emergent метры T
3. **Интерференция → склейка** — поля, атомы, облака
4. **Наблюдатель** — macro-узел + **`τ_frame ~ 15 ms`** + coarse → небо, тела

```
M ──coarse──► T ──brain──► «наблюдаемая Вселенная»
```

**Наблюдаемая Вселенная = UI над T**, не отдельная сущность.

---

## §3. Космология (логика **закрыта**)

**Граница:** §3 **не добавляет** параметров в `g` · не GPU-DoD (MODEL §10).

### 3.1 Почему циклы — без сингулярности

GR Big Bounce требует **ρ → ∞**. В M **ничего не сжимается в точку**:

- решётка **константна**: `hL`, `hT`, `g` не меняются;
- **A3:** `Σ|z|²` сохраняется;
- **тор** (MODEL §10.1) — волна возвращается;
- **A5:** абсолютный ноль **недостижим**.

Цикл = **термодинамика информации**, не коллапс метрики.

### 3.2 Четыре фазы «дыхания»

#### I · Расширение (мы здесь)

Локальный **фазовый сдвиг** → gate → хаос. Фронт по `N₄` с `c₀`. Vortex в **`v_p`**. **A6:** хаос размазывается.

#### II · Затухание (**тепловая смерть T** — не M)

**Только слой T.** Теорема MODEL **§2.3**: на M **`Σ|z|²`** сохраняется, **A5** кипение не прекращается, **`g`** не останавливается.

Macro-фазы перемешаны. Vortex **аннигилируют** → фон **в readout T**. Micro-норма (A3) на M **сохранена**.

#### III · Остывание к «льду»

Релаксация к стационару — синхронное `ω`-вращение (MODEL §7.1). Macro-T: «пустота». Micro-M: тики идут; A5 — ноль асимптотика.

#### IV · Новый цикл

«Лёд» + **A5** + ε-флуктуации → детерминированный micro-сдвиг → gate → новый BB с **выбранной хиральностью** (`n=+1` в нашем пузыре — MODEL §5.0.3 **physics**; **dominance narrative** — meta).

```
     ┌──────────┐                      ┌──────────┐
     │   ЛЁД    │ ── фазовый сдвиг ──► │ РАСШИР.  │
     └────▲─────┘                      └────┬─────┘
          │         A3 + тор + A5           ▼
          └────────────────────────  HEAT DEATH (T only · MODEL §2.3)
```

**BB ≠ t=0:** ~13.8 Gyr — наш **пузырь**, не первый тик M.

### 3.3 Baryon asymmetry (narrative)

**Не «50/50 случайность».** M **детерминирован** (MODEL §2.1): первый A5-сдвиг «лёда» задаёт **хиральность** пузыря — **доминирует `n=+1`**. Антиматерия **локально** рождается, но в **«правой воде»** быстро annihilate. **Геометрия стартового зародыша**, не мистика.

### 3.4 Bесконечность

| | |
|---|---|
| **Прошлое** | Нет «небытия». Автомат **всегда** был. |
| **Будущее** | A3 + §2.3 (нет M heat death). **Выключиться нельзя.** |

T — **пена** на гребне одной волны бесконечного океана тиков.

### 3.5 Sim-leaf (не логический пробел)

| Закрыто логикой | Численно позже |
|-----------------|----------------|
| цикл без сингулярности | long-run entropy on torus |
| **детерминизм M → Born на T** | MODEL §2.1 |
| **будущее/прошлое** | §1 META |

### 3.6 Поиск зародышей (~10–20 прогонов)

**Raw brute force** отсечён: A9 + A4 → случайные поля = мусор.

**4 топокласса** (physics в MODEL §3, seeds):

| n | SeedClass | test |
|---|-----------|------|
| 0 | PLANE_WAVE | T1 |
| +1 | VORTEX_P | T2, A11 |
| −1 | VORTEX_M | T2 · §5.0.3 |
| +2 | VORTEX_N2 | persistence |

**~10–20 runs** по классу · `seeds.py` · `validate_mt.py`.

---

## §4. Карта MODEL ↔ META

| Тема | MODEL | META |
|------|-------|------|
| A3, A5, A6, `g` | §2–§3 | — |
| gap taxonomy | §0 | — |
| access, observer lock | §2.2 stub | §1 |
| system_quanta формулы | §4.8 | §2.2 UI |
| мозг, UI, кадр | stub §4.8 | §2 |
| циклы, BB, ∞ | stub §9 | §3 |
| SM, SI, Higgs | §7–§8 | — |
| GPU контракт | §10 | — |
