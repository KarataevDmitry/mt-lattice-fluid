# §8.2·α·meter · финт: N_a0/a0 — readout, не вход
**Проблема (старый капкан):** оптический метр / a₀ → N_a0 → α. Тогда α зависела бы от T-якоря.

**Финт (переворот зависимости):**
```
α закрыто (мягкая грань, без метра)
  → upstairs m_e
  → N_c = m_P / m_e
  → N_a0 = N_c / α
  → a0 = N_a0 · l_P
```
SI-метр остаётся SI-2019 через c; единица решётки = l_P = hL.
Оптический a₀ / CODATA N_a0_Bohr — **только T-дверь** (~0.45%), не M-определение.
§8.2·H «ℤ N_a0 из структуры H без α» остаётся **OPEN census** — **не блокирует** α и не определяет метр для α.

**Код:** `SI.alpha_meter_na0_bridge_row()` · verify **`Alpha_meter_na0_bridge`**.

#### §8.2·meter·decouple · SI-метр ∉ M
После seal α + meter-fint: **на M метра нет.**

| слой | длина |
|------|--------|
| **M** | hops of **hL** (a≡l_P, Thm 5.2 / §7.4) |
| **α, masses, N_a0** | без метра (`N_a0=N_c/α`) |
| **T-export** | optional: `x_m = N·l_P_SI`, SI-2019, optical a0 — lab speech only |

`√(ħG/c³)` пишет метры для лаборатории; **не** правит линейку M.
**Код:** `SI.meter_decouple_from_M_row()` · verify **`Meter_decouple_from_M`**.

#### §8.2·[L]·l_P · размерность длины = l_P; иерархия из α
**Не** реконструкция исторического SI-метра.

Единица длины **определена** размерно: **`[L]=[V][T]`** ⇒ **`l_P = c·t_P = √(ħG/c³)`**; на носителе **`hL = c0·hT`**, **`l_P ≡ hL`**. α безразмерна и точна ⇒ любой EM-масштаб — hop-счёт:
```
λ̄_C = N_c · l_P           N_c = m_P/m_e
a0   = (N_c/α) · l_P      = λ̄_C / α
r_e  = α·N_c · l_P        = α² · a0
```
Размерный анализ: `[L]=[l_P]`, `[α]=1` ⇒ `L = N(α, N_c, …)·l_P`.
**Код:** `SI.length_dim_from_lP_alpha_row()` · verify **`Length_dim_from_lP_alpha`**.

#### §8.2·[T]·t_P · размерность времени = t_P; тик M = hT
**Не** реконструкция SI-секунды (цезий).

Единица времени **определена** размерно: **`[T]=[L]/[V]`** ⇒
**`t_P = l_P/c = √(ħG/c⁵)`**. На носителе **`hT = κ·t_P`** (истинный M-тик),
**`c0 = l_P/hT = c/κ`**.

SI-2019 секунда (`Δν_Cs` exact) — тот же класс тавтологии, что c-fixed метр: T-export only.
**Код:** `SI.time_dim_from_tP_row()` · verify **`Time_dim_from_tP`**.

#### §8.2Â·unitsÂ·time-first Â· база как в СИ: {L, M, T}
Та же схема, что у СИ: задаём **три базовых размерности**, остальное — производные.

| база | квант | определение |
|------|-------|-------------|
| **[T]** | `t_P` | квант времени (`hT = ÎºÂ·t_P` на носителе) |
| **[L]** | `l_P` | `cÂ·t_P` — путь света за один квант |
| **[M]** | `m_P` | `â/(cÂ² t_P) = â/(cÂ·l_P)` |

Сила, энергия, â¦ — из {L,M,T} **точно как в СИ**.
`G = â c / m_PÂ²` — производная, не четвёртая база.
Числа «метр/секунда/кг» SI-2019 — T-export ярлыки, не прототипы базы.
**Код:** `SI.units_time_first_cascade_row()` Â· verify **`Units_time_first_cascade`**.

#### §8.2·units·T_P · температура без k_B (закрыто)
На M (§5.3.1) **T = ⟨E_kin zigzag⟩** — энергия, отдельной оси Θ нет.

| квант | определение | смысл |
|-------|-------------|--------|
| **T_P** | `E_P = ℏ/t_P` | планковская температура = энергия textbook tick |
| **T_P_M** | `E_0 = s_0/hT = κ·E_P` | Arg-энергия за один M-тик |

**k_B** и кельвин — только T-export: `Θ_P[K] = E_P/k_B`. Не онтология.
**Код:** `SI.planck_temperature_independent_row()` · verify **`Planck_temperature_independent`**.

#### §8.2·geo·voronoi · Ромбический додекаэдр: ячейка FCC
**Не hull соседей** — **Вороной / WS** узла **`hV`**. Якорь тот же: **`a=l_P`** (12 NN на **`l_P`**; ребро каждого ромба **`a`**).
| при `a=l_P` | формула | SI |
|-------------|---------|-----|
| **`V`** (Voronoy) | **`a³/√2 = v_hV`** | m³ |
| **`R_in`** (до стены) | **`a/2`** | m — биссектриса **`|ON|/2`**, не **`κa`** hull |
| **`R_axis`** (ось ±x…) | **`a/√2 = κa`** | m — совпадает с **`R_in(hull)`** кубоокtaэдра |
| **`R_cubic`** (±,±,±) | **`a√(3/2)`** | m — дальняя вершина ячейки |
| **`A_◇`** (один ромб) | **`(2√2/3)a²`**, **`cos θ=1/3`** | m² |
| **`S`** | **`8√2·a²`** | m² |
| **`V/S`** | **`a/16`** | m |
**Dual к 1-tick hull:**
$$
12\ \text{NN} \leftrightarrow 12\ \text{ромбов},\quad
14\ \text{граней hull} \leftrightarrow 14\ \text{вершин WS},\quad
24\ \text{рёбер}.
$$
**Сшивка двух тел при том же `a`:**
$$
\frac{V_{\mathrm{cuboctahedron}}}{V_{\mathrm{Voronoy}}}
= \frac{V_{\mathrm{hull}}}{v_{hV}}
= \frac{16}{3},
\qquad
R_{\mathrm{in}}^{\mathrm{Voronoy}}
= \kappa\, R_{\mathrm{in}}^{\mathrm{hull}}
= \frac{a}{2}.
$$
**`κ=1/√2`** — macro readout по **□-грани hull**; **стена Voronoy** ближе: **`a/2`**. Осевая вершина WS лежит на расстоянии **`κa`** — там же, где **inradius hull**.

---

**Сюда:** физика (§). Impl / verify / код → [`DEVLOG.md`](../DEVLOG.md).
