# §6–§8 · Лестница · SI · SM

## §6. Иерархия M → SM (масштабная лестница, часть модели)
| Уровень | Масштаб | Объект |
|---------|---------|--------|
| 0 | 1·dV = (dl)³ | **pra-дефект** в одном dV, заряд `e₀`, `m₀` |
| 1 | 10³–10⁵·dl | пре-резонансы / лептонные узлы (составные) |
| 2 | ~10¹⁵·dl | кварковые фокусы (составные) |
| 3 | ~10²⁰·dl | адроны (протон), SM |
Конфайнмент: «вытащить кварк» = разорвать топологический узел → **пара pra-дефектов**, не один dV.
**Цвет ≠ вход.** Кратность фокусов в устойчивом составном узле — **prediction** из топологии на FCC/$\varepsilon$ + Паули + $K_P$; ожидание $\sim d=3$ (**§8.4.1-D**). SM-$N_c$ — T-имя, не axiom M.
---
## §7. SI-мост (размерные линейки)
```
dl = hL — Postulate 0.1 (структура); ℓ_P := hL — §7.4
hT — M-тик; t_P = hT/κ — учебниковая метка
dV = v_hV = hL³/√2 — Voronoy FCC (§8.2·geo·voronoi)
|z|² ↔ ρ_E/u_P , u_P = K_P , μ_P = ρ_cell (§7.3)
(Δ z)/dl² ↔ Δ₄ z
κ = 1/√2 , c₀ = hL/hT , c = κ·c₀ , c² = K_P/μ_P
√(ℏG/c³) — CODATA check, не определение hL (§7.4)
```
### 7.1 Константы `g`: SI → код (монолит, без λ)
**Шаги решётки (CODATA 2018 + N κ):**
| символ | формула | значение (СИ) | код |
|--------|---------|---------------|-----|
| `DX` / `hL` | **`hL`** (§0.1); **`ℓ_P:=hL`** (§7.4) | `1.616255×10⁻³⁵` м | `SI.l_P` |
| check | **`√(ℏG/c³)`** vs **`hL`** | same | rel ≈ 0 |
| `t_P` (conv.) | `l_P/c` | `5.391247×10⁻⁴⁴` с | `SI.t_P` |
| **`DT` / `hT`** | **`t_P/√2`** | **`3.812187×10⁻⁴⁴` с** | **`SI.hT`** |
| `c₀` | `l_P/hT` | `4.239706×10⁸` м/с | `SI.c0` |
| `c` (macro) | `κ·c₀` | `2.997925×10⁸` м/с | `SI.c` |
| `μ_P` | `c⁵/(ℏG²)` | `5.155×10⁹⁶` kg/m³ | `SI.mu_P` |
| **`K_P` = `β` = `u_P`** | **`μ_P·c² = c⁷/(ℏG²)`** | **`4.633×10¹¹³` Pa** | **`SI.K_P` / `SI.u_P`** |
**Fluid:** `c² = K_P/μ_P` — macro `c` = скорость волны давления несжимаемой среды (§5.1.1).
**ω — не knob:** один полный оборот вакуумной фазы за **M-тик** `hT`:
```
ω = 2π / hT ≈ 1.648×10⁴⁴ рад/с
hT·ω = 2π
```
**Размерный gate (до нормировки):** λ **убран** — его роль = `hT` (размерность времени):
```
z' = z + l_P²·Δ_disc z + i·hT·( α_SI/(ρ_E + β) − ω )·z
β = K_P = u_P = c⁷/(ℏG²) ≈ 4.633×10¹¹³ Дж/м³
α_SI = (2π + ½)·u_P ≈ 3.143×10¹¹⁴ Дж/м³
```
**Схлопывание в natural (симуляция):** `|z|² = ρ_E/u_P`, подставить `hT·ω=2π`:
```
φ = hT·ω · ( α*/(|z|² + ε) − 1 ) = 2π · ( α*/(|z|² + 1) − 1 )
α* = 1 + 1/(4π) ≈ 1.0795774715
ε = 1
```
| буква диалога | СИ | natural (код) | число |
|---------------|-----|---------------|-------|
| `β` | `u_P` [Дж/м³] | `ε = 1` | `4.633×10¹¹³` |
| `λ` | `hT` [с] | **нет** — внутри `hT·ω` | `3.812×10⁻⁴⁴` |
| `α` | `(2π+½)u_P` [Дж/м³] | `α* = 1+1/(4π)` | `1.079577…` |
| `ω` | `2π/hT` [рад/с] | `phase_scale=2π` | `1.648×10⁴⁴` |
| `γ` | — | `gamma=0.25` | **калибровка dispersion (T)**, не из SI |
### 7.2 Planck-единицы зависят от **c**; **κ** — нет (порядок снизу)
**Исходный интересный ход:** в continuum все Planck-линейки собраны из **(ℏ, G, c)** — **c входит явно**:
$$
\ell_P=\sqrt{\frac{\hbar G}{c^{3}}},\qquad
t_P=\frac{\ell_P}{c}=\sqrt{\frac{\hbar G}{c^{5}}},\qquad
E_P=\frac{\hbar}{t_P}\propto c^{5/2},\qquad
u_P=\frac{c^{7}}{\hbar G^{2}}.
$$
**Скрытое допущение:** **`t_P=\ell_P/c`** уже кладёт **одну** macro-**`c`** и в пространство, и во время — link-speed = **`c`**, т.е. **`κ=1`**.
**M ломает это снизу, не подгоняя c:**
| шаг | откуда | что |
|-----|--------|-----|
| 1 | A1 + FCC + hull при **`a=\ell_P`** | **`κ_{\mathrm{geom}}=R_{\mathrm{in}}/R_{\mathrm{out}}=1/\sqrt{2}`** — **без c** |
| 2 | CODATA **ℏ, G, c** | **`ℓ_P, t_P`** (Planck-лadder **с c**) |
| 3 | геометрия × Planck | **`hT=\kappa_{\mathrm{geom}}\,t_P`**, **`E_0=\kappa_{\mathrm{geom}} E_P`** |
| 4 | алгебра | **`c_0=\ell_P/hT=c/\kappa`**, **`c=\kappa c_0`** — **тождество-проверка**, не определение **κ** |
На гекс-срезе тот же метод: **`κ_{\mathrm{hex}}=\sqrt{3}/2`**, **`hT=(\sqrt{3}/2)\,t_P`**, **`c_0=(2/\sqrt{3})\,c`**, macro **`c`** (CODATA) **тот же**.
```
continuum: t_P = ℓ_P/c ⇒ один тик, один c, κ=1 скрыт
M + geo: hT = κ·t_P ⇒ κ из тела (кубоокtaэдр / гекс)
readout: c = κ·c₀ ⇒ следствие, не корень κ
```
### 7.3 Planck из условий на ячейку (не наоборот)
**Тезис:** планковская **шкала** не «вводится сверху» как `(ℏ,G,c)→ℓ_P,t_P`. **Физика на одном `hV`** + **геометрия FCC** задают лестницу; учебниковый Planck — **слой согласованности** с macro **`c`**, не аксиома **κ**.
**Условия на кирпич** (§5.0 · §5.0.2 · gate §7.1 · §8.2·geo·voronoi):
| # | условие | формула |
|---|---------|---------|
| G | Voronoy FCC при **`a=ℓ_P`** | **`v_{hV}=ℓ_P³/√2`** |
| P1 | Heisenberg / **`Arg`** | **`s₀=ℏ/2`**, **`E₀=s₀/hT`** |
| P2 | локальный **`E=mc²`** на ячейке | **`m_{\arg}=E₀/c²=m_P/√2`** |
| P3 | плотность кирпича | **`ρ_{\mathrm{cell}}=m_{\arg}/v_{hV}=m_P/ℓ_P³=μ_P`** |
| P4 | gate saturation | **`|z|²=1 ↔ ρ_E=u_P`** |
| P5 | fluid (несжимаемая среда) | **`u_P=μ_P c²`**, **`c²=K_P/μ_P`** |
| P6 | Bekenstein на кирпиче | **`B_{hV}=2πE_Pℓ_P/(ℏc\ln2)=2π/\ln2`** (чистое число) |
**Замыкание (без fit):** из P2+P3+G сразу **`μ_P`**, из P5 — **`u_P`**; **`κ`** из hull (§8.2·geo) даёт **`hT=κt_P`**, **`E_P=E₀/κ`**.
**Порядок первичности (M-first):**
```
ℓ_P = a (A1) , hT (M-тик) , s₀=ℏ/2
 ↓ ↓
 v_hV=ℓ_P³/√2 E₀=s₀/hT → m_arg → μ_P
 ↓
 t_P = hT/κ , E_P = E₀/κ (учебниковый Planck — производные)
 ↓
 (ℏ, G, c) CODATA — согласованность абсолютной шкалы, не определение κ
```
**Честная граница:** **метр** в SI всё ещё требует измеренных **`ℏ,G,c`** (или эквивалентного якоря). Но **все ratios** (**`κ`**, **`v_hV/ℓ_P³`**, **`E₀/E_P`**, **`hT/t_P`**, **`ρ_cell=μ_P`**, **`B_hV`**) — **из ячейки + геометрии**, не из «**`c/c₀`** как определения **κ**».
### 7.4 **`a` не может быть иным, чем **`ℓ_P`** (отвязка линейки от **`c`**)
**Тезис:** на M **один** пространственный квант. Геометрическое ребро **`a`** (hull / A1 / Voronoy) **не параметр** — это **`hL`**. Имя **`ℓ_P`** := **`hL`**, не наоборот.
**Цепочка идентичности (не fit):**
| шаг | откуда | что |
|-----|--------|-----|
| 0 | Thm 0.1 (§0) | **`dl = hL`** — ниже нет структуры |
| 1 | A1 | все NN на **одной** длине light-like = **`hL`** |
| 2 | §8.2·geo | выпуклая оболочка 12 NN → ребро **`a = \|NN\| = hL`** |
| 3 | §8.2·geo·voronoi | **`v_hV(a)=a³/√2`**, **`dV`** на том же **`a`** |
| 4 | именование | **`ℓ_P := hL`** (Planck — метка SI, не второй ruler) |
**Почему нельзя **`a ≠ ℓ_P`****: масштаб **`λa`** либо ломает A1, либо **`dV ≠ v_hV`**, либо требует структуру ниже **`dl`** — запрещено §1.4.
**`√(ℏG/c³)`** — **не определение** **`ℓ_P`**, а **проверка** с macro **`c`**. **`c`** здесь — импорт CODATA, не корень M-линейки.
**Механика без macro-`c`:** **`p₀=ℏ/(2ℓ_P)`**, **`m_arg = 2p₀/c₀ = ℏ/(ℓ_P c₀)`** (§5.2.1) — согласовано с **`m_arg=E₀/c²`**.
### 7.5 Дискретность без Postulate 0 — Теорема 0.1
**Глубже §7.4:** не «postulate CA on `(hL,hT)`», а **вывод носителя** из уже принятых **A1–A16**:
```
A3+A13+Z_N[i] → конечный алфавит, счётные конфигурации (не ℝ³ поле)
A1+A2+§1.6 → Λ=FCC, N₁₂, ребро = hL
A10+A11 → hV топологически атомен (n∈ℤ на ∂hV)
A7+A5+§3.12.6 → B_hV=2π/ln2, z_min>0 — нет sub-dl readout
A16+§5.2.1 → s₀=ℏ/2, p₀=ℏ/(2hL) — hL при ℏ без c
 ↓
§7.3 cell → ρ_cell=μ_P, u_P
§7.4 → ℓ_P := hL ; √(ℏG/c³) check
```
**Что остаётся «наверху» (честно):** **`ℏ`** (SI-метр) и **sim-хвосты** (long-run **A6**, star-closure ledger). **Не** postulate «решётка», **не** отдельные **p/F**, **не** **`c`** в цепочке дискретности. **`g`** — **Следствие 0.7**; механика — **Следствие 0.8** / **Thm 5.1**.
### 7.6 Механика (Landau) — Следствие 0.8 / Теорема 5.1
```
A5+§3.7 → s₀=ℏ/2
Thm0.1+§7.4 → p₀=s₀/hL , E₀=s₀/hT
§5.2.1 → L₀=s₀ , F₀=m_arg·g_M (F=mg)
A3+A13+⌊𝒩⌋ → Δp∈p₀·ℤ ; Madelung j — T
§5.2.2 → E=n_E·E₀
```
**Где «мешает постулат»:** Postulate **0.1** даёт только **символы** **`(hL, hT)`** и «ниже **`dl`** нет» — **без Planck**. Если в §0 **заранее** написать **`hL=ℓ_P=√(ℏG/c³)`**, красивая цепочка §7.3–7.4 **схлопывается** в tautology. Поэтому **0.2** в §0 — **сводка после вывода**, не аксиома; **`ℓ_P`** — **имя**, навешенное когда **`a ≡ hL`** уже закрыто физикой ячейки.
**Зигзаг → масса (T):** при локализованном vortex gate отражает фазу в ε-окрестности одного `v_p`; macro readout видит субсветовой зигзаг → `m_rest` (§5).
---
## §8. SM-константы (модель, не meta)
### 8.1 `v_p`-частица: Planck-арифметика (**уже сейчас**, не claim модели)
| Параметр | Формула | Статус |
|----------|---------|--------|
| объём | `v_p = (dl)³` | §5, §7 |
| масса | `m₀ = m_P` | **считается сейчас** из `ℏ,G,c` |
| заряд | `e₀ = e` | **измерено**; U(1) — один квант на дефект |
| частота | `ν₀ = 1/hT` | **считается сейчас** (~2.62×10⁴³ Hz) |
| энергоплотность при `\|z\|²=1` | `u_P = K_P` | **считается сейчас** |
Planck-строки — размерный факт. Элементар = один **`v_p`**. **§8.2:** α_fs и массы SM — алгебра из **`g`** + топологии.
### 8.2 SM-константы: **вычисляются** из `g` (не fitted)
#### SM → Planck
$$
\begin{aligned}
E_0 &= E_P/\sqrt{2}, &
 m_{\mathrm{arg}} &= m_P/\sqrt{2}, &
 \kappa &= 1/\sqrt{2}, \\
\alpha_{\mathrm{fs}}^{-1}
 &= 4\pi^{3}+\pi^{2}+\pi, &
 N_\varphi &= \lceil 4\pi\rceil = 13, &
 N_{\mathrm{hier}} &= 8, \\
a_Q &= 2^{-F}, &
 \rho_Q &= a_Q^{2}, &
 N_{12} &= 12, \\
v &= \alpha_{\mathrm{fs}}^{8}\,E_P\,\sqrt{2\pi}, &
 m_H^{(\mathrm{bare})} &= v/2, \\
\delta\lambda &= \alpha_{\mathrm{fs}}/(4\pi), &
 \lambda &= \tfrac18 + N_{\mathrm{hier}}\,\delta\lambda, &
 m_H &= \sqrt{2\lambda}\,v, \\
m_p &= \alpha_{\mathrm{fs}}\cdot\frac{v}{2}\cdot\Bigl(1+\frac{\kappa^{2}}{N_{12}}\Bigr), \\
m_e &= \alpha_{\mathrm{fs}}^{2}\cdot\frac{m_H}{N_\varphi}, \\
m_\nu^{(\mathrm{atm})}
 &= \alpha_{\mathrm{fs}}^{5}\cdot\frac{2m_H}{N_{\mathrm{hier}} N_\varphi}
 = \alpha_{\mathrm{fs}}^{3}\cdot\frac{m_e}{N_{\mathrm{hier}}/2}, \\
m_n &= m_p + 2 m_e, \\
\alpha_s(v) &= \frac{3}{8\pi}, &
 \frac{1}{\alpha(M_Z)}
 &= \alpha_{\mathrm{fs}}^{-1}-\frac{2\pi}{\ln 2}, \\
m_W &= \frac{e(M_Z)\,v}{2\sin\theta_W}, &
 m_Z &= \frac{m_W}{\cos\theta_W}.
\end{aligned}
$$
#### Постоянная тонкой структуры α
Из gate (§7.1): `α* = 1 + 1/(4π)` — vacuum residue `(α*−1) = Δφ_min/(2π) = 1/(4π)` на тик.
T-слой: EM-связность = **фазовый объём** emergent 3D readout над циклом `2π`; **`4π` = телесный угол**, не `|N|` тайла (§1.4.3):
```
α_fs⁻¹ = 4π³ + π² + π
α_fs = 1 / (4π³ + π² + π) ≈ 0.0072973363
```
**Не** заменять `4 → 6` при переходе на гекс — сломает CODATA.
| | Модель | CODATA 2018 |
|---|--------|-------------|
| `α_fs` | `0.007297336344…` | `0.0072973525693…` |
| `α_fs⁻¹` | `137.036303776…` | `137.035999084…` |
**Считается сейчас** — подставь `π`. Не sim, не knob. Честный хвост vs CODATA: $\Delta(\alpha^{-1})\approx 3\cdot 10^{-4}$ (~2 ppm на $\alpha$) — формула не подгонялась под цифры таблицы.
#### §8.2·α·meaning · Физический смысл (опора, не F-решётка)
**α — это:** безразмерная сила **фазового сопряжения** единичного заряда с вакуумом (EM-канал; телесный угол / фазовый объём emergent 3D).
**α — это не:** определение силы Ньютона; не hop-счёт сам по себе; не `κ/M` как первичное.
| слой | объект | статус |
|------|--------|--------|
| **смысл** | phase↔vacuum coupling | stamped |
| **нога** | `α*−1 = Δφ_min/(2π) = 1/(4π)` (пустая ячейка) | stamped |
| **число** | `α⁻¹ = 4π³+π²+π` (башня на `4π`) | stamped T-readout |
| **следствия** | `F=α F_P/N²`, `α=N_c/N_a0`, `α=κ/M` | readout / open M |
| **открыто** | дискретная доля сопряжения на FCC без continuum-`π` | `Φ_□` open · α_geom exploratory |
Тождество ноги: `4π·α = α/(α*−1)`.
**Код:** `SI.alpha_meaning_ask_row()` · verify **`Alpha_meaning_ask`**.
#### §8.2·α·descent · Забыть α — спуск из физики
**Амнезия:** не использовать `α_fs`, CODATA, π-полином как *вход*.
| шаг | физика | что вылезает |
|-----|--------|--------------|
| 1 | A5 + Heisenberg | `Δφ_min = 1/2` |
| 2 | цикл тика `2π` | residue `r = Δφ_min/(2π) = 1/(4π)`; `α* = 1+r` |
| 3 | emergent `d=3` | `Ω = 1/r = 4π`; `N_φ = ⌈Ω⌉ = 13` |
| 4 | FCC тело | `κ`, `N₁₂`, грани 6□+8△ |
| 5 | A10 | заряд `n∈ℤ` |
| 6 | ? | **безразмерное EM-сопряжение** |
**Нога — не α.** `r ≈ 0.08` — остаток пустого вакуума; тонкая структура на порядок меньше.
**Два завершения (score только AFTER):** continuum-башня на `Ω` → ~2 ppm; `α_geom=137` с граней → ~263 ppm. Сырые `r`, `r²`, `κ_link·r` — неверный масштаб.
**Открыто:** доля сопряжения заряда сквозь `Ω` из `g`/holonomy — не ansatz.
**Код:** `SI.alpha_descent_ask_row()` · verify **`Alpha_descent_ask`**.
#### §8.2·α·mass-defect · Оптика: дефект массы H = этаж КМ (DoD)
**КМ (предыдущий этаж):** $BE=\tfrac12\alpha^2 m_e c^2$, $\Delta m=BE/c^2$ ⇒
$$
\alpha=\sqrt{2\,\Delta m/m_e}.
$$
**На M (те же α, без новой ручки):**
| тождество | смысл |
|-----------|--------|
| $U(a_0)=\alpha E_P/N_{a0}=\alpha^2 m_e c^2$, $BE=U/2$ | вириал |
| $E_{\mathrm{coul}}(N{=}1)/E_0=\alpha/\kappa$ | сила на $F_0$ |
| $BE/E_0=\Delta m/m_{\mathrm{arg}}\ll 1$ | мягкий Arg-ledger, не один клик $E_0$ |
**Оптика:** coupling phase↔vacuum, посадка на $F_0$, hop-лестница и QM-дефект массы — **одно** α. Вывел любое без α — вывел все.
**DoD:** Arg-binding композита H на Λ должен coarse-grain к $\Delta m$. **Открыто:** $\Delta m$ из Arg-связи p–e **без** вставки α.
**Код:** `SI.alpha_mass_defect_optics_row()` · verify **`Alpha_mass_defect_optics`**.
#### §8.2·α·Arg-try · Попытка: Δm из Arg-ledger без α
**Тождество (мостик закрыт):** при $\alpha=\kappa/M$, $U=E_0/(M N_{a0})$, $BE=U/2$
$$
\frac{\Delta m}{m_{\mathrm{arg}}}=\frac{BE}{E_0}=\frac{1}{2 M N_{a0}},\qquad
\frac{\Delta m}{m_e}=\frac{\kappa^{2}}{2 M^{2}}=\frac{\alpha^{2}}{2}.
$$
Связь H — **один Arg-тик**, размазанный на $2 M N_{a0}$ (soft ledger).
**Try без вставки α:**
| кандидат | ppm на $\Delta m/m_e$ | вердикт |
|----------|----------------------|--------|
| $M=97$ → $\kappa^{2}/(2\cdot97^{2})$ | ~−2080 | лучший int (тот же F-ask) |
| $M=96=N_{12}N_{\mathrm{hier}}$ | ~+1.9e4 | хуже |
| $1/(2\cdot137^{2})$ | ~+526 | **reject** — inject α_geom |
| $1/(4\pi\cdot11)^{2}/2$ | ~−1.7e4 | **reject** — FP-exp не fraction |
| $m_e N_\varphi/(2 m_H)$ | ~0 | **reject** — circular ($m_e$ уже с α) |
**Итог try:** identity unifies F/H/QM; **вывод не закрыт** — нужны $M$ и/или $N_{a0}$ из $g$/shell без α.
**Код:** `SI.alpha_arg_binding_try_row()` · verify **`Alpha_arg_binding_try`**.
#### §8.2·α·Schwinger · Старт с лаборатории: $a_e=(g-2)/2$
**Дверь (Куш / Швингер → геоний):** меряют $a_e$, не α напрямую:
$$
a_e=\frac{\alpha}{2\pi}+O(\alpha^{2})\qquad\Rightarrow\qquad\alpha=2\pi\,a_e+\cdots
$$
**Рифма носителя (тождество):** нога вакуума $r=\Delta\varphi_{\min}/(2\pi)=1/(4\pi)$ (§8.2·descent) ⇒
$$
\frac{\alpha}{2\pi}=2\alpha\,r.
$$
Фактор $1/(2\pi)$ — геометрия тика (Heisenberg-пол), не fitted константа QED.
**DoD:** bare $g=2$ для вихря $n=\pm1$ (топология) + A5-dressing → $a_e$ **без** вставки α; тогда α сверху = $a_e/(2r)$.
Однопетлевой Швингер vs CODATA $a_e$ ~+1516 ppm — ожидаемый зазор высших петель.
**Код:** `SI.alpha_schwinger_ask_row()` · verify **`Alpha_schwinger_ask`**.
#### §8.2·α·g2·ask · Спросили носитель: bare $g=2$ и A5→$a_e$
**Метод:** как §8.2·H·ask — (1) факты вихря $n=\pm1$ + спинора; (2) что фиксирует гиромагнитное без α; (3) чем A5-ванна одевает момент; (4) reject / report.
**Опрос — ответы носителя:**
1. **A4 + A16 / §3.10:** spin-½ — $2\pi\to-1$, $4\pi\to+1$ (двойное накрытие).
2. **Thm 5.1:** $L_0=s_0=\hbar/2$ — один спиновый квант на $hV$.
3. **A10:** заряд $n=\pm1$ на электронном pra-vortex.
4. **Орбитальный baseline:** циркулирующий заряд → $g_{\mathrm{orb}}=1$ ($\mu=(Q/2m)L$).
5. **Bare $g_s=2$:** двойное накрытие удваивает магнитный отклик спина относительно орбиты → $g_s=g_{\mathrm{orb}}\times 2=2$. Тогда $\mu_{\mathrm{bare}}=e\hbar/(2m)=\mu_B$ при $|S|=\hbar/2$ — **без α**.
6. **Bare $a_e=0$:** аномалия — не топология, а **одевание** A5-ванной.
7. **Тождество двери:** $a_e^{(1)}=2\alpha\,r$, $r=\Delta\varphi_{\min}/(2\pi)$ — геометрия; сверху $\alpha=a_e/(2r)$.
8. **Отвергнуто:** $a_e\stackrel{?}{=}r$ или $2r$ (⇒ α=1 или ½); $a_e\stackrel{?}{=}|z|_{\mathrm{vac}}^2$ (масштаб $z_{\min}$, не ~$10^{-3}$); вставка α в dressing (круговое для DoD); CODATA $a_e$ как M-определение α.
9. **Открыто:** доля holonomy-облака A5 вокруг вихря → $a_e$ без α; затем α сверху.
**Код:** `SI.alpha_dirac_g2_ask_row()` · verify **`Alpha_dirac_g2_ask`**.
#### §8.2·α·ae·ask · Спросили носитель: A5-облако → $a_e$
**Метод:** (1) назвать облако на Λ; (2) безразмерный избыток μ; (3) α-free дроби; (4) reject / report.
**Опрос — ответы носителя:**
1. **Облако** = ореол $\rho_\Theta$ (Heisenberg §5.0.5) + канал $\Phi_\square$ (Stokes) + A5-ванна.
2. **Факторизация** (дверь Швингера = чтение носителя):
$$
a_e^{(1)}=\alpha\cdot 2r,\qquad 2r=\Delta\varphi_{\min}/\pi=1/(2\pi).
$$
Сопряжение × геометрия тика. **$2r$ закрыт**; **α = доля сопряжения — тот же OPEN**, что §8.2·α·meaning / descent.
3. **α-free tries** ($\Delta\varphi_{\min}/N_{\mathrm{ring}}$, $\kappa/N_{\mathrm{ring}}$, $r/N_{12}$, $\kappa^2/N_{12}$, …) — **неверный масштаб** (лучшие $|\mathrm{ppm}|\gg 10^3$).
4. **Отвергнуто как вывод $a_e$:** $2r/137$ и $\alpha/(2\pi)$ — вставка $\alpha_{\mathrm{geom}}$/α; сверху $\alpha=a_e/(2r)$ возвращает то же.
5. **Итог:** дверь Швингера **не обходит** OPEN сопряжения — только **режет** задачу: $\alpha=a_e/(2r)$ при известном $2r$. $a_e$ без α ⇔ дискретная доля сопряжения (или лабораторный $a_e$). Та же гора, острее факторизация.
**Код:** `SI.alpha_ae_cloud_ask_row()` · verify **`Alpha_ae_cloud_ask`**.
#### §8.2·α·Rydberg·Hall · Лаб-двери рядом со Швингером
**Двери:**
- **Ридберг:** меряют $R_\infty$ (спектроскопия H/D) → $\alpha^{2}$ при известных $m_e$ и QED-уровнях.
- **Холл / фон Клицинг:** плато $R_H=R_K/i$, $R_K=h/e^{2}$. До SI-2019: $\alpha=\mu_0 c/(2R_K)$. После 2019: $e,h$ точны ⇒ $R_K$ точен; **α из Холла больше не независим** ($\mu_0$ измеряется).
**Рифмы носителя (тождества):**
$$
R_\infty = \frac{\alpha^{2} m_e c}{4\pi\hbar} = \alpha^{2}\,\frac{r}{\bar\lambda_C},\qquad r=\frac{\Delta\varphi_{\min}}{2\pi}=\frac{1}{4\pi}.
$$
Та же нога $r$, что у Швингера; мощность $\alpha^{2}$ уже на hop-лестнице ($\alpha^{2}=N_{re}/N_{a0}$).
Фактор $2$ в $\alpha=\mu_0 c/(2R_K)$ — SI-ЭМ, **не** $2r$.
**Опрос:**
1. Обе двери **читают** α с прибора / метрологии — не выводят сопряжение на M.
2. Ридберг факторизует $\alpha^{2}\cdot r$ — $r$ закрыт; $\alpha^{2}$ снова OPEN (hops / coupling).
3. Холл post-2019 — эталон $h/e^{2}$, не источник α.
4. **Отвергнуто:** $R_\infty$ или optical $a_0$ как M-определение α; α из $R_K$ без $\mu_0/\varepsilon_0$-моста после 2019.
5. **Итог:** та же гора, что meaning / Schwinger / ae·ask.
**Код:** `SI.alpha_rydberg_hall_ask_row()` · verify **`Alpha_rydberg_hall_ask`**.
**π-guardrail (§8.2·geo):** непрерывный **`4π`** — **T-readout** (телесный угол), **не** метрика одной **`v_h`**. На **1-tick** теле **кубооктаэдра** живут **6□ + 8△**, **`V=(16/3)v_hV`**, **`κ=1/√2`** — см. **`SI.cuboctahedron_geometry_row()`**.
**Exploratory (π-free, cuboctahedron combinatorics — не ):**
```
α_fs⁻¹_geom = N₁₂(N₁₂+1) − n_△ − 2n_□ + 1 = 12·13 − 8 − 12 + 1 = 137
```
~260 ppm на **`α⁻¹`** vs CODATA (хуже stamped **`4π³+π²+π`** ~2 ppm). Открытый вывод: **`α_geom`** из **`g`** / Coulomb на FCC, не подменять молча.
#### §8.2·H·ask · Спросили носитель: **`N_a0`** (размер H в hops)
**Метод:** как §8.2·geo·ask / §5.2.6 phonon — (1) **`a=l_P`**; (2) выписать факты H; (3) что может задать размер **без** инъекции **`α`**; (4) ratio — отчёт.
**Уже stamped (не knobs):**
| # | факт | откуда |
|---|------|--------|
| H0 | Thm 5.2 — на M нет волны; звук/свет/перенос = **`n_k`**, **`n_E·E₀`** | §0.9 · §5.2.5 |
| H1 | H = составной узел: **`B=1`** + облако **`ρ_Θ`** электрона на оболочках **`Λ`** | §5.0.4–5.0.5 |
| H2 | Hop-лестница: **`N_re --α-- N_c --α-- N_a0`**, **`α=N_c/N_a0`**, **`α²=N_re/N_a0`** | §7.4 + Bohr rung |
| H3 | Масса: **`m_e=α² m_H/N_φ`** ⇒ **`α²=m_e N_φ/m_H`** | §8.2 электрон |
**Опрос — ответы носителя:**
1. **`N_a0 ∈ ℤ`** (hops **`hL`**). Иначе дыра в лестнице: звук квантуется, размер H — continuum.
2. **Масса и hop — одна мощность `α²`:** `N_re/N_a0 ≡ m_e N_φ/m_H`. Массы **не** фиксируют `N_a0` независимо от `α` (тождество, не второй источник).
3. **Отвергнуто:** `N_a0 = N_c·137` (`α_geom`). Вкладывает `α⁻¹` в размер → `α=N_c/N_a0` возвращает то же. Пусто для вывода `α`.
4. **Отвергнуто как определение M:** оптический `a₀` (CODATA) — T-якорь; годится как seed FP, не как носительный `N_a0`.
5. **Открыто:** целое из **структуры H** — ground **`ρ_Θ`** / **`N_pack=#(hV ⊂ атомного WS)`** / радиальная оболочка вокруг `p` на FCC **без** `α` и без оптического `a₀`.
**Код:** `SI.na0_h_carrier_ask_row()` · verify **`Na0_H_carrier_ask`**.
#### §8.2·F·ask · Спросили носитель: сила на решётке **`F₀`** → **`α`**
**Уже stamped:** Thm 5.1 — сила = **`n_F·F₀`**; **`F₀/F_P=κ`**; Кулон **`F=α F_P n₁n₂/N²`**.
**Связка (N=1, |n|=1):**
$$
\alpha F_P = F_0/M \quad\Rightarrow\quad \alpha = \kappa/M,\quad M\in\mathbb{N}.
$$
**Probe:** цель **`M=F₀/(α_c F_P)≈96.899`**.
| кандидат `M` | откуда | `α⁻¹` | vs CODATA |
|--------------|--------|-------|-----------|
| **96** = `N₁₂·N_hier` (= `n_△·N₁₂`) | links × hierarchy | ≈135.76 | ~+9366 ppm |
| **97** = `N₁₂·N_hier+1` | nearest int | ≈137.18 | ~−1040 ppm |
| `137/√2` | `α_geom` path | 137 | ~+263 ppm — **отвергнуто** (инъекция 137) |
**Итог:** путь живой — `α` из посадки Кулона на `F₀`. Лучший int по ppm — **97**; чистейшая комбинаторика — **96**. **`M` из `g` открыто**; π-ansatz (~2 ppm) **не** снят.
**Код:** `SI.alpha_force_lattice_ask_row()` · verify **`Alpha_force_lattice_ask`**.
#### Кулон из носителя (не fitted Maxwell)
**Уже закрыто** — закон не вводят отдельно, его **собирают**:
| кусок | где | смысл |
|-------|-----|--------|
| $Q = n\,e_0$, $n\in\mathbb{Z}$ | §5.2.2-III | топологический заряд (A10), не импульс |
| $\alpha_{\mathrm{fs}}=e_0^{2}/(4\pi\varepsilon_0\,\hbar c)=1/(4\pi^{3}+\pi^{2}+\pi)$ | §8.2 выше | фазовый объём / телесный угол |
| $4\pi$ | §1.4 · gate | телесный угол emergent 3D, не $\|N\|$ тайла |
| $F_P=\hbar c/\ell_P^{2}=c^{4}/G$ | §5.2.1 · Planck | силовая шкала ячейки |
| $\mathrm{div}\,j=0$ на звезде $N$ | §5.2.1 | локальный баланс потока |
Из определения $\alpha_{\mathrm{fs}}$:
$$
\frac{1}{4\pi\varepsilon_0}
=
\frac{\alpha_{\mathrm{fs}}\,\hbar c}{e_0^{2}}.
$$
Два заряда $Q_i=n_i e_0$ на расстоянии $r=N\ell_P$ ($N\in\mathbb{N}$ — graph-distance в $\ell_P$):
$$
\mathbf{F}_{12}
=
\frac{1}{4\pi\varepsilon_0}\frac{Q_1 Q_2}{r^{2}}\,\hat{\mathbf{r}}
=
\alpha_{\mathrm{fs}}\,(\hbar c)\,\frac{n_1 n_2}{r^{2}}\,\hat{\mathbf{r}}
=
\alpha_{\mathrm{fs}}\,F_P\,\frac{n_1 n_2}{N^{2}}\,\hat{\mathbf{r}}.
$$
На соседних ячейках ($N=1$, $|n_i|=1$): $|F|/F_P=\alpha_{\mathrm{fs}}\approx 1/137$ — без новой ручки.
**Дискретный Гаусс (тот же носитель).** Поток фазового тока через замкнутую оболочку вокруг pra-дефекта $\propto n=Q/e_0$. В 3D readout телесный угол $4\pi$ даёт закон $1/r^{2}$ — это **следствие** геометрии носителя + заряда A10, не отдельный Maxwell-axiom на M. Полный Maxwell — **следующий абзац** (тот же канал, не схлопнутый).
| кусок | статус |
|-------|--------|
| $F\propto n_1 n_2/N^{2}$ из $\alpha_{\mathrm{fs}}+F_P$ | выведено алгебра |
| Гаусс $\oint\!j\propto n$ на оболочке | выведено схема · sim двух зарядов |
**Фальсификация:** совпадение с Кулоном требует шкалы $\neq\ell_P$, константы $\neq\alpha_{\mathrm{fs}}$, или заряда $\notin e_0\mathbb{Z}$ — claim мёртв.
#### Планковский ЭМ (формулы на соседних ячейках)
Уже есть в носителе (§5.0.2 · §5.2.1). Не fitted Maxwell — **сбор** тех же кусков, что Кулон.
**Кванты на одном шаге** ($\ell_P$, $hT$):
$$
s_0 = \hbar/2,
\qquad
E_0 = s_0/hT = E_P/\sqrt{2},
\qquad
p_0 = s_0/\ell_P,
\qquad
F_0 = p_0/hT = m_{\mathrm{arg}}\, g_M.
$$
**Поля на связи / грани** (целые $n\in\mathbb{Z}$):
$$
E_i = -\frac{\varphi(x)-\varphi(x+\hat e_i)}{\ell_P}
\quad(\varphi=\mathrm{Arg}\,z),
\qquad
j_i = \frac{\mathrm{Im}\bigl(z^*(x)\,z(x+\hat e_i)\bigr)}{hT}.
$$
**Контур по грани $\square$** (дискретный $\oint$ = сумма по рёбрам):
$$
\Phi_\square
=
\sum_{e\in\partial\square}\!\Delta\varphi_e
=
\mathrm{Arg}\!\prod_{e\in\partial\square}\! z
\quad\in (-\pi,\pi],
\qquad
B_\square = \Phi_\square / \ell_P^{2},
\qquad
\sum_{e\in\partial\square} E_e\,\ell_P = -\Phi_\square.
$$
Это Stokes на решётке: циркуляция $E$ вокруг $\square$ = минус поток $B$ через $\square$. Большой контур = сумма граней внутри (телескопирование).
**Законы на решётке:**
$$
\frac{\Delta\rho}{\Delta t}+\mathrm{div}_\varepsilon\, j = 0,
\qquad
\oint_S j \propto n = Q/e_0,
\qquad
F = n_F F_0\ \ (n_F\in\mathbb{Z}).
$$
**Кулон на соседях** ($N=1$, $|n_i|=1$) — частный случай:
$$
|F|/F_P = \alpha_{\mathrm{fs}},
\qquad F_P = c^4/G.
$$
**Скорость фронта по осям** (не macro-$c$):
$$
c_0 = \ell_P/hT = \sqrt{2}\, c.
$$
Макро-Maxwell ($\nabla\cdot E$, $\nabla\times B$, свет $c$) — **следующий** абзац: те же величины после усреднения.
| кусок | статус |
|-------|--------|
| $s_0,E_0,p_0,F_0$ | выведено |
| $E_i$, $B_\square$, $j_i$, Гаусс, Кулон $N=1$ | выведено |
| sim plaquette на T-readout | открыто |
#### Макро-Максвелл (после усреднения)
Те же $E_i$, $B_\square$, $j$ → обычные поля на T. Четыре уравнения:
$$
\nabla\cdot\mathbf{E}\propto\rho_Q,
\quad
\nabla\cdot\mathbf{B}=0,
\quad
\nabla\times\mathbf{E}=-\partial_t\mathbf{B},
\quad
\nabla\times\mathbf{B}\propto\mathbf{J}+\partial_t\mathbf{E}.
$$
Свет (та же $c$, что §1.1 / §5.1.1 — не второй расчёт):
$$
c = \kappa\, c_0 = c_0/\sqrt{2},
\qquad \kappa=\kappa_{\mathrm{FCC}}=1/\sqrt{2},
\qquad K_P:=\mu_P c^2\ \Rightarrow\ c^2=K_P/\mu_P.
$$
| кусок | статус |
|-------|--------|
| макро-уравнения = readout планк. слоя | выведено |
| sim свободной волны | открыто |
**Закрытие калибровочного ЭМ.** Поля = фазы на рёбрах + циркуляция $\Phi_\square=\mathrm{Arg}\prod_{\partial\square} z$. Отдельного плавающего $A_\mu$ / «гостевого» Maxwell на M нет.
#### Lorentz / изотропия света (из упругости + binomial)
**Не** отдельная группа SO(3,1) на M. Macro-$c$ — **одна** цепочка; $K_P/\mu_P$ — не второй расчёт числа $c$:
$$
c = \kappa\, c_0 = c_0/\sqrt{2}
\qquad(\kappa=\kappa_{\mathrm{FCC}}=1/\sqrt{2}\ \text{(3+1)},\ c_0=\ell_P/hT),
$$
$$
K_P := \mu_P\, c^2
\quad\Rightarrow\quad
c^2 = K_P/\mu_P
\quad\text{(тождество гидродинамики, §5.1.1)}.
$$
**Гекс-срез (2+1):** то же $c=\kappa\,c_0$, но $\kappa_{\mathrm{hex}}=\sqrt{3}/2$, $c_0=(2/\sqrt{3})\,c$, $hT=(\sqrt{3}/2)\,t_P$ (§1.4.2) — не подставлять $\kappa_{\mathrm{FCC}}$ в срез на $N_6$.
Фотон на M = безмассовая мода бегущей фазовой голономии ($n=0$, $\Phi_\square$-фронт). Macro-readout — binomial $(1{-}2{-}1)$ по многим плакеткам (§4.1 · T1):
$$
\text{анизотропия рёбер FCC}
\ \xrightarrow{\ (1{-}2{-}1)\ }\
\text{макро-круг со скоростью } c.
$$
Контуры $\Phi_\square$ сшивают пространство: упругость среды на больших масштабах изотропна. T1 (`isotropy_ratio→1`) — численный след этой цепочки, не axiom «Lorentz вставлен».
| кусок | статус |
|-------|--------|
| $c=\kappa_{\mathrm{FCC}} c_0$; hex $\kappa_{\mathrm{hex}}=\sqrt{3}/2$; $K_P:=\mu_P c^2$ | выведено |
| фотон = holonomy $n=0$ | выведено схема |
| binomial → макро-круг (T1) | выведено §4.2 |
| количественный LIV/MDR budget (MAGIC/Fermi) | открыто |
#### Теория излучения (из носителя, не гостевой Planck-1900)
**Не** отдельный Lagrangian. Излучение = **сектор $n=0$** той же жидкости + readout на T.
**1 · Квант** (уже §5.0.2 / планк. ЭМ):
$$
s_0 = \hbar/2,
\qquad
E_0 = s_0/hT,
\qquad
\nu_0 = 1/hT,
\qquad
h\nu_0 = 4\pi\, E_0.
$$
Мода с частотой $\omega$ (фазовый ход T-dispersion):
$$
E(\omega) = \hbar\,\omega.
$$
**2 · Носитель фотона**
$$
\text{фотон} = \text{голономия }n=0\ (\Phi_\square\text{-фронт}),
\qquad
v_{\mathrm{link}}=c_0,
\qquad
v_{\mathrm{macro}}=c=\kappa\,c_0.
$$
**3 · UV-срез и два Rayleigh–Jeans** — 1-я зона Бриллюэна + «Дебай вакуума» (§5.2.4):
$$
|k| \lesssim \pi/\ell_P,
\qquad
\omega_D \sim 1/hT,
\qquad
N_{\mathrm{modes}} = \#\{k\in\mathrm{BZ}\} < \infty.
$$
| | формула | на M |
|--|---------|------|
| **continuum RJ** | $u(\omega)\propto\omega^{2} k_B T$, $\omega\to\infty$ | **нет** — мод вне BZ нет |
| **дискретный RJ** | на каждой моде $\langle E\rangle = k_B T$ (классика), $U=N_{\mathrm{modes}} k_B T$ | **геометрия допускает** (конечный $N_{\mathrm{modes}}$); **занятость не выведена** |
Дискретный RJ — **не закрыт**. Это только: BZ режет continuum-catastrophe. Сама формула $\langle E\rangle=k_BT$ на моде — классический угол **ещё открыто** Bose-занятости (§5). Квант моды $E=\hbar\omega$; при $\hbar\omega\gtrsim k_B T$ равнораспределение ломается — но мост $T\to\langle n\rangle$ в MODEL пока **не stamped**.
**4 · Излучение как процесс** (уже §5 аннигиляция):
$$
n=+1\ +\ n=-1 \;\rightarrow\; n_{\mathrm{net}}=0
\;+\;
\text{два macro-фронта давления со скоростью }c.
$$
Энергия не уничтожается (A3) — уходит волнами $n=0$.
**5 · Ответ модели: занятость — не закон M**
Спросили носитель (§2.1 · A3 · A16 · §5.2 · BZ):
$$
g\ \text{строго детерминирован (без RNG).}
\quad
\text{Вероятность / }\langle n\rangle\ \text{— T-readout, не микро-шаг.}
$$
Что M **закрывает** (без ансамбля):
| факт носителя | следствие |
|---------------|----------|
| ledger $\Delta E = n_E E_0$, $n_E\in\mathbb{Z}$ (§5.2) | энергия моды ступенями |
| scalar / photon limit $z\in\mathbb{C}$, $n=0$ (§2 A4 note · §8.2) | **нет** Паули на моде → $n=0,1,2,\ldots$ допустимы |
| A16 Паули | только совпадающие спиноры в одном $v_p$ — **не** на $n=0$-фронт |
| BZ + $\omega_D$ | $N_{\mathrm{modes}}<\infty$ → continuum-RJ мёртв |
Что M **не** закрывает:
$$
\langle n\rangle = \frac{1}{e^{\hbar\omega/k_B T}-1}
\quad\text{и}\quad
\langle E\rangle \xrightarrow[\hbar\omega\ll k_BT]{} k_B T
$$
— это **T-статистика** (грубый прибор + хаос из детерминизма, §2.1). $T$ в §5.3.1 — kinetic zigzag **macro-газа материи**, не stamped как температура фотонной ванны.
**Следствие:** дискретный RJ / Planck **не закрыты**. Закрыты лишь: квант, бозонный сектор $n=0$, конечный набор мод. Формулы $\langle n\rangle$, $u(\omega)$, Stefan — требуют T-readout.
**Упущения чеклиста** (вердикт не меняют; без них даже после Bose $u(\omega)$ не собрать):
| hinge | статус | зачем |
|-------|--------|-------|
| $\mu=0$ ($N_\gamma$ **не** аддитивный инвариант как $Q$; свободный $\gamma$ стабилен) | выведено схема: рождение/поглощение с материей (напр. аннигиляция → 2γ) · A3 на энергии | иначе не Bose, а $\mu\neq0$ |
| A6 + хаос §2.1 → равновесная мера на T | hinge, не формула | путь к ансамблю, не $\langle n\rangle$ |
| $T_{\gamma}$ vs $T$ gas (§5.3.1) | открыто контакт | одна $T$ в Planck — не stamped |
| $\omega(k)\approx c\|k\|$ (IR) · DOS | открыто (есть только $\omega_D$) | без DOS нет $u(\omega)\,d\omega$ |
| $g_{\mathrm{pol}}=2$ (поперечные) | открыто | фактор 2 в $u(\omega)$ |
| мода = осциллятор → $\langle E\rangle\to k_BT$ | открыто | классический угол RJ |
| кусок | статус |
|-------|--------|
| $s_0,E_0,h\nu_0=4\pi E_0$; $E=\hbar\omega$ | выведено |
| фотон $=n=0$ (bosonic limit); $c$ vs $c_0$ | выведено |
| BZ + $\omega_D$ режет continuum-catastrophe | выведено геометрия |
| $n=0,1,2,\ldots$ на моде (нет Паули) | выведено из A16-scope |
| $\mu=0$ ($N_\gamma$ ≠ инвариант; $Q$ сохраняется; свободный $\gamma$ стабилен) | выведено схема |
| аннигиляция → 2γ (два фронта) | выведено схема · sim |
| дискретный RJ $\langle E\rangle=k_BT$ | T-статистика, не закрыт |
| Bose $\langle n\rangle$ · $u(\omega)$ · Stefan/Wien | открыто (T + DOS + $g_{\mathrm{pol}}$) |
| $T_{\gamma}$, $\omega(k)$, $g_{\mathrm{pol}}=2$, A6→мера | hinges выше |
#### §8.2·vac · A5-ванна: тепло и спектр (≠ CMB)
**Не** отдельный Lagrangian. **A5-океан** (§0.5 · §5.3) = постоянно кипящий substrate; **CMB** (META §3.0) = last scattering **пузыря** на T — **другой объект**.
**1 · Плотность кипящего вакуума**
$$
|z|^2 \leftrightarrow \frac{\rho_E}{u_P},
\qquad
|z|_{\mathrm{vac}} = z_{\min} = 2^{-B_{\mathrm{amp}}},
\qquad
|z|_{\mathrm{vac}}^2\Big|_{\mathrm{spinor}} = 2\,z_{\min}^2
$$
$$
\rho_{E,\mathrm{vac}} = |z|_{\mathrm{vac}}^2\, u_P
\quad\text{(оба компонента спинора на } z_{\min}\text{)}.
$$
**2 · Спектральные якоря** (то же §8.2 radiation, не дублируем knobs):
$$
\nu_0 = \frac{1}{hT},\quad
\omega_0 = \frac{2\pi}{hT},\quad
\lambda_0 = \frac{c}{\nu_0} = \kappa\,\ell_P = \frac{\ell_P}{\sqrt{2}},
\quad
\omega_D \sim \frac{1}{hT},\quad
h\nu_0 = 4\pi E_0.
$$
**3 · Порядок «температуры» M-ванны** (если $\rho_E \equiv u=aT^4$, **оценка**, не Planck-1900):
$$
T_{\mathrm{M,bath}} \sim \left(\frac{\rho_{E,\mathrm{vac}}}{a}\right)^{1/4},
\qquad
T_{\mathrm{u_P,ceiling}} \sim \left(\frac{u_P}{a}\right)^{1/4}.
$$

$$
T_{\mathrm{CMB}} \approx 2.725\,\mathrm{K}
\quad\text{— T-наблюдение пузыря; не } T_{\mathrm{M,bath}}.
$$
**4 · Ask-model итог**
| кусок | статус |
|-------|--------|
| $\rho_{E,\mathrm{vac}}>0$, $z_{\min}$, $\nu_0$, $\omega_D$, BZ-cutoff | выведено §8.2·vac |
| $T_{\mathrm{M,bath}}$ order ($\sim 10^{31}\,\mathrm{K}$) | выведено algebra |
| $T_{\mathrm{M,bath}} \neq T_{\mathrm{CMB}}$ | выведено по слоям |
| Bose $\langle n(\omega)\rangle$, $u(\omega)$, Stefan | открыто (T + DOS) |
| $\omega(k)$ на BZ | §5.2.4 | открытый спектр |
#### §8.2·geo · Кубооктаэдр 1-tick: от ребра **`a`**
**Якорь (не ratio):** A1 фиксирует **12 NN на расстоянии `l_P`**. Выпуклая оболочка = **кубооктаэдр** с ребром
$$
a = \ell_P \quad\text{[m]}.
$$
**Все длины тела — функции `a`**, ratios — **производные**:
| при `a=l_P` | формула | SI |
|-------------|---------|-----|
| **`R_out`** | **`a`** | m |
| **`R_in`** (1-tick, □ limit) | **`κa = a/√2`** | m |
| **`A_□`** (одна □-грань) | **`a²`** | m² → **`B_□=Φ_□/a²`** (§8.2 Stokes) |
| **`S`** | **`6a²+8·(√3/4)a²`** | m² |
| **`V`** | **`(8/3)√2·a³`** | m³ |
| **`v_hV`** (узел FCC) | **`a³/√2`** | m³ |
| **`V/S`** | compactness | m |
**Потом** безразмерные (не наоборот):
$$
\kappa = \frac{R_{\mathrm{in}}}{R_{\mathrm{out}}} = \frac{1}{\sqrt{2}},
\qquad
\frac{V}{v_{hV}} = \frac{16}{3},
\qquad
\frac{V}{S\cdot a}\;\text{и}\;\frac{A_\square}{A_{\mathrm{tot}}}\;\text{— inventory/открыто.}
$$
**Эталон сопряжения (уже ):** **`κ(a)`** при **`a=l_P`** → **`hT=κ·t_P`**, **`E₀=κE_P`**, **`λ₀=κℓ_P`**. **`c=κc₀`** — **проверка readout**, не определение **`κ`** (§7.2). Не «нашли ratio в таблице» — **сначала длины тела**, потом **`R_in/R_out`**.
**Бум **`V=(16/3)v_hV`**** — следствие **двух объёмов при том же `a`**, не отдельный fit. Сопряжение с **`g`** — **открыто** (как **`κ`** уже сопряжён).
#### §8.2·geo·ask · Спросили носитель: **`a` → coupling**
**Метод:** (1) зафиксировать **`a=l_P`**; (2) выписать **размерные** величины тела; (3) спросить, **какая** входит в фазу/силу; (4) ratio — только отчёт.
| от `a=l_P` | размерная | ratio (производная) | coupling | статус |
|------------|-----------|---------------------|----------|--------|
| ребро | **`a=ℓ_P`** | 1 | A1 NN | якорь |
| inscr./circum. | **`R_in=κa`** | **`κ=1/√2`** | **`c,hT,λ₀`** | выведено |
| □-грань | **`A_□=a²`** | 1 | **`Φ_□`, `B_□=Φ_□/a²`** | открыто |
| per-link | 12 рёбер длины **`a`** | **`1/12`** | **`γ,ν_CA`** | выведено |
| узел | **`v_hV=a³/√2`** | **`1/√2`** vs **`a³`** | **`dV`** | выведено |
| hull | **`V=(8/3)√2·a³`** | **`V/v_hV=16/3`** | bulk budget | inventory |
| compactness | **`V/S`** [m] | **`V/(Sa)`** | surface/bulk | открыто |
| площади □ vs △ | **`6a²`, `8·(√3/4)a²`** | **`A_□/A_tot`** | EM weight | открыто |
| dihedral | **135°** | **3/4×180°** | ridge phase | открыто |
| **`α_fs`** | — | π-ansatz | T-readout | ограничение |
#### §8.2·geo·voronoi · Ромбический додекаэдр: ячейка FCC
**Не hull соседей** — **Вороной / WS** узла **`hV`**. Якорь тот же: **`a=l_P`** (12 NN на **`l_P`**; ребро каждого ромба **`a`**).
| при `a=l_P` | формула | SI |
|-------------|---------|-----|
| **`V`** (Voronoy) | **`a³/√2 = v_hV`** | m³ |
| **`R_in`** (до стены) | **`a/2`** | m — биссектриса **`|ON|/2`**, не **`κa`** hull |
| **`R_axis`** (ось ±x…) | **`a/√2 = κa`** | m — совпадает с **`R_in(hull)`** кубоокtaэдра |
| **`R_cubic`** (±,±,±) | **`a√(3/2)`** | m — дальняя вершина ячейки |
| **`A_◇`** (один ромб) | **`(2√2/3)a²`**, **`cos θ=1/3`** | m² |
| **`S`** | **`8√2·a²`** | m² |
| **`V/S`** | **`a/16`** | m |
**Dual к 1-tick hull:**
$$
12\ \text{NN} \leftrightarrow 12\ \text{ромбов},\quad
14\ \text{граней hull} \leftrightarrow 14\ \text{вершин WS},\quad
24\ \text{рёбер}.
$$
**Сшивка двух тел при том же `a`:**
$$
\frac{V_{\mathrm{cuboctahedron}}}{V_{\mathrm{Voronoy}}}
= \frac{V_{\mathrm{hull}}}{v_{hV}}
= \frac{16}{3},
\qquad
R_{\mathrm{in}}^{\mathrm{Voronoy}}
= \kappa\, R_{\mathrm{in}}^{\mathrm{hull}}
= \frac{a}{2}.
$$
**`κ=1/√2`** — macro readout по **□-грани hull**; **стена Voronoy** ближе: **`a/2`**. Осевая вершина WS лежит на расстоянии **`κa`** — там же, где **inradius hull**.
#### Теория распада (из носителя, не гостевой Γ=ℏ/τ)
**Не** отдельный S-matrix. Распад / аннигиляция — **каналы $g$** на топологии + энергия (A3 · A10 · A11).
**0 · Два разных процесса** (путать нельзя):
| | что происходит | $N_\gamma$ | топология |
|--|----------------|------------|----------|
| **аннигиляция** | два солитона встречаются: $n=+1$ + $n=-1$ | $0\to 2$ | $n_{\mathrm{net}}\to 0$, замок схлопывается |
| **распад** | **один** узел → другие узлы/фронты | может расти | меняется **внутренняя** структура (канал), не пара ± |
| **свободный $\gamma$** | один фронт $n=0$ в вакууме | const | **некуда** распадаться |
Аннигиляция (§5.0.3) **уже** stamped — это **не** распад фотона и **не** «$Q$ течёт».
**1 · Спросили носитель: когда стабилен?**
$$
\text{A10: } n\in\mathbb{Z}\ \text{на удалённой петле.}
\quad
\text{A11: anti-smear держит ядро.}
$$
| объект | стабильность на M |
|--------|-------------------|
| свободный $\gamma$ ($n=0$-фронт) | выведено **стабилен** — уже vacuum-сектор; нет более лёгкого «низа» |
| одиночный vortex $n=\pm 1$ (e±-схема) | выведено **топо-защита**: один не схлопнется в $n=0$ без партнёра |
| пара $n=+1$ с $n=-1$ на сближении | → аннигиляция (§5.0.3), не «распад одного» |
| составной / возбуждённый узел (μ, π, …) | **может** иметь канал вниз — слабый класс stamped (§8.2·6); конкретный Γ/узел открыто |
| нейтральный $B=1$ (нейтрон-схема) | выведено класс $n\to pe\bar\nu$ разрешён; выведено $m_n=m_p+2m_e$ · downhill |
**2 · Что такое «распад» в M**
$$
X \;\rightarrow\; Y_1 + Y_2 + \cdots
\quad\text{допустимо, только если}
$$
- энергия: $\sum E(Y_i) \le E(X)$ (ledger $n_E E_0$, A3);
- сохраняются **аддитивные инварианты** канала: $Q$ (A10), и те $B,L,\ldots$, что stamped как winding/обёртка;
- есть **геометрический ход** $g$ (слабый: смена обёртки / $P_L$ — §8.4; не RNG).
**Не** закон M:
$$
N(t)=N_0\,e^{-t/\tau},
\qquad
\Gamma=\hbar/\tau,
\qquad
\mathrm{BR}_i = \Gamma_i/\Gamma.
$$
Это **T-статистика** многих одинаковых систем (§2.1): $g$ детерминирован; «случайный момент распада» — грубый прибор + хаос, как $\langle n\rangle$.
**3 · Итог опроса**
| кусок | статус |
|-------|--------|
| аннигиляция $\neq$ распад; свободный $\gamma$ стабилен | выведено |
| топо-защита одиночного $n=\pm 1$ | выведено схема |
| **нет stamped хода $\Delta B\neq 0$** ($p\not\to e^+\pi^0$) | выведено ask-model §8.2·5 |
| аннигиляция → 2γ | выведено схема · sim |
| кинематика $E$, сохранение $Q$ | выведено рамка |
| слабый канал как смена обёртки (§8.4) | выведено схема (есть угол) |
| **слабый $\Delta B=0$:** класс $n\to pe\bar\nu$ **разрешён** | выведено ask-model §8.2·6 |
| **$m_n=m_p+2m_e$** (ledger; $k=2$) · $m_n>m_p+m_e$ | выведено ask-model §8.2·7 |
| $\Gamma$, $\tau$, BR, Fermi golden rule | открыто (T + амплитуда) |
| явный map $n\mapsto(p,e,\nu)$, $\mu\to e\nu\bar\nu$ | открытый вывод |
**Связь с $\mu_\gamma=0$:** в аннигиляции $N_\gamma$ растёт, потому что **два** топозаряда схлопнулись и энергия ушла в фронты — не потому что «$Q$ не сохраняется» и не потому что $\gamma$ нестабилен.
**4 · Лемма: $m\ll m_P$ $\not\Rightarrow$ «должен распадаться»**
Кирпич $v_p$ **неделим** (нет $r<\ell_P$). Известные частицы — **не** «тяжёлые куски $m_P$», а **лёгкие** топо-возбуждения:
$$
m_e = m_P\,\alpha_{\mathrm{fs}}^{2}\,f_{\mathrm{геом}}(e)
\quad\Rightarrow\quad
m_e/m_P \sim 10^{-22}\ll 1.
$$
Далеко до предела делимости значит: есть **место** для иерархии узлов / внутренних степеней (Bekenstein $B_{hV}$, составной soliton из многих $hV$). **Не** значит: каждый такой узел обязан иметь канал вниз.
Стабильность = **нет** допустимого хода $g$ с меньшей энергией при тех же аддитивных инвариантах ($Q$, и stamped $B,L,\ldots$), не близость к $m_P$:
| | почему не «обязан» распасться |
|--|------------------------------|
| $e^-$ | легчайший с данным $Q$ (+ $L$, если stamped) — некуда вниз |
| свободный $\gamma$ | уже $n=0$-сектор |
| $p^+$ | легчайший с $B=1$ — распад $p\to e^+\pi^0$ требует **нарушения $B$**; такого хода у stamped $g$ **нет** (ask-model ниже) |
«Распасться на кирпичи» тоже неверно: кирпич — **ячейка решётки**, не свободная частица $m_P$. Масса узла — zigzag+$K_P$ конфигурации, не сумма $N_{\mathrm{cells}}\,m_P$.
**5 · Ask-model: есть ли у $g$ ход с $\Delta B\neq 0$?**
**Вопрос.** Канал $p\to e^+\pi^0$ сохраняет $Q$ ($+e\to +e+0$), но меняет барионное число ($B\colon 1\to 0$) и лептонное ($L\colon 0\to -1$). Есть ли на носителе геометрический ход, который это делает?
**Что уже stamped (не угадайка):**
| кусок | роль для $B$ |
|-------|----------------|
| **§8.4.1-D3** | $B$-класс = **confining** узел: girth FCC-plaquette $=d=3$, кратность фокусов $=d$; `fcc_nn_plaquette_row` |
| **таблица сил · слабое** | хиральность $P_L/P_R$ + **смена оболочки /$n$** внутри узла — **не** роспуск $d$-фокусного замка |
| **аннигиляция §5.0.3** | только $n=+1$ с $n=-1$ → $n_{\mathrm{net}}=0$ (пара) — **не** одиночный $B\to 0$ |
| **A10** | $\oint\mathrm{d}\,\mathrm{arg}=2\pi n$ на удалённой петле — охраняет U(1)-$n$, не даёт GUT- unfolding |
| **A11 · Pauli+$K_P$** | держат ядро / запрещают smear — **усиливают** замок, не открывают $\Delta B$ |
**Ответ носителя:** stamped $g$ **не содержит** хода $\Delta B\neq 0$. Слабый канал меняет оболочку/базис при **том же** confining-классе; сильный — собирает/держит составной узел на girth-$d$; аннигиляция требует антипартнёра. Роспуск $B=1\to$ лептон+мезон потребовал бы отдельного unfolding confining winding — **не выведен** из A1–A16 / FCC / gate.
**Следствие:** $p^+$ стабилен не из $m\ll m_P$, а потому что **нет допустимого downhill-$g$** с сохранением stamped инвариантов (включая confining/$B$-класс).
**Что остаётся открыто:** полный sim-census (§8.4.1-D3) — мог бы *опровергнуть*, найдя устойчивый переход с кратностью $\neq d$ или явным $\Delta B$. Пока census не дал — **запрет $\Delta B$ остаётся схемой носителя**, не «временной дырой».
**6 · Ask-model: есть ли слабый ход $n\to p\,e\,\bar\nu$ при $\Delta B=0$?**
**Вопрос.** Свободный нейтрон в природе распадается: $n\to p\,e^-\,\bar\nu_e$. Инварианты:
$$
B\colon 1\to 1,\qquad Q\colon 0\to(+1)+(-1)+0,\qquad L\colon 0\to 0.
$$
Есть ли у stamped $g$ такой класс хода — или он тоже «открыто = запрет»?
**Что уже stamped:**
| кусок | роль |
|-------|------|
| **таблица сил · слабое** (§8.4) | $P_L/P_R$ + **смена оболочки /$n$**; заряженный ток ↔ партнёры SU(2)-flip ($W$) |
| **§3.11** | хиральная упаковка $P_L/P_R$ на спиноре — носитель слабого flip |
| **§8.2·2** | распад допустим, если $E\downarrow$ + инварианты + геометрический ход |
| **§8.2·5** | $\Delta B\neq 0$ **запрещён** — этот канал **не** того класса |
| **§8.4.1-D3** | $B$-класс (girth-$d$) **сохраняется**: $n$ и $p$ — оба confining $B=1$, разный EM/$Q$-сектор оболочки |
**Ответ носителя (два слоя):**
1. **Класс хода — да, stamped.** Слабый $g$ как раз умеет менять оболочку/$Q$ внутри **того же** $B=1$, выбросив лептонный ток (заряженный SU(2)-flip + $P_L$). Это **не** GUT-роспуск и **не** аннигиляция. Схема $n\leftrightarrow p$ + лептоны — **разрешена** носителем.
2. **Конкретный свободный $n$ обязан распасться — энергия закрыта.** Нужны:
 - идентификация нейтрального $B=1$ узла (нейтрон) vs заряженного (протон) в census;
 - **$m_n=m_p+2m_e>m_p+m_e$** (downhill по A3) — ask-model §8.2·7;
 - явный tick-map $n\mapsto(p,e,\bar\nu)$ и $\Gamma/\tau$ — T-статистика, открыто.
**Следствие:** протон стабилен (§8.2·5), нейтрон **не** топо-защищён так же: у него есть stamped downhill-*класс*, как только mass-split закроет энергию. Пока $\Gamma$ открыто — «$\tau_n\approx 880\,\mathrm{s}$» не выводим, но **запрета канала нет** и порог $\beta$ выполняется ($m_n=m_p+2m_e$).
**Фальсификация:** если sim/census покажет, что нейтральный $B=1$ не связан заряженным током с $p$, или что слабый flip не может родить $(e,\nu)$ при сохранении $L$ — правим §8.4, не SM-таблицу.
**7 · Ask-model: чему равна $m_n$ при квантовании массы?**
**Вопрос.** Порог $\beta$ — арифметика $m_n\,?\,m_p+m_e$, если $m_n$ в том же планковском мире. Непрерывные EM/Weinberg-ansätze не вынуждены. Что заставляет целый ответ?
**Что уже stamped:**
| кусок | роль |
|-------|------|
| **§5.0.2 · `m_arg`** | квант массы на hV: $m_{\mathrm{arg}}=m_P/\sqrt{2}$; **§5.2.1:** $\Delta m\in m_{\mathrm{arg}}\cdot\mathbb{Z}$ |
| **§5.2.3 · `a_Q`,`ρ_Q`** | квант амплитуды/плотности: $a_Q=2^{-F}$, $\rho_Q=a_Q^{2}$; $\rho$ меняется целыми пакетами |
| **§5.0 / §5.2.2** | $E\in E_0\cdot\mathbb{Z}$, $b\in\{0,1\}$, $n\in\mathbb{Z}$; float-knob mass-split — дыра в решётке |
| **§8.2·6** | $n$ и $p$ — один $B=1$, разный $Q$; слабый flip разрешён |
| **§8.2 электрон** | легчайший **заряженный** *канал*-квант (T-readout поверх $m_{\mathrm{arg}}$): $m_e=\alpha_{\mathrm{fs}}^{2}m_H/N_\varphi$ |
| **A3** | downhill только если $\sum E(Y_i)\le E(X)$ |
**Ответ носителя:**
1. **Два слоя.** На M уже есть лестница $m_{\mathrm{arg}}$ и $\rho_Q$ — это не новая аксиома для $n$. Для *изоспин-партнёров* $B=1$ шаг split на T — $m_e$ (легчайший заряд, который слабый канал умеет родить). Не $E_0$ (слишком грубо) и не непрерывный $\alpha m_p\cdot\sin^2\theta_W$.
$$
m_n-m_p = k\,m_e,\qquad k\in\mathbb{Z}_{+}.
$$
2. **Порог $\beta$:** $m_n>m_p+m_e$ $\Leftrightarrow$ $k\ge 2$. Рунг $k=1$ — порог с $Q=0$ (нет фазы для свободного распада).
3. **Основное состояние свободного $n$** — **минимальный** разрешённый рунг: $k=2$. Смысл без кварков: $Q=0$ vs $Q=+1$ на том же confining-замке требует компенсирующей $\pm$-структуры (пара легчайших зарядов) → ровно $2m_e$; слабый flip освобождает $e^-\bar\nu$ и оставляет $Q=+1$ на узле.
$$
m_n = m_p + 2 m_e
\approx 0.9363\,\mathrm{GeV}.
$$
| величина | модель | PDG-ish |
|----------|--------|--------|
| $m_n$ | ≈ **0.9363 GeV** | ≈ **0.9396 GeV** |
| $m_n-m_p$ | ≈ **1.027 MeV** ($2m_e$) | ≈ **1.293 MeV** |
| $m_n-(m_p+m_e)$ | ≈ **0.513 MeV** ($=m_e$) | ≈ **0.782 MeV** |
| rel на $m_n$ | ≈ **0.35%** | — |
**Отвергнуто dig:** EM $\sum q^{2}$ один (неверный знак); $\delta_{\mathrm{pack}}$ один ($\alpha m_p/24<m_e$ → запретил бы $\beta$); fitted float $\times 3/13$ без ledger.
**Не претендует:** точный PDG-$\Delta$ (хвост ~20% на split — тот же класс FCC/binomial, что у $m_p$); $\Gamma/\tau$; явный tick-map.
**Ломает:** $k\notin\mathbb{Z}$; квант $\neq m_e$ при живом §5.2; $k=1$ как свободный $n$; возврат к continuous EM-ansatz как .
#### Массы lepton / baryon (структура + топология узла)
##### Электрон — та же матрёшка: `α² · m_H / N_φ`
**Уже задано (не knobs):**
| # | Утверждение | Откуда |
|---|-------------|--------|
| **E1** | `m_H^(bare)=v/2`, stack `λ=1/8+N_hier·δλ` | §8.3.1 |
| **E2** | `N_φ = ⌈2π/Δφ_min⌉ = ⌈4π⌉ = 13` | §3.12.6 · `hv_bit_budget` — дискретный фазовый бюджет одного `hV` |
| **E3** | `α_fs` | §8.2 телесный угол / фазовый объём |
**Bare:** лёгчайший заряд `n=±1` — **два** EM-фактора на половину vev (лептонный канал), размазанные по **всему** фазовому бюджету кирпича:
$$
m_e^{(\mathrm{bare})}
= \alpha_{\mathrm{fs}}^{2}\cdot\frac{v}{2}\Big/ N_\varphi
= \alpha_{\mathrm{fs}}^{2}\cdot\frac{m_H^{(\mathrm{bare})}}{N_\varphi}
\approx 0.5040\,\mathrm{MeV}.
$$
**С vacuum-bit stack** (тот же empty-cell, что у Хиггса — не новый soft-wall):
$$
m_e
= \alpha_{\mathrm{fs}}^{2}\cdot\frac{m_H}{N_\varphi}
\approx 0.5133\,\mathrm{MeV}
\quad(m_H\approx 125.31\,\mathrm{GeV}).
$$
| величина | bare `/N_φ` | `+ δλ`-stack | PDG |
|----------|-------------|--------------|-----|
| `m_e` | ≈ 0.5040 MeV | ≈ **0.5133 MeV** | ≈ **0.5110 MeV** |
| rel | ≈ 1.37% | ≈ **0.45%** | — |
**Почему `α²` и `/N_φ`, а не как у протона:** электрон — элементарный pra-дефект в **одном** `v_p` (не confining-оболочка). Масса = EM² × пена / фазовый бюджет кирпича. Протон: EM¹ × packing `κ²/N₁₂` на оболочке. Оба читают ту же `m_H`-пену.
**`f_геометрия(e)` теперь следствие**, не вход:
$$
f_{\mathrm{геом}}(e)=\frac{m_H}{E_P\,N_\varphi}
\quad\Rightarrow\quad
m_e=m_P\,\alpha_{\mathrm{fs}}^{2}\,f_{\mathrm{геом}}(e).
$$
Старый CODATA-invert `f≈7.86×10⁻¹⁹` — check; предсказание ≈ `7.75×10⁻¹⁹` (bare) / через stack ближе.
**Лемма:** непрерывный `4π` дал бы `α² m_H/(4π)` (~2–4%); M дискретен ⇒ знаменатель **`N_φ=⌈4π⌉`**, не fitted 13.
**Ломает:** отказ от `v`/`m_H`; `N_φ≠⌈4π⌉`; soft-wall; возврат к CODATA-`f` как аксиоме.
**Не претендует:** `m_μ/m_e`, `m_τ`; хвост ~0.45% (тот же класс FCC/binomial).
##### Нейтрино — глубже по `α` и ёмкости кирпича
**Уже задано (не knobs):** E1–E3 + **`N_hier=⌊B_hV⌋−1=8`** (§8.3.1 / §8.4.1-A).
Заряженный лептон взял **`α²·m_H/N_φ`**. Нейтральный (`n=0`) не сидит на EM² — ещё **три** α-ступени относительно `m_e`, размазанные по **половине** иерархического бюджета кирпича (тот же `/2`, что в `m_H=v/2`):
$$
m_\nu^{(\mathrm{atm})}
= \alpha_{\mathrm{fs}}^{3}\cdot\frac{m_e}{N_{\mathrm{hier}}/2}
= \alpha_{\mathrm{fs}}^{5}\cdot\frac{2\,m_H}{N_{\mathrm{hier}}\,N_\varphi}
= \alpha_{\mathrm{fs}}^{5}\cdot\frac{v_{\mathrm{eff}}}{N_{\mathrm{hier}}\,N_\varphi}
\approx 0.04986\,\mathrm{eV},
$$
где `v_eff=2 m_H` (stack). **Bare** на том же `v` из §8.3.1:
$$
m_\nu^{(\mathrm{atm,bare})}
= \alpha_{\mathrm{fs}}^{5}\cdot\frac{v}{N_{\mathrm{hier}}\,N_\varphi}
\approx 0.0490\,\mathrm{eV}.
$$
| величина | bare (`v`) | `+ δλ`-stack | √\|Δm²₃₁\| (PDG-ish) |
|----------|------------|--------------|----------------------|
| `m_ν^(atm)` | ≈ 0.0490 eV | ≈ **0.04986 eV** | ≈ **0.0500 eV** |
| rel | ≈ 2.0% | ≈ **0.21%** | — |
**Мост к электрону (тождество):** `α³·m_e/(N_hier/2) ≡ α⁵·2m_H/(N_hier N_φ)` — не новый knob, следствие `m_e=α² m_H/N_φ`.
**Почему глубже, чем `e`:** нет заряда `n=±1` → нет ведущего EM²; масса = остаток через α-иерархию кирпича × фазовый бюджет. `N_hier` и `N_φ` — те же ёмкости, что строят `v` и `m_e`.
**Леммы (не hard-gate):**
| лемма | формула | число | к чему |
|-------|---------|-------|--------|
| solar √Δm²₂₁ | `α³·m_e/(N_φ√π)` | ≈ 0.00866 eV | √Δm²_sol ≈ 0.00865; `√π` уже в `v∝√(2π)` |
| lightest NH floor | `α⁴·m_e/N_φ` | ≈ 1.12×10⁻⁴ eV | почти безмассовый `m₁` |
**Ломает:** отказ от `m_e`/`m_H`/`N_hier`/`N_φ`; soft-wall; fitted степень α.
**Не претендует:** PMNS углы; Majorana/Dirac; абсолютная иерархия NH/IH как теорема (atm-шкала — да).
##### Протон — тот же фазовый контур, что `m_H = v/2`
**Bare (топология, не knob):** лёгчайший узел с `B=1` сидит на той же T-пене, что даёт Хиггс; его rest-mass readout — **EM-остаток фазового объёма** `α_fs` на половину vev:
$$
m_p = \alpha_{\mathrm{fs}}\cdot\frac{v}{2}
= \alpha_{\mathrm{fs}}\cdot m_H^{(\mathrm{bare})}
\approx 0.8979\,\mathrm{GeV}.
$$
Каскад уже закрыт в §8.3.1 / §8.4.1: `v = α_fs⁸ E_P √(2π)`, `m_H^(bare)=v/2`. Новый множитель — только **`α_fs`** из §8.2 (телесный угол / фазовый объём), не fitted `f`.
| величина | модель | PDG |
|----------|--------|-----|
| `m_p` bare | ≈ **0.8979 GeV** | ≈ **0.9383 GeV** |
| rel | ≈ **4.3%** | — |
**Смысл:** confining узел (girth `d=3`, §8.4.1-D3) живёт на сильном канале, но **масштаб массы**, который macro-T видит как `m_p`, — EM-доля той же упругой половины `v`, что фиксирует `m_H`. Параллель: `m_H = 1·(v/2)`, `m_p = α_fs·(v/2)`.
**Старый placeholder** `m_p = m_P·(α_fs/3)·f_геометрия` — инверсия CODATA, не вывод. `f` теперь **следствие** bare-формулы, не вход.
##### Stack (планковский мир): `κ²/N₁₂` — вписанная сфера, не soft-wall
**Уже задано (§1.1 / §1.6.2), не knobs:**
| # | Утверждение | Откуда |
|---|-------------|--------|
| **P1** | `κ_FCC = 1/√2` | вписанная сфера кубооктаэдра / изотропный T-фронт; `c = κ·c₀` |
| **P2** | `N₁₂ = 12` | equal light-like NN FCC (§1.6.1) |
| **P3** | `κ² = 1/2` | квадрат packing-кванта |
**Packing-residue на оболочке.** Macro-T, читая массу confining-узла, видит не только EM-долю `α·v/2`, но и **изотропный packing-остаток** той же 1-tick оболочки, что даёт `κ` для света. Один полный квант `κ²` делится поровну на `N₁₂` причинных ребер:
$$
\delta_{\mathrm{pack}}
= \frac{\kappa_{\mathrm{FCC}}^{2}}{N_{12}}
= \frac{1/2}{12}
= \frac{1}{24}.
$$
**Лемма (не отдельный постулат):** у кубооктаэдра `|E|=2 N₁₂=24` (каждая из 12 вершин degree 4) ⇒ `1/|E| = κ²/N₁₂`. Считать «просто 1/24 рёбер» без `κ` — тень той же алгебры, не корень.
**Исправленная масса:**
$$
m_p
= \alpha_{\mathrm{fs}}\cdot\frac{v}{2}\cdot\Bigl(1+\frac{\kappa^{2}}{N_{12}}\Bigr)
= \alpha_{\mathrm{fs}}\cdot m_H^{(\mathrm{bare})}\cdot\Bigl(1+\frac{1}{24}\Bigr)
\approx 0.9353\,\mathrm{GeV}.
$$
| величина | bare `α·v/2` | `+ κ²/N₁₂` | PDG |
|----------|--------------|------------|-----|
| `m_p` | ≈ 0.8979 GeV | ≈ **0.9353 GeV** | ≈ **0.9383 GeV** |
| rel | ≈ 4.3% | ≈ **0.32%** | — |
Параллель с Higgs empty-cell (§8.3.1): там vacuum-bit `δλ=α/(4π)` на иерархии; здесь packing-bit `κ²/N₁₂` на FCC-оболочке. Оба — планковская геометрия носителя, не fitted.
**Ломает формулу:** отказ от `v` / `m_H=v/2`; `κ≠1/√2`; `N₁₂≠12`; soft-wall «поля занятости»; подмена `κ²/N₁₂` на fitted `7π` и т.п.; возврат к CODATA-`f`.
**Не претендует:** хвост ~0.32% (FCC/binomial, как у Higgs ~0.045%); полный census барионных узлов (§8.4.1-D3). Split $m_n-m_p$ — §8.2·7.
#### Таблица «считается сейчас»
| Константа | Формула | Статус |
|-----------|---------|--------|
| `l_P`, `t_P`, `hT`, `m_P`, `μ_P`, `u_P`, `K_P` | Planck + fluid | §8.1 |
| `α*`, `ε`, `ω` | gate §7.1 | §7.1 |
| **`α_fs`** | **`1/(4π³+π²+π)`** | **считается** |
| **`m_e`** | **`α_fs²·m_H/N_φ`** | **считается** |
| **`m_ν^(atm)`** | **`α_fs⁵·2m_H/(N_hier N_φ)`** | **считается** |
| **`m_p`** | **`α_fs·v/2·(1+κ²/N₁₂)`** | **считается** |
| **`m_n`** | **`m_p+2m_e`** | **считается** |
**Проверка M→T (§4.0.1):** `α²·m_H/N_φ` → `m_e` **без** нового knob (`f_геом` = следствие).
**Не meta:** электрон, протон, α, иерархия кварков — **целевые observables модели M→T→SM**.
### 8.3 Higgs: T-пена, не M-первопричина массы
**Стандартная картина (macro):** scalar **Higgs** → Yukawa → **`m`** частиц.
**Наша M→T иерархия (§5.0.1):**
```
M Arg(⟨z⟩/z) + K_P + зигзаг в hV — истинный механизм инерции
 ↓ coarse + resonance
T m_rest, effective scalar «Higgs-like» fluctuation
 ↓ accelerator readout
SM m_H boson — macro wave packet, not micro root
```
**Энергетическая иерархия (§5.0.2):** один **`Arg`-квант** несёт **`E₀ = E_P/√2`** — на **~16 порядков** выше **`m_H`**. LHC видит **T-конгломерат**; M-переносчик — **`s₀=ℏ/2`**.
#### 8.3.1 Топологический смысл: `m_H = v/2 = m_P·α_fs⁸·√(π/2)`
**Уже теоремы / леммы (посылки, не knobs):**
| # | Утверждение | Откуда |
|---|-------------|--------|
| **T1** | `N_hier = ⌊B_hV⌋ − 1 = 8` | §8.4.1-A: бит-бюджет кирпича минус **один бит** `b` |
| **T2** | 8 равноправных каналов α_fs-ослабления | §8.4.1-A |
| **T3a** | `v_peak = α_fs^{N_hier} E_P ≈ 98.17 GeV` | §8.4.1-A — пиковое локальное напряжение решётки |
| **T3b** | `v = v_peak · √(2π)` | §8.4.1-B — Gaussian macro-binomial (пик ↔ интеграл) |
| **T4** | на T-дереве SM: `m_H = √(2λ)·v` | readout; не новый закон `g` |
##### Что **не** поле занятости
§5.0: объём занимает **`z`** (`ρ_field = μ_P·|z|²`). `b∈{0,1}` — бит pra-узла, не continuous soft-wall. Higgs на T — **пена на Arg-океане** (§5.0.1), не первичное scalar-поле.
##### Каскад: пик → геометрия среза → `v/2`
**Базовый каскад (T3a):** множитель `m_P·α_fs⁸` — пиковое локальное напряжение
$$
v_{\mathrm{peak}}=\alpha_{\mathrm{fs}}^{8}\,E_P\approx 98.17\,\mathrm{GeV}.
$$
**Геометрия среза (T3b):** vev — тот же пик, прочитанный как интеграл стандартной нормали:
$$
v=v_{\mathrm{peak}}\cdot\sqrt{2\pi}.
$$
**Тождественность массы и половины vev.** Замкнуть слабую шкалу — отождествить массу бозона с пиком, умноженным на half-Gaussian фактор `√(π/2)`:
$$
m_H
= v_{\mathrm{peak}}\cdot\sqrt{\frac{\pi}{2}}
= \frac{v}{\sqrt{2\pi}}\cdot\sqrt{\frac{\pi}{2}}
= \frac{v}{2}
\approx 123.04\,\mathrm{GeV}.
$$
Алгебра `√(π/2) / √(2π) = 1/2` — лемма из §8.4.1-B. Содержание — **физическое чтение:** в CA бозон Хиггса — фундаментальная **симметричная деформация** упругой среды, которая удерживает пра-решётку от коллапса, «съедая» ровно **половину** доступной энергии локального электрослабого вакуума (`v/2`). Не soft-wall поля занятости; не новый knob.
##### SM-readout запирает `λ`
В QFT `m_H = √(2λ)·v`. Подстановка `m_H = v/2`:
$$
\sqrt{2\lambda}=\frac12
\implies
\lambda=\frac18=0.125=\frac{1}{N_{\mathrm{hier}}}.
$$
`λ` здесь — **не** knob самодействия, а SM-имя для уже зафиксированного отношения `m_H/v = 1/2`, запертого топологией упругой среды + Gaussian M→T. Совпадение с `1/N_hier` — то же целое 8 из §8.4.1-A, не отдельный постулат soft-wall.
##### Механизм зазора `Δλ ≈ 0.0044` — пустая ячейка, не бег потенциала
Разность `λ_exp − 1/8` — **не** «бег» `λ(μ)` QFT. Macro-наблюдатель, считывая массу Хиггса, видит упругое поле pra-вихря, которое поляризует **нененапряжённый сферический фон** пустых ячеек (`b=0`, A5: `ρ_field > 0`).
**Вакуумный квант** (масштаб бита пустой ячейки, §7.1 / §8.4.1-A):
$$
\delta\lambda
= (\alpha^{*}-1)\,\alpha_{\mathrm{fs}}
= \frac{1}{4\pi}\cdot\alpha_{\mathrm{fs}}
= \frac{1}{4\pi\,\alpha_{\mathrm{fs}}^{-1}}
\approx \frac{1}{4\pi\cdot 137.036}
\approx 5.807\times 10^{-4}.
$$
(`α*−1 = Δφ_min/(2π) = 1/(4π)` — остаток пустой ячейки; `α_fs` — уже выведенный EM-фактор §8.2.)
**Сборка по иерархии.** Те же `N_hier = 8` каналов α_fs-ослабления, что строят `v_peak`, суммируют квант при binomial M→T сглаживании:
$$
\Delta\lambda \approx N_{\mathrm{hier}}\,\delta\lambda
= 8\,\delta\lambda
\approx 4.646\times 10^{-3}.
$$
(Уточнение «оси FCC / веса binomial» — открытый вывод на хвост `~10^{-4}`; ведущий множитель — `N_hier`.)
**Исправленный quartic / масса:**
$$
\lambda = \frac18 + N_{\mathrm{hier}}\,\delta\lambda
\approx 0.12965,
\qquad
m_H = \sqrt{2\lambda}\,v \approx 125.31\,\mathrm{GeV}.
$$
| величина | `λ=1/8` (bare) | `+ N_hier·δλ` | PDG |
|----------|----------------|---------------|-----|
| `λ` | 0.125 | ≈ **0.12965** | ≈ 0.1294 (`m_H²/2v²`) |
| `m_H` | ≈ 123.04 GeV | ≈ **125.31 GeV** | ≈ **125.25 GeV** |
| rel(`m_H`) | ≈ 1.76% | ≈ **0.045%** | — |
**Ломает формулу:** отказ от T3a/T3b; `N_hier ≠ ⌊B_hV⌋−1`; отдельное Higgs-поле на M; soft-wall «канала занятости»; подмена `δλ` на fitted knob.
**Не претендует:** width / lineshape; полный вывод half-energy деформации из gate; точная FCC/binomial кратность сверх `N_hier` (хвост ~0.045%).
**Следствие:** **`m_e`** — `α_fs²·m_H/N_φ`; **`m_ν^(atm)`** — `α_fs⁵·2m_H/(N_hier N_φ)` (§8.2); **`m_p`** — `α_fs·v/2·(1+κ²/N₁₂)` (§8.2); **`m_H`** — bare `v/2`, с vacuum-bit stack → ≈ PDG. Higgs VEV не axiom M.
### 8.4 Четыре взаимодействия — симметрично, потому что всё квантуется
**Претензия SM/GR:** четыре силы — четыре разных языка (калибровки U(1)×SU(2)×SU(3), отдельная метрика).
**Претензия M:** один локальный **`g`** на одной решётке; всё — целые кванты (`s₀`, `E₀`, `n`, `b`, `N_ring`, `G`…). **Волна не исключение:** свет и звук — **`n_E·E₀`** / **фононы `n_k`**, не continuum (§0.9 · Thm 5.2). Тогда **нельзя** оставить силы «кусочками разных теорий». Они обязаны быть **четырьмя симметричными каналами одного дискретного носителя** — разными *проекциями / топоклассами / оболочками* одного океана, не четырьмя Lagrangians.
**Симметрия описания:**
```
один g , один носитель Λ , одна лестница квантов
 → 4 readout-канала на T
 → без отдельного «вида взаимодействия» как axiom M
```
| канал (T/SM имя) | носитель на M (рабочая гипотеза) | квант / заряд | статус |
|------------------|-----------------------------------|---------------|--------|
| **электромагнетизм** | фазы на рёбрах + $\Phi_\square$; макро Maxwell; $c^2=K_P/\mu_P$ → изотропия; излучение $n=0$; распад ≠ аннигиляция (§8.2) | `e₀`, $n$ | **ЭМ-блок выведено**; Bose/Planck ; $\Gamma,\tau$ ; LIV ; sim soft |
| **слабое** | хиральность **`P_L/P_R`**, SU(2) flip (§3.11); смена оболочки/`n` | $v$; $G_F$; $\sin^2\theta_W=3/13$; $\alpha(M_Z)$ из $B_{hV}$; $m_W,m_Z$; $N_{\mathrm{gen}}=d$; $\lambda=3/13$ | **EW+CKM-скелет выведено**; $A,\rho,\eta$ — открыто |
| **сильное** | составные узлы (§6); `f_геометрия`; Паули+`K_P` | $\alpha_s(v)=d/(N_{\mathrm{hier}}\pi)$; бегунок $1/(d\pi)\ln(v/\mu)$ | **seed+runner выведено**; census sim открыто |
| **гравитация** | конус/`κ`/упаковка Λ; инерция = Arg-зигзаг (§5.0.1); **$g_{\mu\nu}$ = T-strain**; **$G_{\mu\nu}=\mathcal{E}(\delta)=8\pi\ell_P^{2}T_{\mu\nu}$** (§8.4.2) | `E₀`,`p₀`,`F₀`; $G$ в $l_P$ | **паспорт+Эйнштейн выведено**; stencil/GW/Λ — открыто |
**Что запрещено после квантования всего:**
- вводить 4-ю / 5-ю калибровочную группу **снаружи** `g`;
- держать гравитацию «в другом файле», а SM — «в этом», без общего дискретного корня;
- объявлять `α_s`, `G_F` fitted knobs, если `α_fs` уже претендует на вывод из gate.
**Что ещё не закрыто:** census составных узлов (§8.4.1-D3), stencil $\theta_f$ / GW / $\Lambda$, IR-бегунок/конфайнмент ниже $M_Z$, $A,\rho,\eta$ CKM. Эйнштейн из strain · $N_{\mathrm{gen}}$ · $\lambda=3/13$ — §8.4.2 / §8.4.4. Угол Вайнберга · $\alpha(M_Z)$ · $m_W/m_Z$ — §8.4.3.
**Склейка:** §1.6 (один носитель) · §5.2 (одна лестница) · §5.0.4–5.0.5 (запрещёнка + `ρ_Θ`) · §3.10–3.11 (SU(2)/хиральность) · §8.2 (`α_fs`).
#### 8.4.1 От ansatz к выводу: показатель **8** и **√(2π)** из лестницы / фазы
**Цель:** не подогнать CODATA, а вывести
$$
v=\alpha_{\mathrm{fs}}^{8}\,E_P\,\sqrt{2\pi}
$$
из уже принятых кусков M (**§3.12.6**, **§4.1**, **§5.0**, **§8.2**).
##### A · Показатель **8** = бит-бюджет кирпича минус бит занятости
Из **§3.12.6**:
$$
B_{hV}=\frac{2\pi}{\ln 2}\approx 9.0647\,\mathrm{bit},\qquad
\lfloor B_{hV}\rfloor = 9 = \texttt{mod\_bits}=\texttt{phase\_bits}.
$$
**Физика −1 (lemma занятой ячейки).**
Пра-частица — один `v_p` (**§8.1**). На M у ячейки `hV` только два класса материи (**§5.0**):
$$
\rho_{\mathrm{matter}}\in\{0,\rho_P\},\qquad b(x)\in\{0,1\}.
$$
Частица занимает **планковский объём целиком** — не долю ячейки, не «облако внутри `r < l_P`» (**§5.0.4**: ниже `dl` пространства нет).
**Паули / A16:** два одинаковых спинора в одном `v_p` запрещены → **занятое место никто другой занять не может**.
Значит в бюджете `B_{hV}` ровно **один бит** — бит занятости `b` — **исключительно** зарезервирован под «здесь есть узел / здесь кипит вакуум». Он не свободен для иерархии масштабов: его уже «съела» топология занятости.
$$
N_{\mathrm{hier}}=\lfloor B_{hV}\rfloor-1_{\mathrm{occupancy}}=8.
$$
**Связь с $\alpha^{*}$, не подмена.** Вакуумный остаток
$$
\alpha^{*}-1=\frac{\Delta\phi_{\min}}{2\pi}=\frac{1}{4\pi}
$$
(§7.1 · §5.2.2-V) — это физика **пустой** ячейки (`b=0`, A5-кипение). Бит `b` — то, что **отделяет** пустую ячейку от занятой. Вычитаем не «кусок $\alpha^{*}$ из числа», а **канал занятости**, который нельзя одновременно отдать иерархии ослаблений.
**Рекурсия $\alpha_{\mathrm{fs}}$ (тот же принцип исключённости).**
Каждый из оставшихся **целых** битов бюджета — независимый канал фазового сопряжения с вакуумом. Единственный безразмерный фактор такого сопряжения уже выведен (**§8.2**):
$$
\alpha_{\mathrm{fs}}=\frac{1}{4\pi^{3}+\pi^{2}+\pi}.
$$
Два канала не делят одну ячейку (занятость исключает дробление) → шаг иерархии **дискретен** и равен ровно одному $\alpha_{\mathrm{fs}}$, не $\alpha_{\mathrm{fs}}^{1/2}$ и не произвольному $\lambda$.
Отсюда **пиковая** (M-плотностная) шкала:
$$
v_{\mathrm{peak}}=\alpha_{\mathrm{fs}}^{N_{\mathrm{hier}}}\,E_P=\alpha_{\mathrm{fs}}^{8}\,E_P\approx 98.17\,\mathrm{GeV}.
$$
**Проверка без CODATA $v$:**
$\ln(E_P/v_{\mathrm{peak}})/\ln(\alpha_{\mathrm{fs}}^{-1})=8$ по построению, если $v_{\mathrm{peak}}=\alpha_{\mathrm{fs}}^{8}E_P$.
##### B · Множитель **√(2π)** = Gaussian-мера macro-readout (§4.1)
Переход M→T — не $hL\to 0$, а **binomial (1-2-1)** coarse-grain (**§4.1**). В континуальном пределе ядро (1-2-1) → **гауссова** плотность. Для стандартной нормали
$$
\rho_{\max}=\frac{1}{\sqrt{2\pi}},\qquad
\int\rho=1 \implies \frac{1}{\rho_{\max}}=\sqrt{2\pi}.
$$
**Пик** (плотность / локальный M-масштаб $v_{\mathrm{peak}}$) и **интегральный** порядковый параметр на T (vev $v$) связаны той же мерой:
$$
v=v_{\mathrm{peak}}\cdot\sqrt{2\pi}=\alpha_{\mathrm{fs}}^{8}\,E_P\,\sqrt{2\pi}.
$$
Это не «число из PDG», а **перевод плотность ↔ интеграл** при Gaussian T-readout.
Отсюда же $G_F=1/(\sqrt{2}\,v^{2})$ — стандартная SM-связка поверх уже полученного $v$.
##### C · Численный контроль (после вывода, не до)
| величина | из §8.4.1 | внешняя таблица |
|----------|-----------|-----------------|
| $N_{\mathrm{hier}}=\lfloor B_{hV}\rfloor-1$ | **8** | — |
| $v$ | ≈ **246.08 GeV** | ≈ **246.22 GeV** |
| $G_F$ | ≈ **1.1677×10⁻⁵ GeV⁻²** | ≈ **1.1664×10⁻⁵** |
| $\alpha_s(v)=d/(N_{\mathrm{hier}}\pi)$ | ≈ **0.11937** | — (seed @ $v$) |
| $\alpha_s(M_Z)$ бегунок | ≈ **0.11788** | ≈ **0.1179** |
##### D · $\alpha_s$ seed: модель договаривает $d$ и $\pi$
**Что забыли про тройку.** Цифру $3$ в $3/(8\pi)$ подписали как $N_c$. Но (§4.0.1 · §6) кварк — не предел; SU(3) сверху не вводили. Сверху уже есть **$d=3$** — пространство $(3+1)$ (**§1.6**), то же $d$, что делает $4\pi$ телесным углом в EM (**§1.4.3**).
**Что модель уже сказала про $\pi$.** Лестница EM (**§8.2**):
$$
\alpha_{\mathrm{fs}}^{-1}=4\pi^{3}+\pi^{2}+\pi.
$$
Это **одна башня** фазового сопряжения: сверху полный телесный угол $4\pi$, внизу — **атом плоского угла $\pi$**. Новый $\pi$ для сильного канала не нужен: сильное — короткий packing/confining канал — берёт **нижнюю ступень той же башни**.
$$
d=3\ \text{(§1.6)},\qquad
N_{\mathrm{hier}}=8\ \text{(§8.4.1-A)},\qquad
\alpha_s(v)=\frac{d}{N_{\mathrm{hier}}\,\pi}=\frac{3}{8\pi}.
$$
**Шкала семени = $v$.** Тот же $N_{\mathrm{hier}}$, что дал электрослабый vev (**§8.4.1-A·B**). Seed — не «на планковом потолке», а на полу иерархии ослаблений $\alpha_{\mathrm{fs}}$.
##### D2 · Бегунок $v\to M_Z$ (без импорта QCD $b_0$)
Один лог-шаг T-readout от weak floor $v$ к полюсу $Z$:
$$
\frac{1}{\alpha_s(\mu)}
=
\frac{1}{\alpha_s(v)}
+
\frac{1}{d\pi}\,\ln\frac{v}{\mu}.
$$
Коэффициент $1/(d\pi)$ — тот же packing-квант, что в семени ($d$ и терминал $\pi$), **не** $(11-2n_f/3)/(2\pi)$ из учебника QCD.
При $\mu=M_Z\approx 91.19\,\mathrm{GeV}$ и $v$ из §8.4.1: $\alpha_s(M_Z)\approx 0.11788$ (PDG ≈ $0.1179$, rel ~$10^{-4}$).
**Смысл:** сильный канал почти плоский между $v$ и $M_Z$ на этой лестнице — потому seed «случайно» был близок к таблице; бегунок объясняет зазор без подгона $b_0$.
FCC = тетраэдры; слой $\{111\}$ = гекс (**§1.6.4**). Минимальная equal-NN плакетка — **треугольник**, girth $3=d$. Confining на короткой шкале замыкается на ней → минимальный барион-подобный узел несёт **$d$ фокусов**. Алгебра графа: `fcc_nn_plaquette_row()` — girth == $d$.
##### E · Что ещё мягко (не прятать)
1. **Явный stencil** $\theta_f$ / sim дефицита — §8.4.2-E.
2. **Полный census** составных узлов (§8.4.1-D3).
3. **Бегунок ниже $M_Z$ / IR** (адронная шкала) — отдельный T-readout; здесь закрыт только $v\to M_Z$.
4. **$A,\rho,\eta$ CKM** и точный PDG beyond $\lambda$ — §8.4.4-D.
5. **Бегунок $\sin^2\theta_W$** от голого $3/13$ к PDG@$M_Z$ — soft (§8.4.3).
##### F · Статус
| кусок | статус |
|-------|--------|
| $8=\lfloor B_{hV}\rfloor-1_{\mathrm{occupancy}}$ | выведено |
| шаг $=\alpha_{\mathrm{fs}}$ · $\sqrt{2\pi}$ · $v$ | выведено |
| $d$ и $\pi$ в $\alpha_s(v)$ | выведено |
| бегунок $1/(d\pi)\,\ln(v/\mu)$ → $M_Z$ | выведено **закрыт** · контроль PDG |
| girth FCC plaquette $=d$ | выведено алгебра · sim census |
| GR: $G$ в $l_P$ · $F=mg$ из $s_0$ · $g_{\mu\nu}=$T-strain · $G_{\mu\nu}=8\pi\ell_P^{2}T_{\mu\nu}$ | выведено §8.4.2 |
| $\sin^2\theta_W=3/13$ · $\alpha(M_Z)$ · $m_W,m_Z$ · $N_{\mathrm{gen}}=d$ · $\lambda=3/13$ | выведено §8.4.3–§8.4.4 |
| stencil $\theta_f$ · GW · $\Lambda$ · IR · $A,\rho,\eta$ · бегунок $\theta_W$ | открыто |
**Фальсификация:** usable hierarchy ≠ $\lfloor B_{hV}\rfloor-1$; T-vev без $\sqrt{2\pi}$; terminal EM ≠ $\pi$; бегунок с коэффициентом ≠ $1/(d\pi)$ лучше бьёт PDG без новых knobs; устойчивый узел с кратностью ≠ $d$ на FCC — правим сильный канал, не таблицу; $G\neq\ell_P^{\,2}$ (MODEL) / SI-тождество или $F_0\neq m_{\mathrm{arg}}g_M$ без новой шкалы — ломает GR-паспорт.
#### 8.4.2 · GR-паспорт: $G$ в $\ell_P$, Ньютон из $s_0$, метрика = T-strain
**Жёсткое:** пятого поля нет. Гравитация — **геометрический readout** той же T-среды, что даёт силы §8.4.1, а не отдельный «гравитонный» слот в таблице.
##### A · $G$ в планковских единицах (без новой шкалы)
В SI планковская длина и постоянная Ньютона связаны определением:
$$
\ell_P = \sqrt{\frac{\hbar G}{c^3}}
\quad\Rightarrow\quad
G = \frac{\ell_P^{\,2}\,c^3}{\hbar}.
$$
На решётке с $c=1$, $\hbar=1$ (MODEL-единицы): **$G=\ell_P^{\,2}$**. То есть $G$ **не** подгоняется к астроданным как пятый coupling — она **тождество** с ячейкой, уже зафиксированной планковским каблуком башни. Контроль: `SI.G` ↔ `SI.l_P**2 * SI.c**3 / SI.hbar` в SI; в безразмерных единицах — $G/\ell_P^{\,2}=1$.
##### B · Ньютонова сила из аренового $s_0$
Арена уже несёт поверхностное натяжение / масштаб $s_0$ (force-ansatz / `force_ansatz_row`). Ньютонов предел на тестовую массу $m_{\mathrm{arg}}$ при «земном» ускорении $g_M$ среды:
$$
F_0 = m_{\mathrm{arg}}\,g_M,
$$
где $g_M$ читается из градиента потенциала / strain на масштабе арены (не из отдельного $G$-knob). Связь с $G$:
$$
g_M \sim \frac{G M_{\mathrm{src}}}{R^2}
$$
в слабом поле — **следствие** определения $G=\ell_P^{\,2}$ плюс геометрии источника, а не новый параметр. Фальсификация: если для совпадения с $mg$ нужна шкала ≠ $\ell_P$ / ≠ $s_0$ арены — паспорт ломается.
##### C · $g_{\mu\nu}$ = strain readout T
Метрика не вводится отдельным полем. Локальный **strain** тензора / градиентов T (дефекты, сжатие ячеек, анизотропия FCC-окрестности) **есть** $g_{\mu\nu}-\eta_{\mu\nu}$ в линейном приближении:
$$
g_{\mu\nu} = \eta_{\mu\nu} + h_{\mu\nu}[T],
\qquad
h_{\mu\nu} = \mathrm{strain}_{\mu\nu}(T).
$$
Слабое поле → Ньютон §B; сильное → нужна полная нелинейная карта strain→метрика (soft).
##### C′ · Точный вид $\mathrm{strain}[T(z)]$ на рёбрах FCC
**Жёсткое разделение каналов:** $\Delta\varphi_e$ — дискретная скорость Madelung; **длины** рёбер Regge — из отклонения плотности. Оба входят в один readout $h_{\mu\nu}$.
**1 · Данные ребра** $e=(x\to y)$, $|y-x|=\ell_P$, $y\in N_{12}(x)$:
$$
\Delta\varphi_e
:=
\mathrm{Arg}\bigl(z(x)^\dagger z(y)\bigr)
\in(-\pi,\pi],
\qquad
\rho_e
:=
\tfrac12\bigl(|z(x)|^2+|z(y)|^2\bigr).
$$
(U(1)-срез / относительная фаза спиноров; SU(2)-голономия ребра — soft, не в линейном $h_{ij}$.)
Вакуум: $\rho_{\mathrm{vac}}$ (A5-фон, natural $|z|^2\sim 1$), $\langle\Delta\varphi_e\rangle_{\mathrm{vac}}=0$.
**2 · Растяжение ребра (геометрия Regge):**
**$\ell_P$ не деформируется.** Комбинаторный шаг / атом носителя $dl=\ell_P=\mathrm{const}$ (§1 · §5.1): ячейки не разъезжаются в эмбеддинге, $v_p$ несжимаем. Меняется не планковская длина, а **эффективная длина ребра для Regge/T-readout**:
$$
\varepsilon_e
:=
\frac{\rho_e-\rho_{\mathrm{vac}}}{\rho_{\mathrm{vac}}},
\qquad
\ell_e
:=
\ell_P\,(1+\varepsilon_e)
\quad\text{(линейно; сильное — soft)}.
$$
То же ребро графа по-прежнему соединяет соседей на расстоянии одного тика; $\ell_e\neq\ell_P$ — вес, который macro-геометрия приписывает связи из-за $\delta\rho$, не «резиновый $\ell_P$».
Плоский вакуум: $\varepsilon_e=0\Rightarrow\ell_e=\ell_P\Rightarrow\delta_e=0$.
**3 · Углы грани и дефицит** (из длин, не из $\Delta\varphi$ напрямую):
На треугольной plaquette $f=\triangle(e_1e_2e_3)$ — закон косинусов:
$$
\theta_f^{(v)}
=
\arccos\!
\left(
\frac{\ell_a^2+\ell_b^2-\ell_c^2}{2\ell_a\ell_b}
\right),
\qquad
\delta_e=2\pi-\sum_{f\ni e}\theta_f.
$$
**4 · Линейный метрический readout** в ортонормированном T-базисе ($\hat{e}$ — единичный вектор ребра):
$$
\begin{aligned}
h_{ij}(x)
&=
\frac{2}{|N|}
\sum_{e\ni x}\varepsilon_e\,(\hat{e})_i(\hat{e})_j,
\qquad |N|=12,\\
h_{0i}(x)
&=
\frac{2}{|N|}
\sum_{e\ni x}
\Delta\varphi_e\,(\hat{e})_i,
\qquad
h_{00}(x)
=
-\frac{2}{|N|}\sum_{e\ni x}\varepsilon_e.
\end{aligned}
$$
Тогда $g_{\mu\nu}=\eta_{\mu\nu}+h_{\mu\nu}$, $\mathrm{strain}_{\mu\nu}:=h_{\mu\nu}$.
**Нормировка без knob:** коэффициент «2» + Ньютонов мост §B фиксируют $h_{00}\approx-2\Phi$ при $\nabla^2\Phi=4\pi G\delta\rho$; если после живого stencil не совпадает — ломается паспорт $G=\ell_P^{\,2}$, не крутим α.
**Следствие для Diff-hinge §G:** продольные куски $h_{ij}$ не свободны — они = функции $\{\varepsilon_e[z]\}$; $h_{0i}$ = функции $\{\Delta\varphi_e\}$. Отдельного «поля $h$» нет.
**Антипаттерн:** объявить $\varepsilon_e\propto\Delta\varphi_e$ (путает длину и скорость); fitted α рядом с живым Ньютоном.
##### C′′ · Физсмысл компонент + замыкание Regge (readout, не калибровка)
**Принято как чтение §C′** (линейный режим).
**1 · $h_{00}$ и $h_{ij}$ — упругая деформация плотности**
Пра-вихрь / дефект локально меняет норму спиноров → $\varepsilon_e=(\rho_e-\rho_{\mathrm{vac}})/\rho_{\mathrm{vac}}$ → эффективная длина $\ell_e=\ell_P(1+\varepsilon_e)$ (уплотнение / разрежение упаковки FCC).
$h_{ij}$ собирается как $\sum\varepsilon_e\,\hat{e}_i\hat{e}_j$ — **дискретный тензор деформации** (strain) среды. Отличие от тензора напряжений Коши: stress $=K_P\cdot\varepsilon$ (ответ среды, A7/gate); в метрический readout входит именно $\varepsilon$.
$h_{00}\leftarrow-\varepsilon_e$ — ньютоновский канал: выше плотность дефектов → для macro-наблюдателя замедление временного шага считывания ($h_{00}\approx-2\Phi$). На M та же $\varepsilon$ входит в leapfrog-kick: $\Phi[\varepsilon]$ · $Z^{+}=2Z+\lfloor\mathcal{N}[\varepsilon]\rfloor-Z^{-}$ (§3.12.5a).
**2 · $h_{0i}$ — гравидинамика / увлечение**
$\Delta\varphi_e=\mathrm{Arg}(z(x)^\dagger z(y))$ — Madelung-скорость квантовой жидкости вдоль ребра. $h_{0i}$ = буквальный дрейф пра-спиноров по связям → frame-dragging / гравимагнетизм в T-readout.
**3 · Замыкание на Regge (пассивное следствие)**
$$
\rho_e\to\ell_e\xrightarrow{\text{косинусы}}\theta_f\to\delta_e=2\pi-\sum_{f\ni e}\theta_f.
$$
Кривизна не калибровочное поле: $z$ эволюционирует жёстким локальным $g$ (leapfrog, §3.12); $g_{\mu\nu}=\eta+h$ **проявляется** для macro-наблюдателя через живой stencil. Эйнштейн §D — сборка $\delta_e$ в $G_{\mu\nu}$, не отдельная динамика.
##### C′′′ · BC домена насыщения $\rho=\rho_\star$ и профиль $h$ вокруг ядра
**Домен**
$$
D_\star
:=
\{x\in\Lambda:\rho(x)=\rho_\star\},
\qquad
R_\star
:=
\mathrm{rad}(D_\star)\ge\ell_P,
\qquad
\partial D_\star
=
\{e=(c\to y):c\in D_\star,\ y\notin D_\star\}.
$$
Pra-ядро: часто $|D_\star|=1$ (один $v_p$, $b=1$, $m_{\mathrm{cell}}=m_P$).
**1 · Внутри $D_\star$ (Dirichlet)**
$$
\rho=\rho_\star,
\quad
\varepsilon=\varepsilon_\star,
\quad
\Phi[\varepsilon]\ \text{насыщен (§3.12.5a)},
\quad
b=1,
\quad
m_{\mathrm{cell}}=m_P.
$$
**2 · На $\partial D_\star$ (интерфейс)**
Для каждого ребра $e=(c\to y)\in\partial D_\star$:
$$
\rho_e=\frac{\rho_\star+\rho(y)}{2},
\qquad
\varepsilon_e^{(\partial)}
=
\frac{\rho_e-\rho_{\mathrm{vac}}}{\rho_{\mathrm{vac}}},
\qquad
\ell_e=\ell_P\bigl(1+\varepsilon_e^{(\partial)}\bigr),
$$
$$
\Delta\varphi_e=\mathrm{Arg}\bigl(z(c)^\dagger z(y)\bigr).
$$
Поток occupancy: $\rho(c)$ не растёт выше $\rho_\star$ (A7 clamp). Спинор на границе — обычный $g$ (A1–A2).
**3 · Снаружи (асимптотика)**
$$
\mathrm{dist}(x,D_\star)\to\infty
\quad\Rightarrow\quad
\rho\to\rho_{\mathrm{vac}},\ \
\varepsilon\to 0,\ \
h_{\mu\nu}\to 0.
$$
**4 · Профиль $h$ — и где красота врёт**
Near (интерфейс, линейный strain §C′):
$$
h_\star^{(\mathrm{near})}
:=
-\frac{2}{|N|}\sum_{e\in\partial D_\star}\varepsilon_e^{(\partial)}.
$$
Far (Ньютон §B, $G=\ell_P^{\,2}$, $m\le m_P$):
$$
h_{00}^{(\mathrm{far})}(R)
=
-\frac{2m}{m_P}\,\frac{\ell_P}{R}
\qquad(R>R_\star).
$$
**Не сшивка.** На pra-ядре ($|D_\star|=1$, $12$ NN к A5-floor $\rho_{\mathrm{vac}}=(\mathrm{vac\_amp})^2$) live:
$$
h_\star^{(\mathrm{near})}\approx -4095,
\qquad
h_\star^{(\mathrm{Newton})}:=-\frac{2m}{m_P}\frac{\ell_P}{R_\star}=-2
\quad(m=m_P,\ R_\star=\ell_P),
$$
$$
\frac{|h_\star^{(\mathrm{near})}|}{|h_\star^{(\mathrm{Newton})}|}\approx 2\times 10^{3}.
$$
Если же $\rho_{\mathrm{vac}}\sim 1$ ("залитая" жидкость §5) и $\rho_\star=1$ — то $\varepsilon_e^{(\partial)}=0$ и $h_\star^{(\mathrm{near})}=0$: плотности нет, а Ньютон с $m=m_P$ всё равно хочет $|h|=2$. Оба полюса убивают "красивую" склейку.
**Что это значит**
1. Линейный $\varepsilon\to h$ **не** продолжает быть weak-field на $\partial D_\star$ при A5-floor.
2. Два разных $\rho_{\mathrm{vac}}$ (floor vs filled) — не один объект; пока не зафиксирован **один** vacuum для strain, профиль $h(R)$ из $\varepsilon$ не претендует на $1/R$.
3. Far $1/R$ — **анзац Ньютона**, не вывод из суммы $\varepsilon_e$ по оболочкам. Regge-дефицит на ядре → IR $1/R$ ещё не доказан stencil'ом.
**Рабочая постановка sim (без притворства)**
$$
\text{мерить }h_{00}^{(\mathrm{strain})}(R)\text{ из §C′ на живом }z;
\quad
\text{сравнивать с }-\frac{2m}{m_P}\frac{\ell_P}{R};
\quad
\text{не подгонять }\rho_{\mathrm{vac}}.
$$
Фальсификация: если после фиксации единого $\rho_{\mathrm{vac}}$ и нелинейного strain far-хвост $\neq 1/R$ при $m\le m_P$ — ломается паспорт §B/§C, не крутим α.
##### D · Тензор Эйнштейна из strain (не пятое поле)
Кривизна **не** отдельный слот. На FCC (§1.6 · §8.4.1-D3) элементарная plaquette — треугольник (girth $=d$). Кривизна сидит на **рёбрах-шарнирах** (Regge): сумма углов вокруг ребра $e$ даёт дефицит
$$
\delta_e = 2\pi - \sum_{f\ni e}\theta_f\bigl(\mathrm{strain}[T]\bigr).
$$
Плоский вакуум: $\mathrm{strain}=0$ $\Rightarrow$ $\delta_e=0$. Ненулевой strain на T → ненулевые $\theta_f$ → $\delta_e\neq 0$. Дискретный Эйнштейн — **сборка дефицитов** в тензор на двойственной площади ребра:
$$
G_{\mu\nu}[T]
:=
\mathcal{E}_{\mu\nu}\bigl(\delta(\mathrm{strain}[T])\bigr),
$$
где $\mathcal{E}$ — линейный (слабое поле) / нелинейный (сильное) оператор Regge→континуум. Уравнение поля с уже закрытым $G=\ell_P^{\,2}$ (§A):
$$
G_{\mu\nu}[T] = 8\pi\,\ell_P^{\,2}\,T_{\mu\nu}[T].
$$
**Откуда $8\pi$, не knob.** Ньютонов предел §B уже фиксирует $G$ и $F_0=m_{\mathrm{arg}}g_M$. Континуальный мост $\nabla^2\Phi=4\pi G\rho$ плюс $g_{00}\approx -(1+2\Phi)$ **вынуждает** коэффициент $8\pi G$ в $G_{\mu\nu}=8\pi G\,T_{\mu\nu}$. Новый параметр не появляется: либо паспорт Ньютона ломается, либо $8\pi$ обязателен.
Источник $T_{\mu\nu}$: stress Arg-массы / occupancy (§5.0.1 · §5.0) на той же решётке — не внешний fluid.
**Жёсткое:** определение $G_{\mu\nu}$ через deficit strain + полевое уравнение с $8\pi\ell_P^{\,2}$.
**Мягкое (sim):** явный stencil углов $\theta_f$, GW как бегущий strain, космологический член / IR-кривизна далёкого вакуума.
##### E · Что ещё мягко (GR)
1. **Явный stencil** $\theta_f$ на FCC-тетраэдрах / sim stress-test дефицита.
2. **Космологический член / IR-кривизна** далёкого вакуума — не смешивать с Landau IR сильного бегунка.
3. **Гравитационные волны** как распространяющийся strain — после живого stencil.
4. **Диффеоморфизмы / лишние моды** — §G (открыто).
##### G · Диффеоморфизмы Diff и запрет лишних мод (открыто · hard hinge)
**Возражение:** в ОТО ковариантность относительно Diff убивает нефизические продольные/скалярные моды $h_{\mu\nu}$, оставляя **две** поперечные поляризации GW. На **фиксированной** комбинаторной решётке полной Diff нет: метки ячеек дискретны. Паспорт «$G_{\mu\nu}=\mathcal{E}(\delta[\mathrm{strain}])$» и лозунг «GW = бегущий strain» **сами по себе** не гарантируют, что continuum-предел не выродится в теорию с лишними DOF (класс Brans–Dicke / скаляр-тензор), уже убитую PPN / Солнечной системой.
**Что M уже даёт (не замена Diff):**
| кусок | статус |
|-------|--------|
| нет отдельного гравитонного слота — $g,G$ = readout той же T-среды | выведено паспорт |
| $G=\ell_P^{\,2}$, Ньютон $\Rightarrow 8\pi$ | выведено |
| Einstein из Regge-дефицита strain | выведено определение · stencil soft |
| изотропия / Lorentz на T (A12, binomial+$\kappa$) | выведено micro→macro · **не** Diff |
| явная Diff-калибровка / счёт мод | открытый вывод |
**Честный статус:** это **не закрытый** пункт. Либо (а) в IR из Regge+$\mathcal{E}$+связей Bianchi/continuity всплывает эффективная Diff и ровно 2 моды, либо (б) паспорт GR ломается на PPN/GW — фальсификация, не «подкрутим $\omega$».
**Ответ модели (лемма · framing, не «Diff нашли на FCC»):**
Возражение бьёт в теорию, где $h_{\mu\nu}$ — **независимое** динамическое поле на фоне решётки. У нас такого поля нет (§8.4.2 жёсткое): первичное — $z$ на фиксированной $\Lambda$; метрика — **readout**
$$
g_{\mu\nu}=\eta_{\mu\nu}+\mathrm{strain}_{\mu\nu}[T(z)].
$$
В ОТО Diff — калибровочная избыточность **свободной** $g_{\mu\nu}$. Здесь $g$ не свободна → отдельной «калибровки Diff на ячейках» **не требуется как axiom**. Продольные/скалярные куски $h$, которые Diff убивает в GR, у нас обязаны быть либо
1. **не независимыми DOF** (функции того же $z$), либо
2. **не распространяющимися** в IR (constraints жидкости: A3 continuity / gate / $\nabla\!\cdot\!G=0$ из $\mathcal{E}$), либо
3. **массивными / зазором $K_P$** — и тогда не портят PPN как безмассовый Brans–Dicke.
Эффективная Diff на T — описание той же избыточности **после** того, как readout $g$ начинают трактовать как первичное поле; на M её роль уже сыграли законы носителя.
**Не ответ:** «решётка упругая ⇒ Diff»; Pachner / динамическая триангуляция (у нас комбинаторика FCC **фиксирована**); fitted $\omega_{\mathrm{BD}}$.
**Открытый вывод (§G):** счёт IR-мод strain $=2$, PPN $\gamma\to 1$. Лемма меняет *где* искать — не «вписать Diff в метки ячеек», а «показать спектр readout».
1. **Счёт мод:** линеаризация strain вокруг плоского вакуума ($\delta_e=0$) на FCC → число распространяющихся безмассовых DOF $=2$ (плюс чистая калибровка/constraints), не $2+1$ скаляр и не продольные.
2. **PPN:** $\gamma\to 1$, $|\beta-1|$ в пределах Солнечной системы — без нового knob $\omega$ (анти-Brans–Dicke).
3. **Континуум:** показать, что $\mathcal{E}(\delta)$ + Ньютонов мост дают $\nabla_\mu G^{\mu\nu}=0$ (дискретная Bianchi) → constraints на $h_{\mu\nu}$ в IR, а не только алгебраический Einstein.
4. **Не считать Diff «уже есть»** из A12 или из «решётка упругая».
**Антипаттерн:** объявить «дискретная геометрия = калибровочная свобода»; подогнать $\omega$ под Cassini.
##### F · Статус GR
| кусок | статус |
|-------|--------|
| $G=\ell_P^{\,2}$ (MODEL) / SI-тождество | выведено |
| $F_0=m_{\mathrm{arg}}g_M$ из арены $s_0$ | выведено паспорт |
| $g_{\mu\nu}=\eta+\mathrm{strain}[T]$ | выведено определение |
| $\mathrm{strain}$: $\varepsilon_e[\delta\rho]$, $h_{0i}[\Delta\varphi_e]$, $\theta_f$ из $\ell_e$ | выведено §C′ линейно · сильное soft |
| $G_{\mu\nu}=\mathcal{E}(\delta[\mathrm{strain}])=8\pi\ell_P^{\,2}T_{\mu\nu}$ | выведено определение+Ньютон |
| stencil $\theta_f$ · GW · $\Lambda$ | открытый вывод |
| Diff / 2 поляризации / PPN anti-BD | lemma framing выведено · §G открыт |
**Фальсификация:** отдельное гравитационное поле в таблице сил; $G$ с размерностью/шкалой вне $\ell_P$; метрика, не сводимая к T-strain на той же решётке; полевое уравнение с коэффициентом $\neq 8\pi\ell_P^{\,2}$ при живом Ньютоне §B.
#### 8.4.3 · Угол Вайнберга: фотон / $Z$ и $\sin^2\theta_W=3/13$
#### 8.4.3 · Угол Вайнберга: фотон / $Z$ и $\sin^2\theta_W=3/13$
**Жёсткое:** не fitted knob. Голый угол задаёт геометрия носителя (§1.6) + нейтральный сектор (§3.11).
##### A · Два нейтральных направления → фотон и $Z$
На спиноре уже есть (§3.11):
1. **Глобальная фаза** $z\to z\,e^{i\theta}$ — вакуумный U(1); из него растут электромагнетизм и $\alpha_{\mathrm{fs}}$ (§8.2).
2. **Хиральная ось** $P_L/P_R$ и SU(2)-смешивание в COLLISION — слабый канал.
После пены T / ненулевого Arg остаётся **одна безмассовая** комбинация — **фотон** (дальнобойная фаза). Ортогональная ей — **массивная** — **$Z$**. Угол Вайнберга — вес, с которым хиральная ось подмешивается в фотон.
##### B · Голый угол: кластер FCC = $12+1$
Носитель — FCC, $|N_{12}|=12$ (§1.6.1). Один тик причинной оболочки — **кубооктаэдр** (§1.6.2). Минимальный локальный кластер упаковки:
$$
N_{\mathrm{cluster}} = |N_{12}| + 1_{\mathrm{center}} = 12 + 1 = 13.
$$
Из этих $13$ узлов ровно $d=3$ отвечают за пространственные степени свободы (направление хирального смешивания / упаковка в $(3{+}1)$). Остальные заперты во внутренние (фазовые) степени свободы. Доля пространственных слотов:
$$
\sin^2\theta_W
=
\frac{d}{N_{\mathrm{cluster}}}
=
\frac{d}{|N_{12}|+1}
=
\frac{3}{13}
\approx 0{,}23077.
$$
**Совпадение, не второй knob.** Число фазовых бинов кирпича (§3.12.6)
$$
N_\varphi = \left\lceil\frac{2\pi}{\Delta\varphi_{\min}}\right\rceil = \lceil 4\pi\rceil = 13
$$
**равно** размеру FCC-кластера. Это одна и та же «13» с двух сторон (Heisenberg-фаза и упаковка), а не подгонка $\lceil 4\pi\rceil$ в одиночку.
##### C · Эффективное vs голое
Число $1/N_{\mathrm{hier}}+1/(d\pi)=1/8+1/(3\pi)\approx 0{,}23110$ ближе к PDG@$M_Z$ ($\approx 0{,}23122$), но это **сумма уже закрытых весов бегунка**, не фундамент кирпича. Голое значение — $3/13$ на планковской геометрии; люфт $\sim 0{,}2\%$ до PDG — soft readout / бегунок на слабом масштабе (как $\alpha_s(v)\to\alpha_s(M_Z)$), не новый параметр в таблице сил.
**Запрещено:** ставить $1/8+1/(3\pi)$ вместо $3/13$; объявлять $\theta_W$ fitted knob при живом FCC-носителе.
##### D · Бегунок $\alpha_{\mathrm{fs}}\to\alpha(M_Z)$ (не импорт $1/128$)
Холодная $\alpha_{\mathrm{fs}}$ (§8.2) — полная фазовая башня. На полюсе $Z$ вакуум «съедает» ровно **ёмкость Бекенштейна одного кирпича** $B_{hV}$ (§3.12.6) — тот же объект, из которого вылез $N_{\mathrm{hier}}$:
$$
B_{hV}=\frac{2\pi}{\ln 2},\qquad
\frac{1}{\alpha(M_Z)}
=
\frac{1}{\alpha_{\mathrm{fs}}}-B_{hV}
=
(4\pi^{3}+\pi^{2}+\pi)-\frac{2\pi}{\ln 2}
\approx 127{,}972.
$$
**Не** тот же лог, что у $\alpha_s$: сильный канал между $v$ и $M_Z$ — packing $1/(d\pi)\ln(v/\mu)$; электромагнитный скачок IR$\to M_Z$ — **вычитание бит-бюджета кирпича**, не локальный packing. Контроль PDG $\alpha^{-1}(M_Z)\approx 127{,}955$ (rel $\sim 10^{-4}$).
**Дискретный сосед (не ):** $\alpha_{\mathrm{fs}}^{-1}-\lfloor B_{hV}\rfloor=\alpha_{\mathrm{fs}}^{-1}-9\approx 128{,}036$ — тот же смысл, грубее.
##### E · Массы $W$ и $Z$ (дерево, без новых knobs)
$$
e^{2}=4\pi\,\alpha(M_Z),\qquad
g=\frac{e}{\sin\theta_W},\qquad
m_W=\frac{gv}{2},\qquad
m_Z=\frac{m_W}{\cos\theta_W},
$$
с $\sin^{2}\theta_W=3/13$ и $v$ из §8.4.1. Отношение — чистый контроль угла:
$$
1-\Bigl(\frac{m_W}{m_Z}\Bigr)^{2}=\sin^{2}\theta_W=\frac{3}{13}.
$$
С $\alpha(M_Z)$ из §D: $m_W\approx 80{,}26\,\mathrm{GeV}$, $m_Z\approx 91{,}51\,\mathrm{GeV}$ (PDG $\approx 80{,}38$ / $91{,}19$; rel $\sim 0{,}1$–$0{,}4\%$). С холодной $\alpha_{\mathrm{fs}}$ массы занижены на $\sim 3\%$ — это и был след пропущенного бегунка $\alpha$, не дыра в $v$ или $3/13$.
##### F · Статус
| кусок | статус |
|-------|--------|
| фотон / $Z$ из U(1)$_{\mathrm{vac}}$ ⊕ хиральность | выведено определение §A |
| $\sin^2\theta_W=3/13=d/(|N_{12}|+1)$ | выведено голый |
| $N_\varphi=\lceil 4\pi\rceil=|N_{12}|+1$ | выведено совпадение |
| $1/\alpha(M_Z)=1/\alpha_{\mathrm{fs}}-B_{hV}$ | выведено бегунок |
| $m_W=gv/2$, $m_Z=m_W/\cos\theta_W$ | выведено дерево |
| бегунок $3/13\to$PDG@$M_Z$ · точнее $\alpha$-схема | открыто |
**Фальсификация:** носитель ≠ FCC/$|N|=12$; устойчивый кластер ≠ $13$; $\sin^2\theta_W$ с новой шкалой вне $d$ и $|N_{12}|$; $1/\alpha(M_Z)$ с вычитаемым ≠ $B_{hV}$ лучше бьёт PDG без новых knobs; $m_Z/m_W\neq 1/\cos\theta_W$ при живом $3/13$.
#### 8.4.4 · CKM: поколения и ведущий угол смешения
**Жёсткое:** не fitted матрица. Число поколений и ведущий угол — из той же геометрии носителя, что Weinberg; остальные параметры Wolfenstein — soft / census.
##### A · Откуда смешение
Слабый канал уже несёт (§8.4 · таблица сил): хиральность $P_L/P_R$ плюс **смена оболочки / $n$**. Заряженный ток стыкует два разных базиса на одном узле:
1. **Слабый** — партнёры SU(2)-flip (кто с кем связан $W$).
2. **Массовый / confining** — собственные состояния радиальных оболочек $\varepsilon$ и census-узла (§8.4.1-D3).
Базисы не обязаны совпадать → унитарная матрица смешения $V_{\mathrm{CKM}}\in U(N_{\mathrm{gen}})$.
##### B · Число поколений: $N_{\mathrm{gen}}=d=3$
Независимое «место» для стека оболочек — **пространственная** ось упаковки FCC в $(3{+}1)$ (§1.6). Их ровно $d=3$. Каждое поколение = один такой стек; четвёртый стабильный стек на FCC+Паули запрещён той же геометрией, что фиксирует $d$ в $\alpha_s$ и $\sin^2\theta_W$.
$$
N_{\mathrm{gen}} = d = 3.
$$
##### C · Ведущий угол: тот же кластерный вес $3/13$
Weinberg (§8.4.3) — доля **пространственных** слотов в FCC-кластере $12{+}1$:
$$
\sin^2\theta_W = \frac{d}{|N_{12}|+1} = \frac{3}{13}.
$$
Заряженный ток смешивает поколения **той же** долей: пространственный стек «выглядывает» в фазовый кластер размера $N_\varphi=|N_{12}|+1$. Голый угол Каббибо / Wolfenstein $\lambda$:
$$
\lambda = \sin\theta_{12} = \frac{d}{|N_{12}|+1} = \frac{3}{13}\approx 0{,}2308.
$$
**Тот же объект, другой канал:** нейтральный ток → $\theta_W$; заряженный → $\theta_{12}$. Не новый knob. Контроль PDG $|V_{us}|\approx 0{,}224$ (rel $\sim 3\%$ на голом угле — как у голого Weinberg до бегунка).
Минимальный каркас (остальное soft):
$$
V_{\mathrm{CKM}}
\approx
\begin{pmatrix}
1-\lambda^{2}/2 & \lambda & 0 \\
-\lambda & 1-\lambda^{2}/2 & 0 \\
0 & 0 & 1
\end{pmatrix}
+O(\lambda^{2}\text{ off-diag}),
\qquad \lambda=\frac{3}{13}.
$$
##### D · Мягкое
1. **$A,\rho,\eta$** Wolfenstein и точные $|V_{cb}|$, $|V_{ub}|$, CP-фаза — арена census оболочек / sim.
2. Степени $\lambda^{2},\lambda^{3}$ как грубый сосед для $|V_{cb}|$, $|V_{ub}|$ — не (ошибка больше, чем у $\lambda$).
3. Бегунок $\lambda$ и $\theta_W$ к шкале $M_Z$ — soft, как у $\alpha_s$.
##### E · Статус CKM
| кусок | статус |
|-------|--------|
| $V$ = смена базиса оболочка ↔ слабый flip | выведено определение |
| $N_{\mathrm{gen}}=d=3$ | выведено голый |
| $\lambda=\sin\theta_{12}=3/13$ | выведено голый |
| $A,\rho,\eta$ · точный PDG · бегунок | открыто |
**Фальсификация:** $N_{\mathrm{gen}}\neq d$ при живом FCC; $\lambda$ с новой шкалой вне $d$ и $|N_{12}|$; fitted CKM при закрытом $3/13$ в Weinberg.
**Остаётся в MODEL:** тор (§10.1), A3/A5/A6, антиматерия **`n→−n`** (§5.0.3), seed-топология ниже.
**Остаётся в MODEL:** тор (§10.1), A3/A5/A6, антиматерия **`n→−n`** (§5.0.3), seed-топология ниже.
