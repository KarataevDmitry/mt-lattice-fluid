# §3 · Функция перехода g

### 3.1 Каркас

Дискретность **`Λ`**, **`(hL, hT)`** — **Теорема 0.1** (§0). Локальность и каузальность:

```
Ψ^{t+1} = g(Ψ^t)
g = g_nonlin ∘ g_lin
g_lin : z ↦ L(z) — линейное смешивание по N(x)
g_nonlin: z ↦ z · exp(i·Φ(z)) — локальная фаза
```

```
N(x) = { x ± ê_i } — первая координационная оболочка (A1)
(Δ₄ z)(x) = Σ_{y∈N(x)} ( z(y) - z(x) )
```

Вторая координационная оболочка за один **`hT`** запрещена (**A1**); neighborhood Moore не используется.

На каркасе параметры не фиксируются:

| параметр | где закрывается |
|----------|-----------------|
| **`γ`** | §3.12, §5.2 |
| форма **`Φ`** | §3.12.5 |
| **`L`**: unitary vs diffusive | §3.12 |
| **`α*`** | §7.1 |
| **`ε`** | A7, §7.1 |

### 3.2 Ограничения от A1–A16

| условие | запрещает | остаётся |
|---------|-----------|----------|
| **A3** | рост/падение **`Σ\|z\|²`**, **\|z\|**-dissipation | bond-unitaries на **`N`**; **`Φ`** real |
| **A4** | нелинейность в модуле | только **`exp(iΦ)`** |
| **A5** | устойчивый **`z≡0`** | **`Φ` → large** при **\|z\|→0** |
| **A6** | «замёрзшие» фазы | mixing; product локальных unitary |
| **A7** | **`ρ_E > u_P`** | **`Φ`** с **`+ε`**, **`ε ↔ u_P`** |
| **A8** | сильная нелинейность при больших **\|z\|** | **`w(ρ)`** |
| **A9** | изолированный покой ячейки | CR-метрика; defect у vortex |

Линейное **`z+γΔ₄z`** не унитарно; допустим streaming — product локальных unitaries (**§3.12**).

### 3.4 Фазовый carrier ζ

**`Arg(Σz/z)`** и **`atan2−atan2`** дают ложный скачок **2π**.

**Утверждение:** **`ζ = (Σ_N z)·z*`**, **`Δφ = wrap(Arg ζ)`** — вход **§3.12.5**.

### 3.8 Слабые волны и vortex

**(3+1):** FCC **N₁₂** (§1.6). **(2+1):** гекс **N₆** (§1.4). Слабые возмущения и тяжёлый vortex — на том же stencil (§1.6, §1.4).

### 3.9 Дискретный анализ

#### 3.9.1 Голоморфная среда

Решётка **FCC N₁₂** / срез **гекс N₆**; поле **`z ∈ ℂ²`** (§3.10).

Линейный предел (идеальный вакуум):

```
Δ_disc z = 0 ⟺ z(x) = (1/|N|) Σ_{y∈N(x)} z(y) = ⟨z⟩_N
```

**g** удерживает голоморфность при отклонении **`Arg(⟨z⟩/z)`**; шаг **`R(Φ)`** с saturating **`Φ`** (**§3.12.5**).

| ДА | M |
|----|---|
| `Δ_disc z=0` | **`Δφ→0`**, holomorphic class |
| CR на рёбрах | **A9** |
| риманова поверхность | **`Λ×S¹`**, torus §10.1 |
| глобальная сшивка | phase sync на tick |

#### 3.9.2 Уравнение на решётке

**§3.12.5:** **`ζ`** → saturating **`Φ ∈ ℤ_{N_ring}`** → **`R(Φ)`** → leapfrog **`Z⁺+Z⁻=2Z+⌊𝒩⌋`**.

| член | ДА | M |
|------|----|---|
| **`Arg(⟨z⟩/z)`** | defect holomorphy | **`ζ` int** §3.12.5 |
| saturating kick | метрика у полюса | **`Φ_sat`**, **`K_P`** |
| unitary step | U(1) | **`R(Φ)`** |

При **`φ→0`**: **`z≈⟨z⟩`**; нелинейный **g** — конечная коррекция дефекта.

#### 3.9.3 Следствия

| объект | формулировка |
|--------|--------------|
| **Частица `v_p`** | полюс; **`∮ d arg = 2πn`**, **`n∈ℤ`** (A10) |
| **Заряд** | дискретный вычет |
| **Гейзенберг** | **`Δφ ≥ Δφ_min`**; нарушение → **`K_P`** (A16) |
| **Anti-smear** | размазанное не голоморфно → gate выталкивает полюс (A11) |
| **Нелокальность** | единая Λ; CR-единственность (A9) |

#### 3.9.4 Словарь M → T

| M | T (readout) |
|---|-------------|
| `z ∈ ℂ²` на `v_p` | `\|Φ\|²` binomial macro (§4.1) |
| идеальный вакуум | плоский фон |
| **`Δφ_N` в gate** | локальный **∇φ** |
| pra-vortex **`hV`** | электрон-кандидат §4.0.1 |
| winding **`n`** | заряд **`Q∝n`** |
| saturating **`Φ`** (§3.12.5) | readout §7.1 |

#### 3.9.6 Порог CR-энергии (A9)

На von Neumann **`N`** CR-метрика плоской волны после переходного режима выходит на стационарный уровень от дискретной дисперсии и **`ν_CA`** (§4.1.2).

| величина | формула | значение |
|----------|---------|----------|
| **`ν_CA`** | **`¼·c₀·l_P`**, **`c₀=l_P=1`** | **0.25** |
| **`{B_hV}`** | **`B_hV − ⌊B_hV⌋`**, **`B_hV=2π/ln2`** | **≈ 0.0647** |
| **`E_CR,seed`** | **`ν_CA·{B_hV}²`** | **≈ 0.00105** |
| **`E_CR,stab`** | **`ν_CA·(1+{B_hV})`** | **≈ 0.266** |
| **`|ΔE_CR|`** | **`≤ {B_hV}·E_CR`** | **≈ 6.5%** |

### 3.10 Спин SU(2)

#### 3.10.1 U(1)-only недостаточен

| | U(1) scalar **`z∈ℂ`** | фермионы (T) |
|--|----------------------|--------------|
| **`2π`** | **`z → z`** | **`ψ → −ψ`** |
| полный оборот | **360°** | **720°** (spin **½**) |

Без SU(2) нет Паули → condensate, нет устойчивой matter.

#### 3.10.2 Спинорное состояние

```
z(x,t) = ( z₁(x,t) , z₂(x,t) )ᵀ ∈ ℂ²
|z|² = z†z = |z₁|² + |z₂|²
```

Поле на **`ℂ²`**, не одна фаза на **`S¹`**.

#### 3.10.3 Дискретная SU(2)-эволюция

U(1)-подгруппа:

```
z' = z · exp(iΦ) , Φ ∈ ℝ
```

SU(2) (предел T, §4):

```
z'(x) = U(x,t) · z(x) , U ∈ SU(2)
U = exp( i · Θ_a · σ_a / 2 )
```

Дискретная экспонента (**§3.12.5**):

```
ω = exp(i · 2π / N_ring) , N_ring = 512 (§3.12.6)
Φ ∈ Z_{N_ring}
R(Φ) : (U,V) ↦ ( U·c_Φ − V·s_Φ , U·s_Φ + V·c_Φ ) (mod N)
δZ(Φ) = R(Φ) − Z
```

| режим | M | предел T |
|-------|---|----------|
| шаг **g** | **`R(Φ)`** на обе компоненты | **`exp(i·Φ·σ_z/2)`** |
| SU(2) mix | **`U(n,φ)`** | **`exp(i·φ·n·σ/2)`** |
| spin-½ | **`R(N/2) → −z`** | **2π→−1**, **4π→+1** |

**Следствие:** **`U(2π)=−𝟙`**, **`U(4π)=+𝟙`** на pra-vortex **`hV`**.

#### 3.10.4 Паули и K_P

Два вихря с одинаковой спинорной ориентацией не делят один **`v_p`**:

```
z_A ∥ z_B ⇒ z_A + z_B → 0
```

**`K_P`** → отталкивание при **`d ≤ l_P`** (Pauli pressure на решётке).

### 3.11 U(1)_vac и хиральность

#### 3.11.1 U(1)_vac

```
z → z · e^{iθ}
```

| объект | формула | U(1) |
|--------|---------|------|
| holonomy | **`ζ = (Σ z)·z*`** | invariant |
| Bloch | **`n = z†σ z / |z|²`** | invariant |
| axis | **`n_z × n_s`**, fallback **`n_z`** | invariant |

#### 3.11.2 Хиральность

```
P_L z = (z₁, 0)ᵀ P_R z = (0, z₂)ᵀ z = P_L z + P_R z
```

| фаза | действие |
|------|----------|
| STREAM | **`P_L`**, **`P_R`** независимо на **`N`** |
| COLLISION | **`K_P` + Arg** смешивают компоненты |

**`|z_L|² − |z_R|²`** — U(1)-инвариантный chirality readout.

#### 3.11.3 P, C, T

Обратимость — **A13 / §3.12**. Anti-unitary **`Θ = P∘C∘T`** — предел T (§4):

```
P : (x,y) ↦ (−x,−y)
C : z ↦ z*
T : z ↦ i·σ_x · z (flip chirality)
```

Обратный шаг M:

```
z(t−1) = 2z(t) + ⌊𝒩⌋ − z(t+1)
```

### 3.12 Закон g на ℤ_N[i]

**Следствие 0.7:** **g** замыкает §3.2; **`p,L,F`** — **Следствие 0.8** / **Теорема 5.1**.

#### 3.12.0 Планck natural units

При **`hL=l_P=1`**, **`hT=t_P/√2=1`**, **`m_P=1`**, **`ρ_P=1`** (§1, §7):

| | M |
|--|---|
| конечный бюджет **`hV`** (Bekenstein) | целочисленный спинор **`ℤ_N[i]`** |
| дискретные фазовые сдвиги **`Δφ≥½`** | saturating **`Φ`**, **`R(Φ)`** |
| обратимость | CA **2-го порядка**; иначе хаос от округления |

#### 3.12.1 Leapfrog

**`Z(x,t) ∈ ℤ_{N_ring}[i]`**, **`N_ring = 2^{⌊B_{hV}⌋}`** (§3.12.6):

\[
Z(x,t+\Delta t) + Z(x,t-\Delta t) = 2Z(x,t) + \lfloor \mathcal{N} \rfloor
\]

**`⌊𝒩⌋`** — projected collision (§3.12.5). Память **`(Z_t, Z_{t−1})`**.

#### 3.12.2 Обратимость

```
z(t−1) = 2z(t) + ⌊𝒩(z(t))⌋ − z(t+1)
```

**A13:** **`g⁻¹`** из leapfrog и kick ledger.

#### 3.12.3 Projected collision

| | |
|--|--|
| forward | **§3.12.5** → **`R(Φ)`** → **`Z⁺+Z⁻=2Z+⌊𝒩⌋`** |
| reverse | **`Z⁻ = 2Z + ⌊𝒩⌋ − Z⁺`** |

#### 3.12.5 Projected collision

**`Z = U + iV ∈ ℤ_N[i]`**, **`N_ring = 512`**, **`2π_disc = N_ring`**.

\[
\omega = e^{i\cdot 2\pi/N_{\mathrm{ring}}},\qquad
R(\Phi) = \omega^{\Phi}
\]

Holonomy (без `atan2`):

\[
\zeta_{\mathrm{real}} = \left\lfloor \frac{\langle U\rangle U + \langle V\rangle V}{2^{F}} \right\rfloor,\quad
\zeta_{\mathrm{imag}} = \left\lfloor \frac{\langle V\rangle U - \langle U\rangle V}{2^{F}} \right\rfloor
\]

Saturating kick:

\[
\Phi_{\mathrm{kick}} = \left\lfloor \frac{K_P\,\zeta_{\mathrm{imag}}}{\zeta_{\mathrm{real}} + |Z|^2 + K_P} \right\rfloor \in \mathbb{Z}_{N}
\]

+ Heisenberg floor **`|Φ| ≥ Δφ_disc`**. **`⌊𝒩⌋ = R(Φ)−Z`** на обеих компонентах.

Leapfrog:

\[
Z(x,t+\Delta t) + Z(x,t-\Delta t) = 2Z(x,t) + \lfloor \mathcal{N} \rfloor \pmod N
\]

#### 3.12.5a ε-задержка и предел массы

Связь с strain §8.4.2-C′ (те же **ρ**, без второго knob).

\[
\rho(x)=|Z(x)|^2,\quad
\varepsilon_x=\frac{\rho(x)-\rho_{\mathrm{vac}}}{\rho_{\mathrm{vac}}},\quad
|Z|^2=\rho_{\mathrm{vac}}(1+\varepsilon_x)
\]

\[
\Phi[\varepsilon]=\left\lfloor\frac{K_P\,\zeta_{\mathrm{imag}}}{\zeta_{\mathrm{real}}+\rho_{\mathrm{vac}}(1+\varepsilon_x)+K_P}\right\rfloor,\quad
Z^{+}=2Z+\lfloor\mathcal{N}[\varepsilon]\rfloor-Z^{-}
\]

**A7:** **`m_loc ≤ m_P`** на одном **`v_p`**. BC **`D_⋆`**, профиль **`h_00`** — §8.4.2-C′′′.

#### 3.12.6 Bit budget hV

\[
B_{hV} = \frac{2\pi}{\ln 2} \approx 9.065\ \text{bit}
\]

| | |
|--|--|
| **`N_ring`** | **512** = **`2^{⌊B_{hV}⌋}`** |
| **`N_φ`** | **13** = **`⌈4π⌉`** |
| **`Δφ_disc`** | **41** ticks mod 512 |
| **`B_amp`** | **6** |

#### 3.12.7 Congruence ladder

Из **`N_ring=512`** и stencil **`N_{12}`**:

##### A · Лестница

```
физика → Z_{512}[i] → вычеты mod E₀,p₀ → holonomy Σ_N → leapfrog
```

##### B · Тождества

| id | identity | следствие |
|----|----------|-----------|
| CL-1 | **`Z⁺+Z⁻ = 2Z+⌊𝒩⌋ (mod N)`** | **T⁻¹** (A13) |
| CL-2 | **`R(N/2) → −z`** | spin-½ |
| CL-3 | **`|Φ| ≥ Δφ_disc` или `Φ=0`** | A16 |
| CL-4 | **`n_E = ⌊|Φ|/Δφ_disc⌋`** | **E = n_E·E₀** |
| CL-5 | **`gcd(Δφ_disc, N)=1`** | генератор **`ℤ_N`** |
| CL-6 | **`gcd(N_φ, N)=1`** | 13 секторов |
| CL-7 | **`Σ ΔE ≡ 0 (mod E₀)`** | §5.2.3 |
| CL-8 | **`Σ Δπ ≡ 0 (mod p₀)`** | §5.2.1 |

**Числа:** **`N=512`**, **`Δφ_disc=41`**, **`N_φ=13`**, **`γ=1/12`**.

##### C · Склейка с N_{12}

\[
\zeta = f\!\Bigl(Z(x),\ \sum_{y\in N_{12}(x)} Z(y)\Bigr)
\Rightarrow \Phi\in\mathbb{Z}_N \Rightarrow \lfloor\mathcal{N}\rfloor,\ \text{leapfrog}
\]

##### D · Открытые вопросы

| | |
|--|--|
| CL-O1 | спектр **`Φ`** на **`N_{12}×ℤ_N[i]`** |
| CL-O2 | umklapp **`Σ Δp ≡ 0 (mod ℏG)`** (§5.2.4) |
| CL-O3 | орбиты **g** на конечном алфавите |
| CL-O4 | факторизация **`2^9`** vs **`N_φ=13`** |

Заряд **`n∈ℤ`** (A10) — не mod **`N`**.
