# Кипящий океан

**Манифест** дискретного носителя (M→T→SM): клеточный автомат на планковской решётке, без претензии «информационная модель вселенной».  
Техническая спецификация: `MODEL.md` · протокол: `BUILD.md` · журнал разработки: `DEVLOG.md`.

---

## 1. Аксиоматический базис

Вместо попыток сшить непрерывное пространство Эйнштейна с квантовыми точками — **смена парадигмы**:

- Пространство-время **фундаментально дискретно**, шаги **константны**.
- Физика не ищет «абсолютную истину» — строит **предсказательные модели**.
- Макро-плавность времени — иллюзия («монтаж» дискретных биологических кадров).
- Нет непрерывного предела `hL → 0`. **Континуум на M — нет** (§1.4 `MODEL.md`): `t_P` — continuum-метка; M-тик **`hT ≠ t_P`**. **T** — статистическое макроописание на **фиксированной** сетке (может *выглядеть* гладким).
- **Пространство = решёточная жидкость:** меньше `v_p` нет → **несжимаемая** комплексная «жидкость» на пикселях; вакуум = кипящий океан, частицы = vortex, T = гидродинамика (§5.1 `MODEL.md`).

---

## 2. Математическое ядро M

Решётка `ℤ^d`, шаг `hL = l_P`, M-тик **`hT = t_P/√2`** (§7.1).

**Две скорости:** `c₀ = l_P/hT = √2·c` (M); macro **`c = κ·c₀`**, **`κ = 1/√2`** → CODATA `c`.

```
z(x,t) = u + i·v ∈ ℂ
z(x, t+1) = g( { z(y,t) }_{y ∈ N(x)} )
```

`N(x)` — ε-окрестность; в MVP: **von Neumann `N₄`**, Moore запрещён (размерный конус `Δs²=c²Δt²−Δx²≥0`, §1.3).

---

## 3. Три кита микро-динамики

| Кит | Смысл | В `MODEL.md` |
|-----|-------|--------------|
| **Унитарность** | `Σ_x \|z(x,t+1)\|² = Σ_x \|z(x,t)\|²` | A3 |
| **Локальное перемешивание** | max локальной энтропии → `Δ_disc`, аналог Коши–Риман | A6, A9 |
| **Недостижимость абс. нуля** | `\|z\|→0` → фазовый «взрыв», вакуум кипит | A5, `𝒩(z)` |

---

## 4. Замкнутое уравнение (`MODEL.md` §3.4)

**Isotropic M (shipped):** одна строка, без лучей · `linear_mode=isotropic`

```
z(x,t+Δt) = z · exp( i·2π · [ K_P·α* / (ρ_E + K_P) · Δφ_N₄ − 1 ] )
ζ = (Σ_{N₄} z)·z* ,   Δφ_N₄ = Arg(ζ) ∈ (−π,π]   (не Arg(Σz)−Arg(z))
ρ_E = μ_P·c²·|z|² = u_P·|z|² ,   K_P = u_P ,   α* = 1 + 1/(4π)
```

**Историческая** однострочная запись §4 ниже и split-step `local_ca` — **legacy**; см. §3.6 `MODEL.md`.

При `c = hL/hT = 1`, `β = 1` (старый манифест):

```
z^{t+1} = z^t · e^{-iω}
        + Δ_disc z^t
        + i · ( (ω + ½) / (|z^t|² + 1) − ω ) · z^t
```

`ω` — частота «холостого хода» вакуума.

**Статус реализации:** см. §3 манифеста ↔ `MODEL.md` §3 — **переписано split-step** для A3 (унитарность). Одна строка манифеста **не** сохраняет норму как записана; рабочий код — `local_ca` + `exp(iφ)`.

**Эквивалентная структура (split-step, код):**

```
z* = g_lin(z)                    — локальный unitary mixing (не raw Δ_disc add)
z' = z* · exp(i·Φ(|z*|²))        — vacuum gate 𝒩(z)
```

Связь параметров: `(ω + ½)` в манифесте ↔ `2π·α*` при `α* = 1 + 1/(4π)`; **α_fs = 1/(4π³+π²+π)** — §8.2 `MODEL.md`.

---

## 5. Размерный мост (SI)

```
Δt = hT = t_P/√2 ,  Δx = l_P
z(x,t+Δt) = z·e^{-iωΔt} + l_P²·Δ_disc z + i·( (ωΔt + ½) / (ρ_E/u_P + 1) − ω )·z
Δ_disc z = ( Σ_{y∈N} z(y) − 4z(x) ) / l_P²
ρ_E = u_P · |z|² ,   u_P = K_P = μ_P·c² = c⁷/(ℏG²)
```

Код: **естественные единицы** (`|z|² ↔ ρ_E/u_P`). Константы: `SI.mu_P`, `SI.u_P`, `SI.K_P` · §7 `MODEL.md`.

---

## 6. Как прорастает T (без пределов)

| Эффект T | Механизм в манифесте | Статус |
|----------|----------------------|--------|
| **Гладкость / Lorentz** | фотон «плывёт» над многими `l_P`; статистика больших чисел | гипотеза A12; **усреднение не зафиксировано** |
| **M → T гидродинамика** | Madelung на N₄: continuity + gate = давление | §5.2 `MODEL.md` |
| **Масса покоя** | топологический vortex + зигзаг в ε-окр. от `𝒩` | §5 `MODEL.md`; численно TODO |
| **Запутанность / голография** | жёсткость дискретной аналитичности | A9 метрика; не в `g` |

**DFT / дискретный спектр** — инструмент **T-слоя** (`t_analysis.py`), не замена локального `g`.

---

## 7. Критический эксперимент (самопроверка)

> Без внешних эталонов. Два инварианта на экране симулятора после coarse-graining.

### T1 · Угловatость (Lorentz)

Импульс / волновой пакет на квадратной решётке. Микро — ромб. **Macro** (block-mean): если фронт → **гладкий круг** с одинаковой c по θ — Lorentz на T подтверждён.

### T2 · Солитон и интерференция

Устойчивый движущийся macro-паттерн на кипящем вакууме; столкновение двух пакетов → интерференция, не mush.

**Код:** `validate_mt.py` · `mt_ca/t_validation.py`

| Тест | Статус (512²) |
|------|---------------|
| T1 isotropy | weak PASS (1.17 vs 1.25) |
| T2 soliton | PASS |
| T2 collision | PASS |

---

## 8. `c = k·c₀`, k = ½

Micro causal speed на решётке: **`c₀ = l_P/t_P`**. Macro isotropic front — **вписанный круг в ромб** `N₄`:

```
R_⊥ = (c₀ t) / 2   ⇒   c = k c₀ ,  k = 1/2
```

В SI `c₀` уже равен наблюдаемой `c`; `k` — **геометрия M→T**, не второй «физический» свет.

**«Сверхсвет» для T:** micro `c₀` vs macro `c₀/2` ⇒ **2× macro-c**, но только для структуры **~`dl`**, одного `dV` (§5). Составное тело — снова macro-c. Micro-каузальность `≤ c₀` не ломается.

Космология — **`MODEL.md` §9** (логика закрыта):

- **4 фазы:** расширение → heat death → «лёд» → новый BB (без сингулярности, A3+A5+тор)
- **Бесконечность** тиков в прошлое/будущее
- **M → T → UI:** vortex `v_p` → зигзаг/mass → интерференция → coarse-grain мозга

Не GPU-DoD; long-run sim — отдельный leaf.

---

## Карта: манифест → репозиторий

| § манифеста | Файл / § |
|-------------|----------|
| 1–3 | `model/00`–`02` (основания + аксиомы; genesis → `DEVLOG.md` §7) |
| 4 | `model/03-evolution.md` · `mt_ca/update.py` |
| 5 | `model/06-si-sm.md` §7 · `config.py` (SI anchors) |
| 6 | `model/04`–`05` · fluid §5.1 |
| 7 | `validate_mt.py` · `run_benchmark.py` |
| 8 | `model/01-carrier.md` §1.1 |
| 9 | `META.md` — циклы, бесконечность, генезис T, наблюдатель |

