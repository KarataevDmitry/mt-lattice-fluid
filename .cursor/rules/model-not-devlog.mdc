---
description: MODEL vs DEVLOG boundary — physics SSOT vs impl/verify/narrative
globs: model/**/*.md, MODEL.md, DEVLOG.md
alwaysApply: true
---

# MODEL ≠ DEVLOG (hard gate)

Граница слоёв: `DEVLOG.md` §0 · verify **`Model_purity`**.

## MODEL (`model/00`…`06`)

**Только:** посылки, утверждения, теоремы, следствия, формулы, опыт — **один голос**, как в статье.

**Не писать в section-файлах:**

- `**Код:**` / `Код:` / пути `mt_ca/…` / `*.py`
- `verify **`…`**` / PASS / FAIL / ✅ / ⚠️ как статус impl
- колонки `impl` / `verify` / GPU DoD / module map
- provenance, даты сессий, narrative «мы догнали»
- мета-шапки, ярлыки «legacy / OPEN / слой M»
- слово **«канон»** — в физике его нет; есть посылки, утверждения, теоремы, следствия, опыт

**Граница живёт один раз:** [`MODEL.md`](MODEL.md) hub · [`DEVLOG.md`](DEVLOG.md) §0 · это rule · verify **`Model_purity`**.

## DEVLOG (`DEVLOG.md`)

**Только сюда:** реестр §1, open leaves §3, impl status §4, verify snapshots, GPU DoD, module map, даты, ✅/⚠️.

## При правке §3

- **Закон `g`** — §3.12. §3.1–3.2: каркас; γ, Φ, L, α*, ε не фиксируются на каркасе.
- Float-прототип §3.3–3.4 — только DEVLOG.
- Moore — следствие A1, не выбор neighborhood.
- Вырезанное из MODEL → DEVLOG §1/§3/§4, не выбрасывать.

## Перед ship

```bash
python verify_principles.py --device cpu
```

**`Model_purity` must PASS.** Иначе правь MODEL, не ослабляй gate.
