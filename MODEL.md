# M → T → SM lattice fluid

**Манифест:** [`MANIFEST.md`](MANIFEST.md) — исходная формулировка «Дискретная информационная термодинамика».  
**Meta** (космология, observer UI, access) → **[`META.md`](META.md)** — **не SSOT**; не добавляет knobs в `g`.  
**Devlog** (impl, verify, open leaves) → **[`DEVLOG.md`](DEVLOG.md)** — **не SSOT**; не меняет `g`.  
**SM constants** — §8, часть **MODEL**, не meta.

### Иерархия слоёв (SSOT)

```
физика (известное)  →  M   MODEL.md §0–§8   — канон: g, аксиомы, ДА, M→T→SM
                           ↓  следует
                      impl mt_ca/            — реализация; **может отставать** от M
                           ↓  проверяет (impl ↔ M)
                      sim  validate_mt, GPU  — не «истина»; ловит sim-gap и T-метрики
                           ↓
                      T    coarse, readout     — наблюдаемый слой
                           ↓  (интерпретация)
                      Meta META.md           — космология, UI, access; **не refute M**
                      Devlog DEVLOG.md       — impl status, verify, open; **не refute M**
```

**Истина идёт из MODEL.** Impl и sim **догоняют**. Пока MODEL строится — разрывы **двух родов** (ниже).

### M vs Meta / Devlog (не смешивать)

| | **MODEL** | **META** | **DEVLOG** |
|---|-----------|----------|------------|
| Роль | **SSOT физики** | UI / космология **над** M | impl / verify / open |
| Примеры | `g`, `Z_N[i]`, A1–A16, §5.2 | циклы §3 META, observer | ✅/⚠️, provenance, GPU DoD |
| Спор | «что делает `g`?» → только MODEL | «что видит мозг?» → META | «догнали код?» → DEVLOG |

### Три вида gap (пока M строится)

| gap | смысл | чей долг | лечение |
|-----|--------|----------|---------|
| **model-gap** | в M **не применили** уже известную физику | **MODEL** | дописать/вывести §, аксиому, следствие |
| **sim-gap** | в MODEL **уже сказано**, impl/sim **не догнали** | **mt_ca** / verify | код, projected step, harness |
| **T-metric** | грубый readout / слабая метрика T | **validate_mt**, macro | не «M refuted» |

**Model-gap** = «забыли из уже известного» (Noether, `F=mg`, один `g` на `Z_N[i]`).  
**Sim-gap** = «написали, не закодировали» (`ρ_max` clamp, U(1) probe на кольце).  
**T-metric** = ν_CA readout, T1 круг — **не** дыра в `g`.

**Правило:** sim ≠ M → сначала классифицировать gap; **не** ослаблять MODEL под sim.

### Ночной канон M (2026-09-22) — принято

| # | Тезис | § |
|---|--------|---|
| 1 | Изотропное **`g`**, **`ζ=(Σz)·z*`**, phase wrap | §3.4 |
| 2 | Лучи/sweeps → micro-aniso; default isotropic | §3.6 |
| 3 | Smear → Гейзенберг Δφ≥½ + K_P триггер → anti-smear | §3.7 |
| 4 | **`N₄`** слабые волны; тяжёлый vortex → hex/D2Q9 **если** тест | §3.8 |
| 5 | **ДА:** голоморфная среда, **`g`** = удержание аналитичности | §3.9 |
| 6 | Полюс **`hV`**, вычет **`2πn`**, заряд | §3.9 |
| 7 | CR-единственность → **единое поле Λ**, запутанность **на том же Δt** | §3.9 |
| 8 | GPU: **`DX, DT, K_P, ALPHA*`** | §10.4 |
| 9 | 4 топокласса seeds | §9.7 |
| 10 | **SU(2) спинор** `z∈ℂ²`, **720°** → identity; **Паули** + **`K_P` отталкивание** | §3.10 |
| 11 | **system_quanta:** **`Δx, Δt`, `N_frame`, `I_max`**; мозг/UI → META §2 | §4.8 · META §2 |
| 12 | **Любая система:** из **`M, τ, R`** → **`N_frame`**, fractal **`m_P/M`** | §4.8 |
| 13 | **Газ:** macro-облако **`hV`**; **`PV=nRT`**; **`v_s ≪ c`**; **VdW `a,b` из `K_P,l_P`** | §5.3 |
| 14 | **Плотность M бинарна:** **`ρ ∈ {0, ρ_P}`**; macro **`1000 kg/m³`** = T-иллюзия Gaussian readout | §5.0 |
| 15 | **Масса M:** **`Arg(⟨z⟩/z)`**-зигзаг + **`K_P`**; **Higgs = T-пена**, не первопричина | §5.0.1 · §8.3 |
| 16 | **Arg-квант:** **`s₀=ℏ/2`**, **`E₀=E_P/√2`**, **`m_arg=m_P/√2`**, **`v_arg=c₀`** | §5.0.2 |
| 17 | **Дуализм = T-иллюзия:** M — чистая жидкость; «частица» = солитон; Young = CR-схлоп | §4.9 |
| 18 | **Антиматерия = `n→−n`:** зеркало фазового вихря; annihilation = `n++n−→0` → 2γ | §5.0.3 · §9.2 |
| 19 | **`U(1)_vac` + хиральная упаковка:** global phase на `z`; **`defect_axis` fallback = local Bloch**, не `(0,0,1)`; **`P_L/P_R`** + независимый STREAM | §3.11 |
| 20 | **P / C / T legs (T-layer probes):** mirror, `z*`, chirality boost — **не** M `g⁻¹` | §3.11.3 · A14 |
| 21 | **M = leapfrog на ℤ:** **`l_P=t_P=1`** → нет float; **`z⁺=−z⁻+2z+⌊𝒩⌋`**, `(z,z_past)`; **T⁻¹** = algebra, не CPT-approx | §3.12 · A13 |
| 22 | **Projected collision = Z_N[i]:** **`N_ring=512`**, **`N_φ=⌈4π⌉=13`**, **`frac_bits=⌈log₂(512/13)⌉=6`**, **`Δφ_min=½` rad** | §3.12.5–§3.12.6 |
| 23 | **Локальные законы + SO(2):** **`p₀,L₀,F₀`** из **`s₀`**; **`div j=0`** на **`N₄`**; **`L_z ∈ L₀·ℤ`** | §5.2.1 |
| 24 | **`κ_link = 1/4`:** **`γ = cr_strength = ν_CA_natural`**; **`E₀ = p₀·c₀ = F₀·l_P`**; **`b ∈ {0,1}`**; **§5.2.3** — Pauli/sync/**`n_E`** без float | §5.2.2–§5.2.3 |
| 25 | **Gate M = `ω^Φ`:** **`exp(i·Θ·σ/2)`** — T-нотация; tick = **`R(Φ)`** / Rot_LUT на **`Z_N[i]`**, не matrix exp | §3.10.3 · §3.12.5 |

---

## §0. Генезис модели (начало диалога → M)

**Вход:** «почему под травой замедляется время?» → механизм ОТО (искривление метрики, геодезики, redshift).

**Сдвиг:** та же картина мира, что у дискретного автомата — **время и «ходы»**, не плавный континуум «из коробки».

**Гипотеза:** если время **квантуется**, мир на элементарном уровне **дискретен**.

**Идентификация квантов** (не подгонка постфактум):

```
hT = t_P/√2   — **истинный** M-тик (§7.1); t_P = l_P/c — учебниковый, не hT
hL = l_P     — шаг пространства = планковская длина
c₀ = hL/hT = √2·c   ;   c = κ·c₀ , κ = 1/√2   (macro CODATA)
```

Другого «ещё меньшего» шага в M нет: ниже `dl` не входит (§1, §5).

**Следствие:** элементарный уровень = **КА с фиксированными шагами** `(hL, hT)`. Состояние — **целочисленный спинорный инвариант** на `v_p` (§3.12); эволюция — **локальная `g` 2-го порядка**, один тик = один `hT`. Float32 на M — только T-/debug, не физика ячейки.

**Общий вид `g`** (§3) следует сразу из дискретности + локальности + каузальности (один тик → `N₄`, не Moore):

```
Ψ^{t+1} = g(Ψ^t)     локально по окрестности, шаги фиксированы
```

**Затем** — не до — на этот каркас навешиваются **абсолютные условия**, известные из физики:

| Условие | Следствие для `g` |
|---------|-------------------|
| Абсолютный ноль недостижим (A5) | vacuum gate, `z≡0` не устойчив |
| Энтропия не убывает локально (A6) | mixing / `Δ₄` |
| U(1), сохранение заряда (A4) | только `exp(iφ)` |
| Унитарность / норма (A3) | unitary linear step — **цель, см. §2** |

Полный реестр и пробелы — **§2**.

**Не meta:** предсказательная модель = `g` + проверяемые T/SM-следствия. **Континуум на M — не гипотеза, ответ «нет»** (§1.4); плавность — readout **T**, не аксиома M.

---

## §1. Дискретное пространство-время (Planck lattice)

```
Λ = (ℤ/Nℤ)^d     (d=2 в MVP)
t ∈ ℤ
dl = hL = l_P    — фиксированный шаг пространства (меньше не влезает)
hT = t_P/√2      — истинный M-тик (§7.1); t_P = l_P/c — учебниковый
dV = dl³         — элементарный объём ячейки
```

**Две скорости (не одна «c=1»):**

| | символ | СИ | natural (sim) |
|---|--------|-----|----------------|
| **тактовая CA** | `c₀` | `l_P/hT` = **√2·c** ≈ 4.24×10⁸ m/s | `c₀ = dl/hT = 1` |
| **свет в вакууме (T)** | `c` | **299 792 458 m/s** (CODATA) | `c = κ·c₀`, κ=1/√2 |

**Сшивка (не костыль):** учебниковый `t_P=l_P/c` предполагает `c₀=c`. На N₄ macro readout **`c = κ·c₀`**. Чтобы CODATA **`c`** совпала: **`hT = t_P/√2`**, **`Δx = l_P`**, тогда **`c₀ = √2·c`** и **`κ·c₀ = c`**.

**Natural units** в коде — `dl=hT=1`; размерный конус на M — **`Δx=l_P`**, **`Δt=hT`**, **`c₀=l_P/hT`** (не macro `c` в одном тике).

### 1.4 Континуум-гипотеза → **нет** (на M)

**Учебниковый** `t_P = l_P/c` — не «ошибка измерения», а **определение в continuum-рамке**:

- одна **`c`** на всех масштабах;
- смысл предела **`hL → 0`**, **`hT → 0`**;
- гладкое `(x,t)` как первичное.

**Как только** N₄ + **`κ = 1/√2`** + CODATA **`c`** заставляют **`hT = t_P/√2`** и **`c₀ = √2·c`**, continuum-упаковка **не описывает M**:

| continuum-рамка | M |
|-----------------|---|
| один тик `t_P` | **`hT = t_P/√2`** |
| link-speed = `c` | **`c₀ = √2·c`** |
| macro `c` = link-speed | **`c = κ·c₀`** (readout) |
| предел `hL→0` | **нет** — `dl = l_P` const |

**Ответ на континуум-гипотезу (фундаментальный уровень):** **нет**.  
M — **решётка + локальный `g`**. Непрерывность, диффуры, «плавное» время — **T** (coarse, DFT, статистика на **фиксированной** сетке), не предел сетки.

**Не путать:** T **может** выглядеть континуумно — это **эффективная** теория наблюдателя; это **не** «M → continuum при `dl→0`».

**Ниже `dl` нет смысла и нет доступа:** решётка не сжимается; «внутри пикселя» меньше планковской длины **не входит** — это минимальный квант пространства M.

**Каузальность одного тика:** информация не дальше **одной осевой** клетки.

```
N(x) = N₄(x) = { x ± ê_i }     (2D: 4 соседа)
```

**Moore запрещён:** диагональ за один `hT` потребовала бы `\|Δx\|=\sqrt{2}\,l_P` ⇒ `\sqrt{2}\,c₀` — см. §1.3.

### 1.1 Две `c` и мост **`c = κ·c₀`**

#### `c₀` — тактовая скорость M

```
c₀ = l_P / hT = √2 · c ≈ 4.24×10⁸ m/s     (SI, M-такт)
c₀ = dl / hT = 1                            (natural, sim)
c  = κ · c₀ = c_CODATA                      (T, κ = 1/√2)
```

Один тик `hT` — не дальше **одной осевой** клетки `l_P`. Конус на N₄ — **ромб** (L1), не круг.

#### `c` — macro T (измеряемая «скорость света в вакууме»)

**CODATA `c`** = **`κ·c₀`** при **`hT = t_P/√2`**. Не «медленнее на 30%» — **учебниковый `t_P` не равен M-тику**.

```
c_T = κ · c₀        (natural; κ — readout, не новая константа SI)
```

**Механизм (provenance):** фотон = macro-пакет; micro-траектория — зигзag по осям N₄; **L1-фронт** `|x|+|y|≤c₀t` (ромб). Macro-круг = **вписанная** окружность в ромб.

**κ из геометрии N₄** (проверено BFS + аналитика, `scripts/check_kappa.py`):

```
L1-радиус R = c₀·t
ось:     (R,0)         → euclidean R      → κ_axis = 1
диаг.:   (R/2,R/2)     → euclidean R/√2   → κ_diag = 1/√2
macro:   вписанный круг в ромб → R/√2     → κ = 1/√2
```

**Частая ошибка (была здесь):** перепутать координату `R/2` с euclidean-радиусом точки `(R/2,R/2)` — там **`R/√2`**, не `R/2`. Ложное **`κ=1/2`** = **забыли √2**.

```
κ = 1/√2 ≈ 0.7071     (канон для isotropic macro-круга)
```

| Слой | Скорость (natural) | Геометрия |
|------|---------------------|-----------|
| M, ось | `c₀` | ромб N₄ |
| M, диаг. | `c₀/√2` | угол 45° на границе ромба |
| T (isotropic readout) | **`κ·c₀`, κ=1/√2** | вписанный круг |
| SI macro | `c = κ·c₀` = CODATA | §7 |

**Sim note:** T1 (`validate_mt.py`) **не калибрует κ** сейчас — radial probe → NaN; `g` diffusive, не ballistic front. Геометрия конуса жёсткая; κ **не fitted**.

### 1.2 «Сверхсвет» относительно T — только на масштабе `dl`

**Micro (M)** causal bound: не быстрее **`c₀`** по решётке (A1). Это потолок, не энергетический тюнинг.

**Macro (T)** наблюдает **`c_T ≈ c₀/√2`** (isotropic readout). Структура с **полным micro-конусом** `c₀` по оси для T-наблюдателя:

```
v_T ≈ (c₀ / c_T) · c_T  = √2 · c_T        (относительно macro-«света»)
```

«Сверхсвет» здесь = **быстрее macro-c**, не нарушение micro-каузальности и не `v > c₀^SI`.

**Условие доступа:** локализован в **одном `dV`** (элементарный масштаб, `dl = l_P`). Составная структура (>1 dV) **усредняется** на T → снова **`κ = 1/√2`**, macro-c.

```
размер ≈ dl ,  в одном dV   ⇒   может нести micro c₀ (ось)
размер ≫ dl ,  составная     ⇒   T видит c_T ≈ c₀/√2
```

**Честность:** это следствие геометрии M→T + §5 (элементар vs составная), не ticket для макро-FTL. Сигнал быстрее macro-c **не выходит** за micro-ромб `c₀`; «быстрее c» — артефакт coarse readout.

### 1.3 Окрестность ε — из **симметрии** пространства-времени (provenance)

**Genese черновика:** Moore/LBM → **симметрии** → **размерный** световой конус → **N₄**.

#### Световой конус = ε (размерный)

За один M-тик `Δt = hT = t_P/√2` сосед на `Δx` каузален на **M** iff (используем **`c₀`**, не macro `c`):

```
Δs² = c₀² Δt² − Δx²  ≥  0 ,   c₀ = l_P/hT = √2·c
```

| сосед | `Δx` | `Δs²` | один `hT`? |
|-------|------|-------|-------------|
| ось N₄ | `l_P` | `c₀²hT² − l_P² = 0` | ✅ light-like |
| Moore диаг. | `√2 l_P` | `−l_P² < 0` | ❌ space-like → **√2·c₀** запрещено |

**Macro interval** с CODATA `c` и **`hT`**: `c²hT² − l_P² < 0` на оси — macro `c` относится к **T-readout**, не к одному M-тику.

**Вывод:** на квадратной решётке **каузально честная** micro-ε = **von Neumann `N₄` (крест)**. Moore **не** «компромисс первой версии» — **нарушает A1**, если link за один tick.

#### Альтернативы (не канон MVP; из истории / для специалиста КА)

| путь | смысл | статус в репо |
|------|--------|---------------|
| **Гекс** (6 соседей, FHP) | все `Δs²=0`, micro-изотропия | open leaf (другая топология) |
| **Moore + D2Q9 веса** | LBM-кompensация диагонали | **не M** — другой оператор mixing |
| **Стохастическая ε** (Bandman) | размыть анизотропию micro | против **детерминизма §2.1** на M |

**Круг / Lorentz на T:** не Moore-micro, а **coarse-grain** + **`κ=1/√2`** (§1.1) + кипение A5 · T1 (`validate_mt.py`). Micro остаётся **ромб**; macro стремится к **кругу**.

#### `f_геометрия`

Тип ε → **`f_геометрия(n, ε)`** (§8.2). Канон: **`N₄`**, код: `laplacian.py`, `linear_step_local_ca`.

**Канон M (этот репо):** **`N₄` only** — не инженерный выбор, **проекция светового конуса** на дискретный граф.

---

## §2. Абсолютные условия (реестр)

Навешиваются **на каркас `g`** (§3.1), **после** дискретности + КА. Не наоборот.

| # | Условие | Следствие для `g` |
|---|---------|-------------------|
| A1 | **Каузальность**, `c = dl/hT = 1` | один тик → `N₄`, Moore запрещён |
| A2 | **Локальность** | `g` зависит только от ε-окрестности |
| A3 | **Унитарность / сохранение информации** | `Σ|z|²` invariant; linear unitary; nonlinear = `exp(iφ)` |
| A4 | **U(1) заряд / SU(2) спин** | спинор **`ℂ²`**, gate **SU(2)** (§3.10); A4 = unitary **`exp(iΦ)`** на спиноре |
| A5 | **3-й закон: абсолютный ноль недостижим** | vacuum gate при \|z\|→0; вакуум = осциллирующий фон |
| A6 | **2-й закон (локально): энтропия не убывает** | mixing / max локальной фазовой энтропии при фикс. норме → `Δ₄` |
| A7 | **Планковский потолок плотности** | `\|z\|² ↔ ρ_E/u_P ≤ 1`; знаменатель `+ε`, `ε↔u_P` |
| A8 | **Макро-линейность** | при больших \|z\| нелинейность **затухает** (T-гладкость) |
| A9 | **Дискретная аналитичность (Коши–Риман)** | `g` = оператор удержания голоморфности; CR-единственность → глобальное поле Λ |
| A10 | **Топологическая квантованность заряда** | полюс **`hV`**; **`∮ d arg = 2πn`**, **`n∈ℤ`** (вычет ДА) |
| A11 | **Устойчивость солитона / anti-smear** | размазанное **не голоморфно**; **`K_P`+`Δφ` затвор** → самофокусировка |
| A12 | **Изотропия / Lorentz (T)** | micro: **isotropic streaming** (§3.6); macro-круг — **(1-2-1)** readout + **`κ=1/√2`** |
| A13 | **Обратимость шага (микро)** | **2-й порядок** **`z⁺=−z⁻+2z+⌊𝒩⌋`** на **ℤ** (§3.12); **`g⁻¹`** = algebra / swap |
| A14 | **P / C / T / U(1)_vac** | discrete symmetry probes on `g`; long **`g·P`** open |
| A15 | **Принцип наименьшего действия** | **`κ=1/√2`**, **`γ=κ_link=1/4`**, **`α*=1+1/(4π)`** из геометрии N₄ + gate (§5.2.2, §7.1) |
| A16 | **Фермион / запрет Паули** | спин **`1/2`**: **`2π` → −1**, **`4π` → +1**; два совпадающих спинора в **`v_p`** запрещены |

**Verify / impl:** [`DEVLOG.md` §1](DEVLOG.md#§1-реестр-аксиом-a1a16-verify).

**Поле и целевой закон (A3):**

```
z(x,t) ∈ ℂ²     — дискретный спинор на v_p (§3.10); **единственное** поле в `mt_ca`
Ψ^t = { z(x,t) }_{x∈Λ}
Σ_x z†z = const   (unitary / SU(2) gate)
```

**Scalar slice (impl v3, bosonic/photon limit):** `z ∈ ℂ`, `Σ|z|²` invariant — §3.4.

### 2.1 Детерминизм M → иллюзия «квантовой случайности» на T

**`g` строго детерминирована:** нет RNG, нет `rand()` в **`projected_step_fixed`**.  
`z(x,t+1) = g({z(y,t)}_{y∈N₄(x)})` — **100% функция** прошлого (A2).

**Откуда тогда вероятность в T / QM?**

1. **Динамический хаос из детерминизма** — gate `𝒩(z)` + mixing (A5, A6): микроскопическое отличие фаз на t=0 → через O(1) тиков непредсказуемый **узор** на M; macro = «белый шум» вакуума.
2. **Грубый прибор** — наблюдатель не читает одну ячейку `v_p`; **Gaussian macro readout** по **≫1** ячеек (§4.1). Суммирование детерминированных фаз → **интерференция** → волновое описание; `|Φ|²` на T = **статистика** macro-фильтра, не dice в `g`.

```
M: детерминированный CA, каждый тик вычислим
T: |Ψ|², Born — термодинамика + масштаб наблюдения, не фундаментальный random
```

**Эйнштейн-позиция в модели:** «Бог не играет в кости» на M; кости — **UI T**, если прибор грубее `hL`.

### 2.2 Будущее и прошлое (meta → [META.md §1](META.md#§1-эпистемология-access))

**На M (здесь):** §2.1 + **A13** — траектория **определена**, `g⁻¹` существует; информация **не стирается** (A3).

**Не в MODEL:** «замок наблюдателя», UI-кадр, «не подсмотреть» analytically, нарратив циклов — **[`META.md`](META.md) §1, §3**. Meta **не** добавляет параметров в `g`.

---


## §3. Функция перехода `g`

### 3.1 Каркас (из КА — до абсолютных условий)

Дискретность + фиксированные `(hL, hT)` + локальность + каузальность **сразу** задают форму:

```
Ψ^{t+1} = g(Ψ^t)
g     = g_nonlin ∘ g_lin
g_lin :  z ↦  L(z)      — линейное смешивание по N₄(x)
g_nonlin: z ↦  z · exp(i·Φ(z))   — локальная фаза (комплексное поле)
```

```
N₄(x) = { x ± ê_i }
(Δ₄ z)(x) = Σ_{y∈N₄(x)} ( z(y) - z(x) )
```

**Ещё не зафиксировано на этом этапе:** коэффициент `γ`, форма `Φ`, unitary vs diffusive `L`, значение `α*`, `ε`.

**Moore нет** — следствие A1, не отдельный выбор.

### 3.2 Абсолютные условия → ограничения на каркас

| Условие | Что запрещает | Что остаётся |
|---------|---------------|--------------|
| A3 унитарность | рост/падение `Σ|z|²`; \|z\|-dissipation | локальные bond-unitaries на `N₄`; `Φ` real |
| A4 U(1) | нелинейность в модуле | только `exp(iΦ)` |
| A5 абс. ноль | устойчивый `z≡0` | `Φ` → large при \|z\|→0 (vacuum gate) |
| A6 энтропия | «замёрзшие» фазы | mixing через product локальных unitary; T-предел |
| A7 ρ_E ≤ u_P | бесконечная плотность в dV | `Φ` с `+ε`, ε ↔ u_P |
| A8 макро-линейность | сильная нелинейность при больших \|z\| | `w(ρ)` + gate asymptotics |
| A9 Коши–Риман | изолированный покой ячейки | метрика CR; vortex core — defect |

**Конфликт A3 vs A6 (legacy):** `z+γΔ₄z` не унитарна. **M:** `linear_step_local_ca`. **T:** DFT-спектр для калибровки ω(k), не micro_step.

### 3.3 Конкретизация (δS + split-step — рабочая, не канон)

**Шаблон действия** (natural units, hL=hT=1):

```
S = Σ_{links (x,y)∈N₄} Re[ conj(z_x)(z_y - z_x) ]     — кинетика / перемешивание (A6)
  + Σ_x V(|z_x|²)                                      — потенциал U(1) (A4, A5)
```

`V(|z|²)`: минимум **не** в `z=0`; вариация → **фазовый** член (A4).

**Split-step из δS=0** (черновик параметров):

```
z* = local_ca(z; γ)                            — bond-unitary sweeps (M)
φ  = 2π · ( α* / (|z*|² + ε) - 1 ) · w(ρ)     — vacuum gate (A5, A7, A8)
z' = z* · exp(i φ)                             — (A4)
```

### 3.4 Финальное изотропное `g` (канон M)

Одна ячейка `v_p = hV` — одно **`z(x,t) ∈ ℂ`**. За один M-тик **`Δt = hT`**: перенос и gate — **один** унитарный `exp(i·…)`, без лучей.

#### Размерный вид (СИ)

```
z(x,t+Δt) = z(x,t) · exp( i · 2π · [ K_P·α* / (ρ_E + K_P) · Δφ_N₄ − 1 ] )
Δφ_N₄ = Arg( Σ_{N₄} z · z* )     ∈ (−π, π]     (не Arg(Σz)−Arg(z))
```

| | |
|--|--|
| **`N₄(x)`** | каузальный крест (§1.3), не Moore |
| **`Σ_{N₄} z`** | сферическое поглощение/излучение фазы; **без** порядка осей |
| **`ζ = (Σ z)·z*`** | **algebraic** phase diff; avoids `Arg(Σz/z)` / `atan2−atan2` branch cuts (§3.4.1) |
| **`Δφ_N₄`** | дискретный **∇φ** (поток жидкости) |
| **`ρ_E = μ_P·c²·\|z\|² = u_P·\|z\|²`** | локальная плотность энергии [J/m³] |
| **`K_P = u_P = μ_P·c² = c⁷/(ℏG²)`** | модуль упругости вакуума [Pa] (§5.1.1) |
| **`α* = 1 + 1/(4π)`** | из gate (§7.1) |
| **`Δt = hT`** | истинный M-тик (§7.1) |

#### Natural (sim)

```
|z|² = ρ_E/u_P ,   ε = 1
ζ = (Σ_{N₄} z) · z* ,   Δφ = Arg(ζ) wrapped to (−π, π]
φ = 2π · ( α*/(|z|²+ε) · Δφ − 1 ) · w(ρ)
z' = z · exp(iφ)
```

**Код:** `update.wrapped_phase_diff` · `isotropic_phase` · default `linear_mode=isotropic`.

#### 3.4.1 Phase wrap (вычислительно, не физика)

**Баг:** `Arg(Σz/z)` или `atan2(Σz) − atan2(z)` → скачок **2π** у границы **±π** → ложное «фазовое давление», шрамы на сетке.

**Fix:** **`ζ = (Σ_{N₄} z) · z*`**; **`Δφ = (Arg(ζ)+π) mod 2π − π`**. Один угол разности, минимальный дуговой интервал. Код: `wrapped_phase_diff`.

**Legacy (`local_ca`):** bond-sweeps + отдельный gate — §3.6, anisotropy ~1.25.

`w(ρ)` — A8 · `α*`, `ε` — §7.1.

### 3.5 Пересборка v2→v3 (слои M / T)

| | M (КА) | T (дискретный анализ) |
|---|--------|------------------------|
| Шаг | локальный `g` на `N₄`, 1 tick = `hT` | coarse / DFT / dispersion readout |
| Унитарность | bond-unitary sweeps + `exp(iφ)` | — |
| FFT | **не в micro_step** | `spectral_apply`, `amplitude_spectrum` |

**v2 ошибка:** DFT внутри `micro_step` — инструмент T на месте M.

**v3:** `linear_step_local_ca` (4 подшага на disjoint links); DFT в `t_analysis.py` для калибровки ω(k) и macro-мод.

**Проверка:** `python verify_principles.py`

### 3.6 «Лучи» убили micro-изотропию (provenance → open leaf)

**Инсайт:** как только в M появились **фиксированные направленные переносы** — LBM-лучи `f_k`, bond-sweeps по **x** затем **y** — micro-пространство стало **анизотропным по конструкции**. Жидкость в «четырёх трубах» не становится изотропным оcean'ом от coarse alone.

**У нас уже так в коде:**

```61:72:d:\Experiments\PersonalCursorFolder\open\mt-lattice-fluid\mt_ca\linear.py
def linear_step_local_ca(z: torch.Tensor, gamma: float) -> torch.Tensor:
    ...
    z = _horizontal_bonds(z, theta, offset=0)
    z = _horizontal_bonds(z, theta, offset=1)
    z = _vertical_bonds(z, theta, offset=0)
    z = _vertical_bonds(z, theta, offset=1)
    return z
```

Четыре **ориентированных** прохода = **лучи на рельсах**. T1: **`micro_anisotropy ≈ 1.25`**, macro ≈ 1.17 — coarse **маскирует**, не лечит корень.

**Что не трогаем:** одна ячейка = одно **`z ∈ ℂ`** на `v_p` (§5) — уже монолит, не «4 луча в памяти GPU» как D2Q9.

**Что не путаем:** **`N₄` ε** (§1.3) — **каузальный** предел «кто достижим за один `hT`»; это **не** LBM-streaming. Moore = диагональные лучи **вне** конуса.

#### Цель: изотропный **перенос** без выделенных осей

Не «костыль κ на T», а M-streaming **без preferred direction**:

1. **Перенос (изотропный):** ячейка тянет фазу из **равноправной** каузальной окрестности — **сфера/круг** на решётке = все соседи **`N₄(x)`** с **одинаковым** весом; не chessboard-sweep по x→y.
2. **Столкновение (gate):** **`u_P` / `K_P`** + **`α*`** — фазовое давление среды (§5.1.1); только **`exp(iφ)`** (A4, A3).

**Код** — **`ζ=(Σ_{N₄}z)·z*`**, **`Δφ=wrapped Arg(ζ)`** (§3.4.1):

```
z' = z · exp( i · 2π · [ α*/(|z|²+ε) · Δφ − 1 ] · w(ρ) )
```

**Impl:** [`DEVLOG.md` §4](DEVLOG.md#§4-impl-status-по-разделам) · default **`⟨z⟩_N₄` + phase** (§3.6).

| подход | micro изотропия | каузальность | код |
|--------|-----------------|--------------|-----|
| **`⟨z⟩_N₄` + phase** (§3.6) | ✅ **micro≈1.0** | ✅ N₄ | **default** |
| bond-sweeps `local_ca` | ❌ ~1.25 | ✅ N₄ | legacy |

**Следствие для A12:** изотропность **не аксиома M**; либо **isotropic streaming** на M, либо **T-only** Lorentz через coarse + κ (§1.1) — два пути, не смешивать как «уже доказано».

### 3.7 Размазывание → Гейзенберг → триггер `K_P` (канон M)

**Impl backlog:** [`DEVLOG.md` §4](DEVLOG.md#§37-heisenberg--anti-smear) · GPU DoD §3.7.3.

#### 3.7.1 Конфликт двух лагерей (чистая физика поля)

| | **Лучи / LBM / `local_ca`** | **Изотропный котёл `⟨z⟩_N₄`** |
|--|------------------------------|-------------------------------|
| Перенос | направленные степы, импульс по осям | одна реакция на **сумму** соседей |
| Столкновение двух пакетов | лучи проходят/отражаются, сохраняют «личность» | `g` **усредняет** фазы в точке → нелинейная **диффузия** |
| Micro-изотропия | ❌ ~1.25 (bond-sweeps) | ✅ ~1.0 |
| Цена | анизотропия | **решено §3.7.2** (Гейзенберг + `K_P`) |

**Исходная дилемма:** казалось — вихри сливаются в суп (чернила в воде).

#### 3.7.2 Закрытие M (принято)

1. **Триггер = `K_P`** в §3.4 — Планковское кипение при **`ρ_E→0`** (A5).
2. **Гейзенберг:** **`Δφ ≥ 1/2` рад** — геометрический затвор; ниже запрещено.
3. **Anti-smear:** размазанное **не голоморфно** → gate выталкивает полюс (§3.9).
4. **Corpuscular duality восстановлен** на M — **не парадокс**, а readout солитона (§4.9); GPU obstacle/slit ⏳.

#### 3.7.3 GPU-leaf (4070)

§3.4 без правок M. DoD: T1-круг · Gaussian head-on · vortex **`n=±1`** (axis ratio) · hex §3.8 **если** квадрат проступит. См. [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

### 3.8 Квадрат N₄ · hex/D2Q9 (канон M · условный ladder)

**GPU test:** [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

#### 3.8.1 Слабые волны vs тяжёлый vortex на N₄

| режим | N₄ (крест) | ожидание |
|-------|------------|----------|
| слабые волны / фотон | ✅ каузально чист | T1: **круглый** coarse-фронт, micro≈1 |
| плотный vortex **n=±1** | ⚠️ **квадратная** симметрия решётки | macro-профиль может **проступать** — «круг → сглаженный квадрат» |

**GPU метрика:** anisotropy контура vortex — [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

#### 3.8.2 Условный fix-ladder (не сейчас)

**Триггер:** только если тест покажет **деформацию vortex**, не preemptively.

| ступень | что | цена |
|---------|-----|------|
| **0 (default)** | §3.4 isotropic на **N₄** | квадрат может проступать |
| **1** | та же фазовая `g`, **hex** (6 соседей) | другой stencil / код; лучше изотропный Laplacian |
| **2** | **D2Q9** LBM-веса | возврат **population/rays** — конфликт с §3.6 «без лучей»; только если hex не хватило |

**Не смешивать:** hex = сохранить **scalar `z` + isotropic phase**; D2Q9 = другая парадигма M.

#### 3.8.3 → см. **§3.9** (канон ДА; §3.8.3 удалён — дублировал и противоречил M)

### 3.8 bis Квадрат N₄ · hex/D2Q9 (канон M · условный ladder)

**Канон:** §3.9 + §3.4 **`g`** = **слой M**. `mt_ca` **следует**; implementation debt → [`DEVLOG.md` §4.9](DEVLOG.md#§39-da--cr).

#### 3.9.1 Пространство как дискретная голоморфная среда

Решётка **`Λ ⊂ ℤ²`**, **`N₄`**, **`z=u+iv ∈ ℂ`**.

**Вакуум (линейный предел ДА):**

```
Δ_disc z = 0   ⟺   z(x) = (1/4) Σ_{y∈N₄(x)} z(y) = ⟨z⟩_{N₄}
```

**Эволюция M (нелинейный возврат к классу):** отклонение измеряется **`Arg(⟨z⟩/z)`**; **`g`** восстанавливает аналитичность через **`exp(iφ)`** (§3.4) — **не** линейное `z←⟨z⟩`, а **оператор удержания голоморфности** на каждом **`Δt=hT`**.

| ДА | слой M | impl (`mt_ca`) |
|----|--------|----------------|
| `Δ_disc z=0` (идеал) | предел **`Δφ→0`**, **`ρ_E→0`** | boulon + gate |
| CR на рёбрах | **A9** — жёсткая связь `u,v` | **`cr_strength`** + verify A9 |
| риманова поверхность на графе | **`Λ×S¹`**, torus §10.1 | ✅ |
| единственность / глобальная сшивка | **одна** дискретная аналитическая функция Λ | **`holomorphy_sync`** on tick |

#### 3.9.2 Дискретно-аналитическое уравнение эволюции (канон)

Материя = **отклонение от гармоничности**. Оператор **`g`** — удержание класса через **фазовое сопряжение** (§3.4, SI):

```
z(x,t+Δt) = z(x,t) · exp( i · 2π · [ K_P·α* / (ρ_E + K_P) · Δφ_N₄ − 1 ] · w(ρ) )
Δφ_N₄ = Arg( ⟨z⟩_{N₄} / z ) = Arg( (Σ_{N₄} z) · z* )     wrapped ∈ (−π, π]
Δt = hT = t_P/√2
```

**Анатомия в терминах ДА:**

| член | ДА-чтение | §3.4 M | impl |
|------|-----------|--------|------|
| **`Arg(⟨z⟩/z)`** | дефект локальной голоморфности | ✅ | `wrapped_phase_diff` |
| **`K_P·α*/(ρ_E+K_P)`** | метрический предохранитель у полюса | ✅ | `isotropic_phase` |
| **`exp(i·…)`** | unitary возврат в класс (U(1)) | ✅ | A3 |

**Связь с `Δ_disc z=0`:** линейный предел **`φ→0`** ⇔ **`z≈⟨z⟩`**; нелинейный **`g`** — **конечная** коррекция дефекта, не противоречие ДА.

#### 3.9.3 Феномены как теоремы ДА (слой M)

| феномен | формулировка M | impl / T-readout |
|---------|----------------|------------------|
| **Частица `v_p`** | изолированный **полюс**; **`∮ d arg = 2πn`**, **`n∈ℤ`** | ✅ seeds **`n∈{±1,±2}`** · A10 |
| **Заряд** | дискретный **вычет** | T readout |
| **Гейзенберг** | **`Δφ ≥ 1/2` рад**; нарушение → кипение **`K_P`** → самофокусировка | ✅ **`heisenberg_floor`** · A16 |
| **Anti-smear** | размазанное **не голоморфно** → gate выталкивает полюс | ✅ A11 · GPU readout |
| **Нелокальность / запутанность** | **единая** дискретная аналитическая функция Λ: локальный полюс меняет **ГУ всей** функции **на том же `Δt`** — не FTL-сигнал, а **перестройка единого поля** (CR-единственность) | ✅ **`holomorphy_sync_step`** |

#### 3.9.4 Словарь (ДА ↔ M ↔ T)

| ДА | M (`mt_ca`) | T (readout) |
|----|-------------|-------------|
| дискретная голоморфная функция `z` | `z ∈ ℂ²` на `v_p` | `\|Φ\|²` Gaussian macro (§4.1) |
| `Δ_disc z=0` | **идеальный вакуум** (не generic tick) | плоский фон |
| дефект `Arg(⟨z⟩/z)` | `Δφ_N₄` в gate | локальный **∇φ** |
| полюс / log-сингулярность | pra-vortex **`hV`** | электрон-кандидат §4.0.1 |
| вычет **`2πn`** | winding **`n`** | заряд **`Q∝n`** |
| метрика **`K_P`** | `α*/(ρ+ε)` | Планковский барьер |
| риманова поверхность на графе | **`Λ×S¹`** (фаза) | — |

#### 3.9.5 Impl

→ [`DEVLOG.md` §4.9](DEVLOG.md#§39-da--cr)

#### 3.9.6 A9 — порог дисперсионной CR-энергии (не legacy `0.05`)

На **von Neumann `N₄`** forward-diff CR-метрика на **гладкой** плоской волне **не** остаётся ≈0: после burn-in поле выходит на **стационарный** уровень от **дискретной дисперсии** + **планковской вязкости** решётки (§4.1.2), а не «ломается».

| величина | формула | значение |
|----------|---------|----------|
| **`ν_CA`** (natural) | **`¼·c₀·l_P`**, **`c₀=l_P=1`** | **0.25** |
| **`{B_hV}`** | **`B_hV − ⌊B_hV⌋`**, **`B_hV=2π/ln2`** | **≈ 0.0647** |
| **`E_CR,seed`** | **`ν_CA·{B_hV}²`** — до burn-in | **≈ 0.00105** |
| **`E_CR,stab`** | **`ν_CA·(1+{B_hV})`** — потолок после burn-in | **≈ 0.266** |
| **`|ΔE_CR|`** (stationarity) | **`≤ {B_hV}·E_CR`** между двумя поздними окнами | **≈ 6.5%** |

**Verify `A9`:** `PLANE_WAVE` · burn-in **32** + settle **32** · **`e₀ ≤ E_CR,seed`**, **`e₁ ≤ E_CR,stab`**, стационарность.

**Код:** `si_constants.cr_seed_ceiling` · `cr_dispersion_ceiling` · `cauchy_riemann.cr_stationarity_tolerance`.

**Итог §3.9:** ДА = **строгий язык M**. **`g`** = **SU(2) spinor gate** (§3.10); §3.4 — **бозонный предел** `|z|²` invariant.

### 3.10 Спин SU(2): фермионы, знак при 360°, запрет Паули (канон M)

#### 3.10.1 Проблема U(1)-only

| | **U(1) scalar `z∈ℂ` (§3.4 impl)** | **физика T (e, q, …)** |
|--|-------------------------------------|-------------------------|
| оборот **`2π`** | **`z → z`** (identity) | **fermion: `ψ → −ψ`** |
| полный возврат | **`360°`** | **`720°`** (spin **`1/2`**) |
| слепок | бозоны / фотон OK | **нет Паули** → вихри **налезают** → condensate, нет химии/твёрдости |

**Без SU(2)** цифровая жидкость остаётся «плоской» — **corpuscular** на U(1) недостаточно для **matter**.

#### 3.10.2 Состояние ячейки — дискретный спинор

```
z(x,t) = ( z₁(x,t) , z₂(x,t) )ᵀ  ∈  ℂ²
|z|² = z†z = |z₁|² + |z₂|²
```

**ДА-носитель:** не одна фаза на **`S¹`**, а **спинорное** поле на **`ℂ²`**; голоморфность + gate — в **SU(2)**, не только U(1).

#### 3.10.3 Эволюция: от `exp(iφ)` к SU(2) — и **дискретный M-канон**

**Scalar §3.4 (бозонный предел / T-нотация):**

```
z' = z · exp(iΦ)     ,   Φ ∈ ℝ        — U(1)
```

**SU(2) на T / continuous limit (смысл gate, не M-tick):**

```
z'(x) = U(x,t) · z(x)     ,   U ∈ SU(2)
U = exp( i · Θ_a · σ_a / 2 )     — Lie-алгебра; **не** matrix `expm` на M
```

**Канон M (§3.12.5): дискретная «экспонента» на кольце**

```
ω = exp(i · 2π / N_ring)     ,   N_ring = 512 (§3.12.6)
Φ ∈ Z_{N_ring}                 — целые ticks mod N_ring, не float rad

R(Φ) : (U,V) ↦ ( U·c_Φ − V·s_Φ , U·s_Φ + V·c_Φ )   (mod N)
  c_Φ = cos(2π·Φ/N_ring) , s_Φ = sin(2π·Φ/N_ring)   — Rot_LUT Q30, без float trig

δZ(Φ) = R(Φ) − Z     — kick ⌊𝒩⌋ на каждой lane спинора
```

**Эквивалентность слоёв (не смешивать запись и реализацию):**

| режим | discrete M | continuum alias |
|-------|------------|-----------------|
| **canonical tick `g`** | один **`Φ`** → **`R(Φ)`** на **`z₁`** и **`z₂`** lanes | **`exp(i·Φ·σ_z/2)`** как общая фаза (подгруппа **`U(1)_vac`**) |
| **SU(2) mix** | **`U(n,φ)`** — half-angle LUT + Pauli (`su2_apply`) | **`exp(i·φ·n·σ/2)`** — holomorphy sync, chiral; **не** один leapfrog-tick |
| **spin-½ sign** | **`R(N_ring/2)`** → **`−z`** на компоненте; **`U(n,4π)=+1`** | verify **`SU2_360`**, **`SU2_720`** |

**Правило:** на M **`g`** = **`ω^Φ`** / **`Rot_LUT`**, не **`scipy.linalg.expm`**. Float **`su2_apply`** — probe/readout; единственный forward-tick — **`projected_step_fixed`** (§3.12.3).

**Следствие spin-½ (T-смысл, проверяется probe):**

```
U(2π) = −𝟙     (один оборот вихря hV → знак минус)
U(4π) = +𝟙     (два оборота → возврат)
```

Топологический pra-vortex **`hV`**: при обходе **сопряжённых** соседей — **«половинчатый»** оборот в **спинорном** пространстве (не только **`Δ arg = π`** на U(1)).

#### 3.10.4 Запрет Паули и `K_P`-отталкивание

**Правило M:** два вихря с **одинаковой спинорной ориентацией** **не могут** занять **один** **`v_p`**:

```
z_A ∥ z_B  ⇒  z_A + z_B → 0   (аннигиляция / выталкивание)
```

**`K_P`** + gate → **бесконечное отталкивание** на сверхмалых **`d ≤ l_P`**: упругость жидкости = **Pauli pressure** на решётке.

**Цепочка масштабов замкнута:** **`hV`** (топология) + **SU(2)** (спин) + **Паули** (matter) + **`K_P`** (упругость).

#### 3.10.5 Impl

→ [`DEVLOG.md` §4.10](DEVLOG.md#§310-su2--pauli)

### 3.11 U(1)_vac gauge + хиральная упаковка SU(2)_L × SU(2)_R (канон M)

#### 3.11.1 U(1)_vac на спиноре

```
z → z · e^{iθ}     (одна global phase на обе компоненты)
```

**Инварианты gate (до SU(2) mix):**

| объект | формула | U(1) |
|--------|---------|------|
| holonomy | **`ζ = (Σ z)·z*`** | ✅ |
| Bloch | **`n = z†σ z / |z|²`** | ✅ |
| axis | **`n_z × n_s`**, fallback **`n_z`** | ✅ (не фикс. **`(0,0,1)`**) |
| Arg carrier | **`Δφ = wrap(Arg ζ)`** | ✅ |

**`defect_axis`:** local Bloch fallback when **`‖n_z×n_s‖→0`**.

#### 3.11.2 Хиральность без второго поля

```
P_L z = (z₁, 0)ᵀ     P_R z = (0, z₂)ᵀ     z = P_L z + P_R z
```

| фаза tick | M-смысл | impl |
|-----------|---------|------|
| **STREAM** | **`P_L`**, **`P_R`** независимо на **`N₄`** | component-wise **`linear_step`** / neighbor sum |
| **COLLISION** | **`K_P` + Arg** смешивают компоненты | **`su2_apply` + holomorphy sync** |

**`|z_L|² − |z_R|²`** — U(1)-инвариантный chirality readout; **не** закон «+1 preferred» (§9.2 bubble).

#### 3.11.3 P, C, T и optional CPT (T-layer — не M `g⁻¹`)

**Канон M (2026-09-22):** обратимость шага — **A13 / §3.12** (`g⁻¹` = leapfrog algebra + kick ledger). **Не** через CPT-product и **не** через `z→z*` alone.

На **T-слое** / continuous limit — anti-unitary партнёр **`Θ = P ∘ C ∘ T`** (диагностика, не DoD sim):

```
P : (x,y) ↦ (−x,−y)           — инверсия пространственных осей (torus flip)
C : z ↦ z*                    — комплексное сопряжение компонент спинора
T : z ↦ U_boost z             — смена знака киральности, U = exp(i·π·σ_x/2) = i·σ_x
```

**Смысл T:** flip **`|z_L|² − |z_R|²`** (SU(2)-буст по **`σ_x`**), не заряд **`n`** alone и не голая conjugation.

**Не путать с A13:**

```
M reverse:  z(t−1) = 2z(t) + ⌊𝒩⌋ − z(t+1)     — verify Leapfrog (§3.12)
T probe:    g⁻ᴺ(z) ≈ Θ · gᴺ( Θ(z) )            — optional; scripts/run_symmetry_probe.py
```

Product **`Θ`** на projected **`Z_N[i]` g** после gauge-fix encode **не** замыкается bit-exact — **не** sim-gap, **не** refutation M.

| leg | seed probe | M verify |
|-----|------------|----------|
| **P** | **`mirror`**: **`n→−n`** | parity seeds + 1-step **`g·P`** |
| **C** | **`z*``**: **`n→−n`** | charge seed |
| **T** | **`σ_x` boost**: **χ→−χ** | chirality flip proxy |
| **CPT** | product **`Θ`** | optional diagnostic only |

**Open (long-run P·g):** [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

#### 3.11.4 A14 на решётке (impl probes)

| симметрия | seed / 1-step | long-run |
|-----------|---------------|----------|
| **P** | **`mirror_x`**: **`n→−n`** ✅ | **`g·P vs P·g`**: winding drift ⚠️ |
| **C** | **`z→z*`**: **`n→−n`** ✅ | partner evolution — T readout |
| **T** | **`i·σ_x`**: **χ→−χ** ✅ | not **`z*`** alone |
| **`U(1)_vac`** | **`g(z e^{iθ})≈g(z)e^{iθ}`** ✅ | verify **`U1_vac`** |
| **CPT product** | optional **`Θ g Θ`** diagnostic | not M verify DoD |

**Не путать:** отсутствие long-run **`P·g`** commute ≠ «Вселенная right-handed» — это **contingent seed + chirality dance**, не аксиома M (§9.2).

#### 3.11.5 Impl

→ [`DEVLOG.md` §4.11](DEVLOG.md#§311-symmetries)

### 3.12 Эволюция M: обратимый КА 2-го порядка на планковском базисе (канон)

#### 3.12.0 Из первых принципов (Planck natural units)

В **истинном M-базисе** **`hL=l_P=1`**, **`hT=t_P/√2=1`**, **`m_P=1`**, **`ρ_P=1`** (§1, §7):

| continuum / GPU habit | M |
|----------------------|---|
| `z ∈ ℂ` float32, mantissa ∞ | **один `v_p` = конечный информационный бюджет** (Bekenstein) |
| `z_{next}=f(z)` + round каждый такт | **дискретный спинорный инвариант**; фаза — **целые сдвиги**, кратные **`Δφ≥½`** (§3.7) |
| обратимость «если биекция» | **2-й порядок обязателен**: 1-й порядок + **`𝒩`** + округление → необратимый хаос |
| T⁻¹ через CPT / `z*` | **T⁻¹ на M**: **`z(t−1)` из algebra**; память **`(z_t, z_{t−1})`** симметрична |

**Вывод:** Вселенная на M — **строго обратимый целочисленный (fixed-point) КА 2-го порядка** на **`Z_N[i]`**. Float32 — **только decode/readout** для T; **не** альтернативный `g`.

#### 3.12.1 Закон `g` (Fredkin / leapfrog) — модульное пространство Z_N[i]

**Состояние ячейки** — не пара «произвольных регистров», а элемент **фактор-кольца** по модулю **`N = 2^{32}`** (или `[2^{64}]`):

\[
Z(x,t) \in \mathbb{Z}_{N}[i]
\]

Все сложение / вычитание / умножение **неявно mod N**; на GPU (4070, `uint32_t`) — **аппаратное переполнение**, без побитовых масок.

**Алгебраическое уравнение 2-го порядка** (одна строка):

\[
Z(x,t+\Delta t) + Z(x,t-\Delta t) = 2Z(x,t) + \lfloor \mathcal{N} \rfloor
\]

Эквивалентно **`Z⁺ = 2Z + ⌊𝒩⌋ − Z⁻`**. **`⌊𝒩⌋`** — projected collision (§3.12.5); округление **только** в **`𝒩`**.

**Коэффициенты `2` и `−1`** — не подгонка: топология **каузального конуса** von Neumann на **`N₄`** (discrete d'Alembert / leapfrog).

**Память:** **`(Z_t, Z_{t−1})`** — среда **помнит** такт **`t−1`** наравне с **`t`**. T-reverse: **`Z_{t−1} = 2Z_t + ⌊𝒩(Z_t)⌋ − Z_{t+1}`** (mod N).

#### 3.12.2 T⁻¹ и CPT

```
z(t−1) = 2z(t) + ⌊𝒩(z(t))⌋ − z(t+1)        — exact на ℤ / fixed lattice
```

**CPT (§3.11.3):** anti-unitary партнёр на **T-слое** / continuous limit. **Leapfrog:** дискретный **`g⁻¹`** без **`z*`**. Оба согласованы; **A13 закрывает leapfrog**, не float 1-st order.

#### 3.12.3 Projected collision + kick ledger (impl)

| направление | правило |
|-------------|---------|
| **forward** | **`projected_step_fixed`**: §3.12.5 ζ → Φ_sat → Rot_LUT → **`Z⁺+Z⁻=2Z+⌊𝒩⌋`** в **`Z_N[i]`** |
| **reverse** | **`Z⁻ = 2Z + ⌊𝒩⌋ − Z⁺`**; ledger хранит **`⌊𝒩⌋`** (bit-exact; можно пересчитать из **`Z_t`**) |

**Единственный forward:** **`projected_step_fixed`** — нет float-пути, нет `first_order`.

#### 3.12.4 Impl (`mt_ca`)

| слой | модуль |
|------|--------|
| **`Z_N[i]`** ring ops | **`z_ring.py`** · **`N_ring = 512 = 2^9`**, mod from §3.12.6 |
| Planck **`⌊·⌋`** encode | **`fixed_point.py`** · **`frac_bits=6`**, **`mod_bits=9`** |
| **projected 𝒩** | **`projected_collision.py`** · default **`use_projected_collision=True`** |
| **`g` 2-го порядка** | **`reversible.evolve_canonical`** · **`projected_collision.py`** |
| `(Z, Z_past)` + ledger | **`simulator.py`** |
| decode/readout | **`fixed_point.decode_spinor`** — T/UI, не tick |

#### 3.12.5 Projected collision — аксиомы ДА + Bekenstein (mod Z_N[i])

**Модульное пространство:** \(Z = U + iV \in \mathbb{Z}_{N}[i]\), **`N_ring = 2^{⌊B_{hV}⌋} = 512`**, **`2\pi_{\mathrm{disc}} = N_ring`**. Wrap — **`mod N_ring`**, не произвол GPU.

**Дискретная экспонента (§3.10.3):**

\[
\omega = e^{i\cdot 2\pi/N_{\mathrm{ring}}},\qquad
R(\Phi) = \omega^{\Phi}\ \text{on each }(U,V)\ \text{lane via Rot\_LUT}
\]

**Шаг 1 — holonomy (без `atan2`):**

\[
\zeta_{\mathrm{real}} = \left\lfloor \frac{\langle U\rangle U + \langle V\rangle V}{2^{F}} \right\rfloor,\quad
\zeta_{\mathrm{imag}} = \left\lfloor \frac{\langle V\rangle U - \langle U\rangle V}{2^{F}} \right\rfloor
\]

**Шаг 2 — saturating kick (Bekenstein в знаменателе, не post-project):**

\[
\Phi_{\mathrm{kick}} = \left\lfloor \frac{K_P\,\zeta_{\mathrm{imag}}}{\zeta_{\mathrm{real}} + |Z|^2 + K_P} \right\rfloor \in \mathbb{Z}_{N}
\]

+ Heisenberg floor \(|\Phi|\ge \Delta\Phi_{\min}^{\mathrm{disc}}\). **`⌊𝒩⌋`** = **`δZ(Φ) = R(Φ)−Z`** on both spinor components (same **`Φ`** → **`U(1)_vac`** on tick; full **`U(n,φ)`** — §3.10.3 probe path).

**`Rot_LUT`:** 4096-entry cos/sin Q30; **без float trig** on GPU hot path. Verify **`DiscreteRotExp`** — **`R(Φ)=ω^Φ`**, not matrix **`exp(i·Θ·σ/2)`**.

**Шаг 3 — 2-й порядок (symmetric, one line):**

\[
Z(x,t+\Delta t) + Z(x,t-\Delta t) = 2Z(x,t) + \lfloor \mathcal{N} \rfloor \pmod N
\]

#### 3.12.6 Bit budget of `hV` — вывод из Planck (дна ниже нет)

**Элементарный кирпич** — одна ячейка **`hV = l_P³`**. Её информационная ёмкость **не knob** и **не ширина CUDA-регистра**:

\[
B_{hV} = \frac{2\pi E_P l_P}{\hbar c \ln 2}
       = \frac{2\pi}{\ln 2}
       \approx 9{,}0647 \text{ bit}
\]

(последний шаг: **`E_P l_P = \hbar c`** из Planck-определений.)

**Heisenberg floor** (§5.0.2, **`s₀ = ℏ/2`**) задаёт минимальную фазовую сетку:

\[
N_\phi = \left\lceil \frac{2\pi}{\Delta\phi_{\min}} \right\rceil
       = \lceil 4\pi \rceil = 13
\]

**Разбиение бюджета** на spinor **`z ∈ ℂ²`**:

| величина | формула | значение |
|----------|---------|----------|
| **`B_{hV}`** | **`2π/ln 2`** | **≈ 9.065 bit** |
| **`N_ring`** | **`2^{⌊B_{hV}⌋}`** | **512** |
| **`mod_bits`** | **`⌊B_{hV}⌋`** | **9** |
| **`N_φ`** | **`⌈2π/Δφ_min⌉ = ⌈4π⌉`** | **13** |
| **`Δφ_min`** | **`s₀/ℏ = ℏ/2·(1/ℏ) = ½` rad** | **28.65°** — физический пинок |
| **`B_amp`** | **`⌈log₂(N_ring/N_φ)⌉`** | **6** → **`frac_bits = 6`** |
| **`Δφ_disc`** | **`round(N_ring·Δφ_min/2π)`** | **41 ticks mod 512** |
| **`𝒩_states`** | **`2^{B_{hV}}`** | **≈ 520** |

**Не путать:** **`frac_bits`** — не «удобная двойка для GPU», а **`⌈log₂(512/13)⌉`**: сколько амплитудных уровней помещается в кирпич при **13** фазовых делениях на **`N_ring`**. **`Δφ_min = 1/32`** — отвергнуто как инженерный костыль.

**`α_fs⁻¹ = 4π³ + π² + π ≈ 137`** (§8.2) — **не** ширина кольца: это **число каналов фазового сопряжения** на T-readout; verify **`Compton_e`**, **`VdW_algebra`**, gate row — PASS.

**Impl vs M:**

| слой | смысл |
|------|--------|
| **`N_ring = 512`** | **M-физика** — modular bounce видим |
| **`int32` storage** | транспорт; редукция **`mod 512`** на каждом tick |
| **`W_sim ≫ B_{hV}`** (legacy 32-bit lane) | **запрещён** как default — padding ≠ аксиома |

**Код:** `si_constants.HV` · `hv_bit_budget()` · verify **`HvBitBudget`** · `MConfig.mod_bits/frac_bits/phase_bits` defaults.

## §4. M → T (macro; без предела hL→0)

### 4.0 Зачем переход **принципиален** (не технический coarse)

**`dl = l_P` и `dV = (dl)³` известны** как определения из `ℏ, G, c` (§7) — но **засечь их прибором нельзя**: слишком маленький размер и объём. Это не «ещё не построили микроскоп» — **принципиальный** предел доступа.

**Красивый выход:** механика **как предельный переход `c → ∞`**, только **наоборот** — и **без** continuum на M (§1.4):

| Классика | Наша модель |
|----------|-------------|
| micro relativistic, `c` finite | **M** — дискретный, `c₀ = dl/hT` fixed |
| предел `c → ∞` / **`hL → 0`** | **нет** — continuum-гипотеза **отвергнута** на M |
| остаётся **macro** (Newton) | остаётся **T** — наблюдаемый мир (может *выглядеть* гладким) |
| transition: убрать релativistic corrections | transition: **M → T** (усреднение + дискретный анализ) |

Макромир **остаётся** единственным слоем, к которому есть доступ. Значит, из M **обязан** следовать T — не опция, а **структура познания и модели**.

```
M   — КА на решётке (никогда не «увидим» напрямую)
      ↓  Gaussian macro readout (§4.1), DFT на T-слое
T   — волны, c, mass, SM-наблюдаемые («наш мир»)
```

**Проверка модели** — только на **T** (T1/T2, dispersion, SM). **M** верифицируется косвенно: если T-инварианты сходятся, M-каркас не refuted.

### 4.0.1 M→T: якорь — **известный T** (электрон), не новый огород

**`mt_ca` = модель M.** Одна штука. Отдельного «toy» нет — была моя путаница.

**Смысл:** не «сначала Theory of Everything». Берёшь **T-объект с уже известными параметрами** — электрон остаётся электроном, `m_e`, `e` из experiment — и спрашиваешь: **из чего он на M?**

**Сдвиг:** кварки — **не** фундаментальный предел. Предел — **`v_p = dV = (dl)³`**.

```
частица(ы) в v_p     — pra-дефект, один dV (§5)
      ↓  комбинации
кварки, пре-резонансы
      ↓
электрон, протон, …   ← T: уже validated снаружи
```

**Проверка M→T** = комбинации `v_p` + `g` **дают** известный электрон (или **любой** другой точный SM-объект) **без** новой подгонки. Не сходится → refute.

Coarse / T1 / dispersion — **механизм** слоя T. **Электрон** — **конкретный** якорь «мы смотрели не туда: не кварк, а `v_p`».

### 4.1 Оператор макро-усреднения (ядро фильтрации)

**Не** простое арифметическое среднее по блокам: **резкие границы** macro-блоков порождают **ложные отражения** волн на T-слое.

Макроскопическое поле **Φ(X,t)** в макро-точке **X** — **свёртка** дискретного поля **z(x,t)** с **целочисленным биномиальным** фильтром **(1-2-1)⊗(1-2-1)/16** на окрестности **Ω(X)** радиуса **R ≫ l_P**:

```
Φ(X,t) = (1-2-1)^{⊗2·R} z(x,t)     — separable von Neumann isotropic readout
```

Эквивалентно одному проходу **3×3** с весами **`[1,2,1; 2,4,2; 1,2,1]/16`**. **Σ W = 1**. Идеальная **изотропия на N₄** — без float **`exp(−r²/σ²)`**.

**Impl:** `mt_ca/macro.py` — `binomial121_smooth`, `macro_average_spinor`, `macro_amplitude`; `t_validation.coarse_grain(block)` → **R≈block** passes, stride **= block**.

**Legacy v0** (block-mean, debug only):

```
Φ_B = (1/|B|) Σ_{x∈B} z(x)     readout: |Φ_B|
```

#### 4.1.1 Алгебраический вывод макро-уравнения (NLSE / Ginzburg–Landau)

Вне плотных вихрей (**ρ_E ≪ K_P**) нелинейный замок в gate раскладывается по Тейлору:

```
exp(i·2π·[K_P·(1+1/4π)/(ρ_E+K_P)·Θ_flow − 1])
  ≈ 1 + i·2π·Θ_flow − i·2π·ρ_E/K_P + …
```

Оператор **Σ(…·W)** к обеим частям; дискретные фазовые разности **Θ_flow** по **N₄** → макро-**∇²**; шаг **Δt = hT**:

```
Φ(X,t+Δt) − Φ(X,t) ≈ l_P² · ∇²Φ(X,t) − i·Δt · (ρ_P c²/K_P) · |Φ|² Φ(X,t)
```

Делим на **Δt**. **c₀ = l_P/Δt = √2·c**, **K_P = ρ_P c²**:

```
∂Φ/∂t = i · (c₀² Δt / 2) · ∇²Φ  −  i · ω_macro · |Φ|² Φ
```

Подстановка **c₀ = √2·c** схлопывает коэффициент диффузии в **ℏ/(2m)** (размерный квант T-слоя). Получаем **нелинейное уравнение Шрёдингера** / **Ginzburg–Landau** — сверхтекучая квантовая жидкость на **T**, **не** новый M-оператор.

#### 4.1.2 Эффективная макро-вязкость (T не идеален; M — да)

**Парадокс:** на **M** жидкость **сверхтекуча** и **бездиссипативна** (A3: **`Σ|z|²`** invariant, **`g⁻¹`** существует). На **T** волны **затухают**, есть **трение**, **излучение**, **BH-квазитепло** — макромир **неидеален**.

**Разрешение (Chapman–Enskog над ДА):** диссипация — **не** новый член в **`g`**, а **остаток coarse-graining** при конечных **`Δx = l_P`**, **`Δt = hT`**.

**1 · Численная (сдвиговая) вязкость решётки**

При **(1-2-1)**-усреднении §4.1 в макро-уравнение **алгебраически** вылезает остаток — **numerical viscosity** LBM/CA:

- на **M** фазы **не теряются** — уходят в **микро-Гейзенберговскую пену** (A5);
- на **T** это выглядит как **вязкое затухание** — перекачка macro-кинетики в **micro-«тепло»** вакуума; вакуум = **идеальный термостат**.

**2 · Квантовая турбулентность (вихревая вязкость)**

A5 ⇒ перманентное **micro-кипение**. Столкновение **`hV`**: часть фазовой энергии → **каскад микро-вихрей** (не только elastic bounce). Macro-имя: **турбулентная вязкость** — radiative damping, friction in fields.

**3 · Макро-уравнение с диссипацией (T-слой, не M)**

К §4.1.1 добавляется **вещественный** член (**не** нарушает A3 на M):

```
∂Φ/∂t = i·(ℏ/2m)·∇²Φ − i·ω_macro·|Φ|²Φ  +  ν_CA·∇²Φ
```

**`ν_CA = ¼ · c₀ · l_P`**

Множитель **`¼`** — число **ортогональных** направлений переноса в **N₄**-окрестности (von Neumann causal cross, §2).

**SI (из `ℏ, G, c`, без подгонки):**

| | |
|--|--|
| **`c₀ = √2·c`** | ≈ **4.23970560×10⁸** m/s |
| **`l_P`** | ≈ **1.616255×10⁻³⁵** m |
| **`ν_CA`** | ≈ **1.713111×10⁻²⁷** m²/s |

**Natural units** (`c₀ = l_P/hT = 1`, `l_P = 1`): **`ν_CA = ¼`**.

**Следствие:** macro-неидеальность = **доказательство дискретности**. Continuum-вакуум **без** **`l_P`** не имел бы **`ν_CA`**.

#### 4.1.3 Физический смысл (почему T «почти идеален»)

**Почему макромир кажется идеальным?**

**`ν_CA ~ 10⁻²⁷` m²/s** — **чудовищно мало**. Для сравнения:

| среда | кинематическая **`ν`** [m²/s] |
|-------|-------------------------------|
| **вакуум M→T** | **~1.7×10⁻²⁷** |
| жидкий гелий | ~10⁻⁸ |
| вода | ~10⁻⁶ |

Квантовая жидкость вакуума **на многие порядки текучее** любой лабораторной сверхтекучести. Отсюда: **фотоны** от далёких галактик летят **миллиарды св. лет** почти **без затухания** — трение о **`l_P`-пиксели** проявляется только на **космологических** масштабах (§9 long-run sim).

**Предел диссипации (тепловой порог вакуума)**

**`ν_CA`** задаёт **точную скорость**, с которой **macro-фазовая информация** уходит в **Гейзенберговский шум** квантовой пены (A5, §3.7). Micro-**дренаж** macro-энергии — **не** нарушение A3 на M, а **readout T**.

**Стрела времени на T:** необратимость термодинамики **не** из «сломанного **`g`**», а из **одностороннего coarse-graining** — из macro в micro-«тепло» вакуума **нельзя** собрать когерентный кадр обратно (A6 локально; A13 micro обратим, T — нет). Связь: §2.1 (хаос gate), META §2 (мозг readout'ит **не** каждый **`hT`**).

**Impl / sim:**

| слой | что делаем |
|------|------------|
| **M** **`g`** | **без** **`ν∇²z`** — A3 sacred |
| **T** readout | Gaussian **`Φ`**: пакет **затухает** на GPU (`T3_macro_viscosity`) |
| **T** fit | оценка **`ν_eff`** из decay **`|Φ|²`** vs **`ν_CA = ¼·c₀·l_P`** — open T6 |

**GPU DoD:** волна **без компенсации** → плавное macro-затухание в фоновое кипение; **micro norm** stable (A3).

**Impl / T-fit:** [`DEVLOG.md` §4](DEVLOG.md#§4-t-layer) · **`ν_eff`** open.

### 4.9 Волна ↔ «частица»: демистификация корпускулярно-волнового дуализма

**Классический парадокс** («то шарик, то волна») — **не свойство M**, а **иллюзия T-масштаба** наблюдения.

#### 4.9.1 M: монолитная жидкость, без «шариков»

На **`hV`-пикселях** нет изолированных корпускул, летящих сквозь пустоту. **100%** — изотропная комплексная **фазовая жидкость**: давление, вихри, **`Arg`-токи** (§5, §5.0.1). «Пустота» = micro-кипение вакуума (A5), не absence of field.

#### 4.9.2 «Частица» = устойчивый солитон (topological vortex)

Macro-«твердое тело» = **нелинейный волновой узел** в **`hV`**:

1. фазовое давление **закручивается** в vortex (§9.7 seeds);
2. **`Δφ ≥ ½`** (Heisenberg, §3.7.2) + **`K_P`** **блокирует smear** → anti-smear выталкивает полюс (§3.9);
3. узел **локализован**, как капля с **поверхностным натяжением** **`K_P`**.

Прибор «тыкает» в **`K_P`-упругость** целого узла → кажется **точечной корпускулой**.

#### 4.9.3 Двухщелевой эксперимент (Young) без мистики

| фаза | M | T (readout) |
|------|---|-------------|
| полёт | **волновой хвост** vortex/пакета через обе щели | интерференция **гидродинамики** |
| детекция | плотность в пикселе → **CR-самофокус** + **`ρ→ρ_P`** clamp (A7) | энергия **схлопывается** в одну macro-точку |
| вывод | **один солитон**, два масштаба смотрения | «волна в полёте, точка при ударе» |

**Не суперпозиция миров** — **один** нелинейный объект: размазан в T при propagation, **локализован** при interaction threshold.

**GPU Young leaf:** [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

### 4.2 Самопроверка M→T (без внешних эталонов)

Валидация — **чистая математическая симметрия** на константной сетке. Readout — **Gaussian §4.1**, не block-mean.

#### T1 · «Угловatость» (Lorentz)

**Micro:** isotropic streaming (§3.6) → **micro_anisotropy ≈ 1**. **Macro:** Gaussian readout + **`κ=1/√2`** → круг.

| Метрика | PASS |
|---------|------|
| `isotropy_ratio` → 1 | macro < 1.2 и macro ≤ micro |

#### T2 · Солитон и интерференция

Macro-пакет живёт на вакууме; два пакета → интерференция, не mush. Gaussian head-on — **`T2_gaussian`**.

**Validate:** `python validate_mt.py` — см. [`DEVLOG.md` §6](DEVLOG.md#§6-команды-verify).

### 4.3 Дискретный анализ (не «непрерывность из коробки»)

```
ẑ(k) = Σ_x z(x) · e^{-ik·x}          (дискретный гармонический анализ)
|ẑ(k,t)|²  — спектр блока или поля
ω(k)      — из фазового сдвига мод (калибровка γ)
```

Код: `mt_ca/t_analysis.py`. DFT **читает** macro-структуру и **калибрует** local CA; **не заменяет** тик `g`.

«Непрерывность» — **эмерджентное описание** T, если усреднение + спектр это поддержат.

### 4.4 Персональные кванты системы (fractal M → T)

**Канон:** §4.8. Измерил **`M`, `R`, `τ_frame`** macro-системы → **индивидуальные** **`Δx`, `Δt`, `N_frame`, `I_max`** вычисляются **без knobs**.

GR **`hT_person ~ hT · (dt_coord/dτ)`** (§0) — **дополнительный** redshift-слой поверх Compton-масштаба; не путать с **`Δt_sys`** ниже.

### 4.5 T-эффекты из манифеста (§6)

| Манифест | Смысл | Статус |
|----------|-------|--------|
| Гладкость, Lorentz | macro over many `l_P` | ✅ Gaussian readout · T1 |
| Масса = зигзаг vortex | `𝒩` в ε-окр. | §5; sim TODO |
| Голография / запутанность | дискретная аналитичность | ✅ A9 + holomorphy sync |

### 4.6 Критический эксперимент (манифест §7)

**DoD:** macro readout → гладкая волна, импульс, `c=1`.  
**Сейчас:** M stable (`verify_principles`); T — **Gaussian macro** + **`T_dispersion`** PASS.

### 4.7 Проверки T

| test | статус |
|------|--------|
| dispersion ω(k) | ✅ **`T_dispersion`** (phase advance) |
| impulse front на macro map | ✅ T1 |
| vortex stability | ✅ A11 · **`Vortex_hex`** |
| Gaussian collision §3.7.4 | ✅ **`T2_gaussian`** |
| NLSE coefficient **`ω_macro`** fit | open T6 |
| macro **`ν_CA`** damping on Gaussian **`Φ`** | ✅ **`T3_macro_viscosity`** |

### 4.8 Системные кванты (fractal M → T, **physics**)

**Канон M:** из **`(M, ℏ, c, κ)`** для любой когерентной macro-системы — **индивидуальные** **`Δx_sys`, `Δt_sys`, `N_frame`, `I_max`** **без knobs** в `g`.

**Fractal-закон:**

```
Δx_sys = ℏ / (M · c)                    — **λ̄_C(M)**: reduced Compton «пиксель» тела
λ_C    = h / (M · c) = 2π · Δx_sys       — полная комптоновская длина волны
Δλ(θ)  = λ_C · (1 − cos θ)               — комптоновский сдвиг на масштабе M
Δt_sys = Δx_sys / c₀ = ℏ / (M · c₀)     ,   c₀ = √2 · c  (§1.1)
N_frame = τ_frame / Δt_sys              — тактов за один UI-кадр (τ — **вход** T-биологии)
I_max   = 2π · M c² · R / (ℏ ln 2)      — Bekenstein [bit/кадр]
Δx_sys / l_P  =  Δt_sys / hT  =  m_P / M
```

Чем **легче** система, тем **ближе** личные кванты к **`l_P`, `hT`**.

**Примеры (`system_quanta`, CODATA):**

| система | **M** | **τ_frame** | **Δx_sys** | **Δt_sys** | **N_frame** | **Δx/l_P** |
|---------|-------|-------------|------------|------------|-------------|------------|
| мозг | 1.4 kg | 15 ms | 2.51×10⁻⁴³ m | 5.93×10⁻⁵² s | **2.53×10⁴⁹** | ~1.6×10⁻⁸ |
| человек | 70 kg | 15 ms | **5.03×10⁻⁴⁵ m** | **1.19×10⁻⁵³ s** | **1.27×10⁵¹** | ~3.1×10⁻¹⁰ |
| **дрозофила** | **1 mg** | **4 ms** | **3.52×10⁻³⁷ m** | **8.30×10⁻⁴⁶ s** | **4.82×10⁴²** | **~0.022** |

**Impl:** `mt_ca.si_constants.system_quanta(M_kg, tau_frame_s, *, R_m=None)` → `SystemQuanta`;  
`reduced_compton_wavelength`, `compton_wavelength`, `compton_scattering_shift`.

**Meta (мозг как macro-узел, кадр ~15 ms, UI, «наблюдаемая Вселенная»):** → **[`META.md` §2](META.md#§2-наблюдатель-и-ui-t-над-t)** — **не SSOT**; не добавляет параметров в `g`.


## §5. Элементарный объём dV и pra-дефект (M)

### 5.0 Бинарная плотность M (**`ρ_matter ∈ {0, ρ_P}`**)

**Инверсный переворот:** и **`hV = l_P³`**, и **`hm = m_P`** квантованы жёстко ⇒ **истинная плотность материи** на M **не непрерывна**:

```
ρ_P = m_P / l_P³ = μ_P  ≈  5.155×10⁹⁶ kg/m³
```

**Не путать две плотности** (A7 vs §5.0):

| символ | смысл | диапазон на M |
|--------|--------|----------------|
| **`ρ_field = ρ_E = μ_P·|z|²`** | энергия **фазового поля** (A7, gate) | **`0…ρ_P`** (потолок A7) |
| **`ρ_matter`** | **масса pra-дефекта** в `hV` | **`{0, ρ_P}`** только |

**A5:** вакуум **`ρ_matter = 0`**, но **`ρ_field > 0`** — кипящий фон; **`z ≡ 0`** не устойчив. **`|z|²`** — не «занятость ячейки».

**Занятость `b(x)` — следствие, не knob:**

```
ρ_matter(x) = ρ_P · b(x)          ,   b(x) ∈ {0, 1}
b(x) = ρ_matter(x) / ρ_P = m_cell(x) / m_P
```

**§8.1:** элементарный pra-дефект в **одном** `v_p` ⇒ **`m_cell ∈ {0, m_P}`** ⇒ **`b` уже в §5.0**, не новая аксиома.

**Как выразить `b` через поле `z` (стандартные величины M):**

| канал | формула | § |
|-------|---------|---|
| **масса** | **`b(x) = m_cell(x) / m_P`** | §8.1 |
| **топология (A10)** | **`b(x) = min(1, |n_∂(x)|)`** — **`n_∂`** = дискретный winding на контуре **`∂(hV)`** вокруг **`x`** (**`∮ d arg(z₂/z₁) = 2πn`**) | §3.9 · A10 |
| **Arg-дефект (A11)** | pole locked ⇔ **`|Δφ_N₄(x)| ≥ Δφ_min`** + anti-smear **`K_P`**; **`b=1`** на **ядре** pra-vortex, не на размазанном хвосте | §5.0.1 · §3.7 |

Три строки **эквивалентны** на уровне 0 (один **`m_P`** в одном **`v_p`**). **`n_∂`** и **`|Δφ|`** — **readout из `z`**, не отдельные поля.

На **M** у ячейки **`hV`** только **два** класса **`ρ_matter`**:

| **`b`** | **`hV`** | **`ρ_matter`** |
|---------|----------|----------------|
| **0** | нет pra-vortex ( **`n_∂ = 0`** ) | **0** |
| **1** | ядро pra-**`v_p`** ( **`|n_∂| ≥ 1`**, **`m_cell = m_P`** ) | **`ρ_P`** |

**Macro-иллюзия T:** «вода **1000 kg/m³**», «воздух **1.2 kg/m³**» — **не** микро-плотности ячеек. Это **Gaussian coarse-grain** (§4.1): в macro-объёме **~1 из 10⁹³** ячеек занято pra-вихрем (**`ρ_P`**), остальные — вакуум; прибор **усредняет** → «разрежённая» macro-плотность. **Материя не размазана** — **дискретно распределена** по пикселям.

**Следствия:**

| феномен | M | T (иллюзия) |
|---------|---|-------------|
| «плотность материала» | доля занятых **`hV`** | **`kg/m³`** continuous |
| предел сжатия | **`ρ → ρ_P`** монолит | VdW **`b`**, §5.3.3 |
| «сингулярность» BH | **нет `ρ > ρ_P`:** монолит — все пиксели = 1 | continuum **`ρ → ∞`** |

**Verify:** **`Rho_P_binary`** · occupancy readout — [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

#### 5.0.1 Истинный переносчик массы: квант **`Arg`-давления** (не Higgs)

**SM-мacro:** бозон Хиггса = «податель массы» — частицы **вязнут** в непрерывном scalar-поле.

**M (ДА):** масса **не** рождается полем Хиггса. Macro-масса = **эффективное замедление** — частота **внутренних отражений зигзага** pra-**`hV`** в **`K_P`**-жидкости (§5, §3.7). Плотность квантуется **`ρ ∈ {0, ρ_P}`** (§5.0) — не continuous «вязкость в Higgs».

**Истинный M-переносчик инерции** — не частица с объёмом, а **квант фазового натяжения** на каждом **`hV`**:

```
Δφ_N₄ = Arg( ⟨z⟩_{N₄} / z )     wrapped ∈ (−π, π]
```

Тот же член, что в gate §3.9.2 · impl: **`holonomy_zeta`**, **`wrapped_phase_diff`**.

**Механизм зигзага:**

1. безмассовый **каузальный** перенос по **N₄** (`c₀` по осям);
2. натыкается на **`K_P`** + нелинейный gate → **разворот** фазы в ε-окрестности **`v_p`**;
3. **`Arg`-поток** **передаёт** фазовый сдвиг по соседям и **запирает** энергию в ячейке → macro-readout **`m_rest`**.

**Хиггс на T:** не первопричина, а **вторичная macro-структура** — **пена** на **`Arg`-океане**: крупная неустойчивая **волна плотности** из **~10⁴⁰** планковских **`Arg`-квантов**. LHC **125 GeV** ≈ раскачка жидкости macro-энергией → видим **флуктуацию T**, не micro-механизм.

| слой | «кто даёт массу» |
|------|------------------|
| **M** | **`Arg(⟨z⟩/z)`** + **`K_P`** + зигзаг в **`hV`** |
| **T** | **`m_rest`** readout; **Higgs** = macro-возбуждение поля плотности |
| **SM fit** | **`m = m_P α_fs² f_геом`** (§8.2) — check topology, не Higgs-knob |

**Sim leaf:** [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

#### 5.0.2 Фундаментальный квант фазового действия **`s₀`** (переносчик **`Arg`**)

**`Arg`-поток** — безмассовый калибровочный квант **фазового натяжения** на одном **`hV`**, но его **действие за один M-тик** квантуется из геометрического затвора §3.7.2:

```
Δφ_min = ½ rad     (Heisenberg floor — impl: heisenberg_phi_min)
s₀ = ℏ · Δφ_min = ℏ/2 ≈ 5.272×10⁻³⁵ J·s
```

**Истинная энергия одного кванта** за M-тик **`hT = t_P/√2`** (§7.1):

```
E₀ = s₀ / hT = (ℏ/2) / (t_P/√2) = (1/√2) · ℏ/t_P = E_P / √2
E_P = ℏ/t_P ≈ 1.956×10⁹ J
E₀ ≈ 1.383×10⁹ J ≈ 8.63×10²⁷ eV
```

**Локальный энергетический эквивалент на пиксель** (не «масса частицы», а **`E=mc²`** на **`hV`**):

```
m_arg = E₀/c² = m_P / √2 ≈ 1.538×10⁻⁸ kg
```

**Скорость переноса** — **не** macro-свет **`c = c₀/√2`** (fractal T-зигзаг), а **чистая тактовая скорость CA** по каузальным осям **`N₄`**:

```
v_arg = c₀ = l_P/hT = √2 · c ≈ 4.24×10⁸ m/s
```

| величина | формула | SI (CODATA) | vs Higgs |
|----------|---------|-------------|----------|
| **`s₀`** | **`ℏ/2`** | **5.27×10⁻³⁵ J·s** | — |
| **`E₀`** | **`E_P/√2`** | **~1.38×10⁹ J** | **~8.6×10²⁷ eV** |
| **`m_arg`** | **`m_P/√2`** | **~1.54×10⁻⁸ kg** | micro-эквивалент, не rest mass |
| **`v_arg`** | **`c₀`** | **√2·c** | **> c** (M-такт, не macro-photon) |
| Higgs (T) | macro fluctuation | **~1.25×10¹¹ eV** | **~10¹⁶× слабее `E₀`** |

**Код:** `si_constants.s_0`, `E_0`, `m_arg`, `v_arg`, **`p_0`, `L_0`, `F_0`** · verify **`Arg_quantum`**, **`MechanicalQuantum`**.

#### 5.0.3 Антиматерия: геометрическая инверсия **`n → −n`**

**SM-мacro:** антивещество — «другое вещество», рождение парами, загадка baryon asymmetry.

**M (ДА):** античастица — **тот же** солитон **`hV`**, **зеркальное** фазовое вращение:

| | частица | античастица |
|---|---------|-------------|
| обход **`N₄`** | **`+2π`** (правый вихрь) | **`−2π`** (левый) |
| топокласс | **`n = +1`** · `VORTEX_P` | **`n = −1`** · `VORTEX_M` |
| объём | **`hV`** | **`hV`** (тот же) |
| **`m_rest`** | зигзаг + **`K_P`** | **то же** — **`K_P`** не различает знак вращения |
| **заряд T** | **`+e`** | **`−e`** — направление **`Arg`-потоков** инвертировано |

Impl: `seeds.py` · `exp(i·charge·θ)` · verify **A10** (winding at seeds).

**Аннигиляция (e⁺ + e⁻ → 2γ)** — не «уничтожение материи», а **алгебраическая компенсация** топологии:

```
n = +1  +  n = −1  →  n_net = 0     (фазовые градиенты противоположны, ±2π схлопываются)
```

1. два вихря сближаются на **`N₄`** → **`Arg`-градиенты** накладываются **в противофазе**;
2. **топологический замок** (Heisenberg + **`K_P`**) **исчезает** — среда возвращается к **гармоническому вакууму** `n=0`;
3. запертая энергия **не уничтожается** (A3) — уходит **двумя** macro-волнами давления со **`c = κ·c₀ = c₀/√2`** — **T-фотоны** (§1.1).

**Baryon asymmetry (§9.2 Phase IV→I):** не «50/50 случайность». M **детерминирован** (§2.1): первый A5-сдвиг «лёда» задаёт **хиральность** пузыря — **доминирует `n=+1`**. Антиматерия **локально** рождается (коллайдер, U-вихрь), но живёт в **«правой воде»** — быстро встречает **`n=+1`** фон → annihilation. **«Левые круги на правой воде»** — не мистика, а **геометрия стартового зародыша** (§9.7 leaf: какой класс «наш»).

**Sim leaf:** annihilation **VORTEX_P + VORTEX_M** → **`n_net→0` + 2-front** — [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

**Объём ячейки:**

```
dV = (dl)³ ,   dl = l_P = const
```

Один **элементарный** pra-дефект (pra-vortex, «кирпич» уровня 0) **локализован в одном `v_p = dV`** — одной ячейке решётки.

**Параметры `v_p`-частицы (уровень 0) — не гипотеза модели.** Это **Planck-арифметика уже сейчас**: подставь измеренные `ℏ, G, c, e` → числа (§7). Модель **идентифицирует** «кирпич» с одним `v_p`, а не «обещает вычислить потом».

```
v_p = dV = (dl)³
m₀ = m_P = √(ℏc/G)     ≈ 2.18×10⁻⁸ kg
e₀ = e                 ≈ 1.602×10⁻¹⁹ C
ν₀ = 1/hT             ≈ 2.62×10⁴³ Hz
u₀ = u_P = K_P = c⁷/(ℏG²)    ↔ |z|² = 1 в natural
c₀ = dl/hT = √2·c
```

Свободных «новых» констант на этом уровне **нет** — только `ℏ, G, c, e` (измерены) + целый `n` (топология).

**Если структура занимает больше одного `v_p` на фундаментальном уровне — это уже составная структура** (узел из нескольких pra-дефектов), не элементарная частица.

**Топология:** vortex charge `n ∈ ℤ` (U(1)); **спинорный полуоборот** при обходе **`hV`** → **SU(2) sign** (§3.10).

**Паули на одном `v_p`:** два fermionic pra-дефекта с совпадающим спинором **запрещены** — **`K_P`** выталкивает (§3.10.4).

**Самозахват:** gate `φ` запирает фазовый дефект внутри одного dV.

**Зигзаг → масса (T):** **`Arg`-запирание** + внутренние отражения в ε-окрестности **`v_p`** → macro **`m_rest`** (§5.0.1).

### 5.1 Пространство = **несжимаемая** lattice fluid

**Меньше `v_p` не существует** (§1, §5) ⇒ **`v_p` несжимаем** — минимальный квант объёма. Не «пустота между точками», а **заполненные до краёв** ячейки.

**Пространство — идеальная комплексная спинорная квантовая жидкость** на константных пикселях `(hL, hT)`:

| Образ | M | аксиома |
|-------|---|---------|
| **Океан / вакуум** | базовое кипение, boulon + gate | A5, §10.2 |
| **T (macro)** | **гидродинамика** поля `z` — волны, front, coarse | §4 |
| **Частица** | устойчивый **водоворот** (vortex/soliton) в `v_p`… | §5, §9.7 |
| **Масса** | **`Arg`-зигзаг** + **`K_P`**-отражение в **`hV`** (§5.0.1) | §5 |
| **Энергия / объём** | **не сжать** — только **перемешать** | A3 `Σ|z|²`, A6 |

```
несжимаемость  ↔  фиксированный v_p + сохранение нормы (A3)
«пустоту» искали — среда изначально полная; |z|→0 недостижим (A5)
```

**Не «просто метафора»:** см. §5.2 — Madelung-изоморфизм на дискретной решётке. Имя репо **`mt-lattice-fluid`** — онтология M.

### 5.1.1 Сопротивление сжатию (модуль упругости среды)

**Несжимаемость** — не только «фиксированный `v_p`». Нужен **ответ среды** на попытку локально изменить «плотность» информации. В continuum — **модуль объёмной упругости** / давление на сжатие. В M его роль несёт **нелинейный gate** `𝒩(z)` (§3.4), не отдельный fitted knob.

**Базовое давление (жидкость заполнена):** `ε = 1` ↔ **`u_P`** — фоновый модуль покоя, «заполнено до краёв» (§7.1). В natural: знаменатель `|z|² + ε` при равновесии вакуума.

**Сопротивление деформации:** локальное отклонение `ρ = |z|²` от фона → скобка gate:

```
φ = 2π · ( α* / (ρ + ε) − 1 ) · w(ρ)     ,   α* = 1 + 1/(4π)
```

Чем сильнее «сжать» или «разредить» ячейку, тем **жёстче** фазовый доворот `exp(iφ)` — среда **возвращает** к вакуумному балансу (A5, A7). Это **цифровой аналог** колоссального ответного давления несжимаемой жидкости; перенос избытка — не сжатие, а **волновой фронт** или **vortex** (§5.2).

**Связь с κ (упругость ↔ скорость волны давления):** геометрия N₄ задаёт **`κ = 1/√2`**. С **`hT = t_P/√2`** (§7.1):

```
c = κ · c₀     — macro скорость «звука»/света на T (CODATA)
c₀ = l_P/hT    — тактовая скорость передачи по осям M
```

**`κ` — не подгон:** коэффициент **геометрии N₄** (macro-изотропия из ромба). Отдельно от **`α*`, `ε`** (amplitude-stiffness gate).

#### Размерный модуль упругости **`K_P`** (не fitted)

Macro-волна давления несжимаемой жидкости = **свет** со скоростью **`c`** (CODATA). Гидродинамика:

```
c² = K / ρ_mass        ⇒        K_P = μ_P · c²
```

| символ | формула (СИ) | значение |
|--------|--------------|----------|
| **`μ_P`** (mass density fluid) | `c⁵/(ℏG²)` | `≈ 5.155×10⁹⁶` kg/m³ |
| **`K_P`** (bulk modulus) | **`μ_P·c² = c⁷/(ℏG²)`** | **`≈ 4.633×10¹¹³` J/m³ (Pa)** |

**Сшивка с gate:** **`K_P = u_P`** = **`β`** в размерном уравнении §7.1. Не новая константа — **тот же** Planck-барьер: **`K_P = μ_P·c² = c⁷/(ℏG²)`**.

```
|z|² = ρ_E / u_P     ,     ε = 1  ↔  ρ_E = u_P = K_P   (natural floor)
```

**Нотация:** **`μ_P`** [kg/m³] · **`u_P`** [J/m³] · **`K_P`** [Pa], **`K_P = μ_P·c² = u_P`**.

Код: `SI.mu_P`, `SI.u_P`, `SI.K_P` · `mt_ca/si_constants.py`.

**Вязкость (второй параметр среды):** A5-кипение — **микро-вязкость**; на **T** — emergent **`ν_CA = ¼·c₀·l_P`** из coarse-graining §4.1.2 (**не** член в **`g`**). Isotropic streaming (§3.6) ≠ замена **`ν_CA`**.

| параметр | роль в fluid | M |
|----------|--------------|---|
| **`K_P = μ_P·c² = u_P`** | модуль объёмной упругости вакуума | **`c⁷/(ℏG²)`** §5.1.1 |
| **`ε`, `α*`** | amplitude-stiffness gate | `φ(ρ)` |
| **`κ = 1/√2`** | геометрия N₄ → `c = κ·c₀` | §1.1, §7.1 |
| **A5 кипение** | микро-вязкость | `|z|≫0` вакуум, не ламинар |
| **`γ = κ_link = 1/4`** | кинетическое смешивание по **`N₄`** | §5.2.2 · `local_ca` |

**Impl / sim:** **`κ_link = ¼`** — [`DEVLOG.md` §2](DEVLOG.md#§2-audit--gap-tracker) · ballistic check на **`c`** — [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

### 5.2 Гидродинамика M → T (Madelung на **N₄**)

**История (provenance):** Madelung (1926) — непрерывное уравнение Шрёдингера **эквивалентно** уравнениям Эйлера для жидкости с «квантовым потенциалом». Madelung — в **continuum**; мы переносим принцип на **дискретный** CA при фиксированных `(hL, hT)`.

**Канон ε:** поток и `div_disc` — на **N₄** (не Moore; в ночном черновике было «Мур» — **снято**, A1).

#### Неразрывность (сохранение «объёма» информации)

A3: глобальная норма `Σ_x |z|²` invariant. Локальная «плотность» `ρ = |z|²` в ячейке `v_p` меняется **только перетоком** через mixing (bond-unitary / Δ₄):

```
Δρ/Δt + div_disc(j⃗) = 0
```

`j⃗` — дискретный поток комплексной амплитуды/фазы по связям **N₄** (`local_ca`, §3.4). Аналог **сохранения объёма** несжимаемой жидкости (§5.1): fluid не исчезает — **перемешивается**.

#### Давление / импульс — gate `𝒩(z)` (модуль сопротивления)

Роль **давления** и **сопротивления сжатию** — **нелинейный gate** `φ(|z|²)` (§3.4, §5.1.1, §7.1):

- `ε = 1` — базовая «заполненность» жидкости (несжимаемый фон);
- отклонение `ρ` от фона → `α*/(ρ+ε) − 1` — **упругий** фазовый ответ;
- избыток не сжимается in-place — уходит **волновым фронтом** (`n=0`, §9.7) или **закручивается** в vortex (massive, зигзаг §5).

Effective **Euler / Navier–Stokes на T** — coarse-grain над этим M-слоя; полный дискретный NS **не** записан буквально — **структурный** изоморфизм, численно через T-readout.

#### Вязкость и «квантовая пена»

A5: абсолютный ноль недостижим → жидкость **никогда ламинарна** на M. Микро-турбулентность = вечное кипение boulon + gate. На **T** — нулевые колебания вакуума, `|Ψ|²`-шум coarse-grain (§2.1).

| Madelung (continuum) | M (дискрет) | T (macro) |
|----------------------|-------------|-----------|
| плотность ρ | `\|z\|²` | `\|Φ\|²` Gaussian macro (§4.1) |
| поток j | bond flux N₄ | macro current from **Φ** |
| квантовое давление | gate `φ`, **`ε`, `α*`** (§5.1.1) | effective pressure |
| модуль упругости → `c` | **`κ = 1/√2`**, `hT` (§1.1, §7.1) | macro `c` |
| вязкость / нуль | A5 кипение (§5.1.1) | vacuum fluctuations |

#### 5.2.1 Локальные законы · кванты **p, L, F** · **SO(2)** (вывод из §5.0.2)

**Ошибка continuum-мышления:** считать **`p, L, F`** float-интегралами **`∫ Im(z*∇z)`** и искать их сохранение в ℝ. На M это **T-чтение**. На M — **целые кванты** той же лестницы, что **`s₀`** и **`⌊𝒩⌋`**.

**Посылка:** **`s₀ = ℏ·Δφ_min = ℏ/2`** (§5.0.2) — минимальное **действие** одного **`Arg`-события** на **`hV`** за **`hT`**. Пространство **`l_P`**, время **`hT`** — тоже кванты. **Всё механическое квантуется** — не три отдельных аксиомы.

##### Лестница (Planck-арифметика, без knobs)

```
p₀ = s₀ / l_P = ℏ / (2 l_P) = m_arg · c₀/2     — импульсный квант
L₀ = p₀ · l_P = s₀ = ℏ/2                       — моментный квант
g_M = c₀ / (2 hT) = l_P / (2 hT²)               — M-ускорение «клетка за тик²»
F₀ = p₀ / hT = m_arg · g_M                      — **F = mg** на одном hV
```

**Красота (§5.0.2 + §5.2.1):** не три разных knob — **одна** лестница **`s₀`**. Импульс **`p₀`**, масса **`m_arg`**, ускорение **`g_M`**, сила **`F₀`** связаны **ньютоновски**:

```
p₀ = m_arg · (c₀/2)        ,   v_half = c₀/2
F₀ = m_arg · g_M           ,   **F = mg**
ΔF = Δm · g                ,   Δm ∈ m_arg·ℤ , g = g_M  (локально на hV)
```

**Не путать:** **`E₀/c₀ = p₀`** — это **импульс**, не сила. **`F₀ = E₀/(c₀·hT) = p₀/hT`**.

| M-заряд | квант | SI (CODATA) |
|---------|-------|-------------|
| **`p`** | **`n_p · p₀`**, **`n_p ∈ ℤ`** | **`~½ m_P c`** |
| **`L_z`** | **`n_L · L₀`**, **`n_L ∈ ℤ`** (полуцелое при spin) | **`ℏ/2`** |
| **`F`** | **`n_F · F₀`**, **`n_F ∈ ℤ`** | **`~ c⁴/G`** (Planck force) |

**Связь с §3.12:** **`⌊𝒩⌋ ∈ ℤ_{N_ring}`** — не «дрейф float-импульса», а **передача целого числа **`p₀`-пакетов** между **`v_p`** и **`N₄(v_p)`** за **`hT`**. Kick ledger = **локальная бухгалтерия** **`Δp ∈ p₀·ℤ`**.

##### Локальная неразрывность (A3 на звезде **`N₄`**)

A3 глобально: **`Σ_x |z|² = const`**. **Локально** на каждом **`v_p`**:

```
Δρ(x)/Δt + div_disc j⃗(x) = 0
```

**Bond-flux** на ориентированной связи **`x → x+ê_i`** (natural units):

```
j_i(x) = Im( z*(x) · z(x+ê_i) ) / hT
div_disc j = Σ_i [ j_i(x) − j_i(x−ê_i) ]
```

| слой `g` | локальный закон |
|----------|----------------|
| **bond-unitary** `local_ca` (§3.6 legacy stream) | **точно:** каждая 2×2 link-mix сохраняет **`|a|²+|b|²`** на паре → **`div_disc j=0`** по **`ρ=|z|²`** |
| **isotropic gate** `z'=z·exp(iφ)` (§3.6 default) | **`ρ(x)` не меняется in-place**; перенос **импульса** — в **фазовом** **`π`**, не в **`ρ`** |
| **leapfrog + ⌊𝒩⌋`** (§3.12) | **`ρ`** mod **`N_ring`**; **импульс** — **целые** kick на ledger; **локальное закрытие** звезды ниже |

**Не T-гипотеза** для bond-слоя — **M-оператор** на **`N₄`**. DFT/spectral — только калибровка T (§3.5).

##### Локальное сохранение импульса

**Импульсная плотность** (дискретная, 2D):

```
π^a(x) = Im( z*(x) · [z(x+ê_a) − z(x−ê_a)] ) / (2 hT)     a ∈ {x,y}
```

**Локальный баланс** на звезде **`N₄(x)`** (изотропный gate + projected kick):

```
Σ_{y∈N₄(x)} Δπ^a(y) = 0   (mod p₀)     за один hT на каждой оси a
```

**Смысл:** gate **`φ`** собран только из **SO(2)-скаляров** в точке **`x`** (**`ρ`**, **`ζ=(Σ_{N₄}z)·z*`**, **`Δφ`**). Нет preferred axis → **отдача** соседям **симметрична**; **«сила»** **`F = Δp/hT`** — **целое **`n_F·F₀`**, не утечка в continuum.

##### **SO(2)** — вывод, не «квадрат ломает круг»

**1 · Изотропная звезда.** **`N₄(x) = {x±ê_x, x±ê_y}`** с **равными** весами в **`Σ_{N₄}`** (§3.6). Нет выделенной оси в **локальном** правиле.

**2 · SO(2)-скалярный gate.** Под **поворотом плоскости** **`R_θ ∈ SO(2)`** (на решётке: **`θ = k·π/2`**, **`k∈ℤ`**) поле **`Ψ`** и соседи **вращаются вместе**:

```
φ(R·Ψ) = φ(Ψ)     ⟹     g(R·Ψ) = R·g(Ψ)
```

**`ρ`**, **`|ζ|`**, **`Δφ`** — **скаляры**; **`exp(iφ)`** — **U(1)-поворот** спинора в **`v_p`**. **Локальная** динамика **SO(2)-ковariantна**.

**3 · Noether → **`L_z`**. Генератор **`SO(2)`** — **момент**:

```
L_z(x) = ε_{ab} x^a π^b(x)     →     L_z = Σ_x L_z(x) ∈ L₀ · ℤ
```

**Локально:** **`ΔL_z(x)/Δt + div j_L(x) = 0`** (дискретный аналог); **`j_L`** — **угловой поток** по связям **`N₄`**.

**4 · **`C₄` vs `SO(2)`** — одна ось, два разрешения:**

| слой | SO(2) |
|------|-------|
| **M** | **локальная** **`SO(2)`**-ковariantность + **`L_z ∈ L₀·ℤ`**; на торе **`R_{π/2}`** exact |
| **T** | macro **(1-2-1)** (§4.1) **сглаживает** **`C₄`** → **видимый** круг / **`κ=1/√2`** (T1) |

**Квадрат §3.8** — **контур vortex** на coarse, не отмена **локального SO(2)** gate. Тяжёлый vortex может **проступать** на T; **M-закон** **`L_z`** остаётся **дискретным**.

##### Таблица симметрия → заряд (M)

| симметрия | локальный закон | дискретный заряд |
|-----------|-----------------|------------------|
| **U(1)** phase / A3 | **`ρ`**, **`div j=0`** | **`Q = Σ|z|²`** |
| **трансляция** (однородный **`Λ`**) | **`div π=0`** (mod **`p₀`**) | **`P^a ∈ p₀·ℤ`** |
| **SO(2)** (изотропный **`N₄`**) | **`div j_L=0`** (mod **`L₀`**) | **`L_z ∈ L₀·ℤ`** |
| **время** (**`hT`**) | **`E`** per tick | **`E ∈ E₀·ℤ`**, ledger |
| **CR / topo** (§3.9) | winding | **`n ∈ ℤ`** (A10) |

**Verify / impl:** mechanical quanta — [`DEVLOG.md` §2](DEVLOG.md#§2-audit--gap-tracker) · §5.2.1.

#### 5.2.2 Полная лестница квантования · **`κ_link = ¼`** · **`b ∈ {0,1}`** · **`E ∈ E₀·ℤ`**

**Тезис:** после §5.2.1 на M **не остаётся** независимых mechanical float-knobs. Всё — **`s₀`**, **`l_P`**, **`hT`**, **`N₄`**, **`n ∈ ℤ`**.

##### I · Геометрический квант **`κ_link = 1/|N₄| = ¼`**

На **`d=2`** каузальная звезда von Neumann:

```
N₄(x) = { x±ê_x , x±ê_y }     ⇒     |N₄| = 4
```

**Изотропный** локальный шаг делит перенос **равномерно** по четырём осям (§3.6, §5.2.1). Тогда **одна доля** импульса/фазы/CR-связи на ось за **`hT`**:

```
κ_link = 1 / |N₄| = 1/4
```

**Три имени — один квант** (не три fitted параметра):

| символ | слой | роль |
|--------|------|------|
| **`γ`** | M kinetic mix (`local_ca`, dispersion) | **`γ = κ_link`** |
| **`cr_strength`** | A9 holomorphy coupling | **`cr_strength = κ_link`** |
| **`ν_CA`** | T emergent viscosity (§4.1.2) | **`ν_CA = κ_link · c₀ · l_P`** (natural: **`¼`**) |

**Вывод A15 для `γ`:** не «калибровка ω(k)», а **следствие |N₄|=4** + isotropic star. DFT/spectral **сверяет** T, не **задаёт** M.

**Natural units:** **`ν_CA_natural = κ_link = ¼`**. **`cr_seed_ceiling = ν_CA · {B_hV}²`** (§3.9.6) — тот же **`κ_link`**, умноженный на Bekenstein-дробь **`{B_hV}`**.

##### II · Энергетический квант **`E₀`** (замыкание лестницы)

Из **`s₀ = ℏ/2`** и **`hT`**:

```
E₀ = s₀ / hT = E_P / √2
```

**Эквивалентные формы** (один квант, не разные knobs):

```
E₀ = p₀ · c₀           ,   p₀ = s₀/l_P ,   c₀ = l_P/hT
E₀ = F₀ · l_P          ,   F₀ = p₀/hT
E₀ = L₀ / hT           ,   L₀ = s₀
```

**Работа одного kick:** сдвиг на **`l_P`** силой **`F₀`** за **`hT`** = **`E₀`**. Kick ledger (§3.12) = **целое число **`n_E · E₀`**, **`n_E ∈ ℤ`**.

**Локальный баланс энергии** (аналог **`div j=0`**):

```
Σ_{y∈N₄(x)} ΔE(y) = 0   (mod E₀)     за один hT на hV x
```

##### III · Заряд **`Q = n·e₀`** — топологический, не импульсный

**A10:** **`n ∈ ℤ`** — winding / вычет ДА. **Электрический заряд** — **не** **`n_p · p₀`**, а **идентификация**:

```
Q = n · e₀     ,   e₀ = e (CODATA)     ,   n ∈ ℤ
```

**EM-связность** на T **выводится** без нового knob (§8.2):

```
α_fs = e₀² / (4π ε₀ ℏ c) = 1 / (4π³ + π² + π)
```

**Смысл:** **`e₀`** — **якорь T** (измерен); **`α_fs`** — **число M** из gate geometry. Не наоборот.

##### IV · **b(x)** — вывод из **ρ_matter**, **m_P**, **n_∂** (не notation)

§5.0 уже задаёт **ρ_matter ∈ {0, ρ_P}**. **b** — **определение**:

```
b(x) = ρ_matter(x) / ρ_P = m_cell(x) / m_P     ∈ {0, 1}
```

**Три эквивалентных readout из одного z** (без нового поля):

```
b(x) = m_cell(x) / m_P                           — §8.1, m₀ = m_P
b(x) = min(1, |n_∂(x)|)                          — A10, ∂(hV) вокруг x
b(x) = 1  ⟺  |Δφ_N₄(x)| ≥ Δφ_min  AND  pole locked (A11)  — §5.0.1
```

**|z|² — не b:**

| | **ρ_field ∝ |z|²** | **ρ_matter = ρ_P·b** |
|--|----------------------|-------------------------|
| вакуум A5 | **> 0** (фон) | **0** |
| pra-vortex | **≤ ρ_P** (A7) | **ρ_P** на ядре |
| impl | clamp **ρ_max** | **primary = n_∂**, не \|z\|² |

**Macro (T):**

```
ρ_macro ≈ ⟨b⟩ · ρ_P     ,   ⟨b⟩ ≪ 1   ⇒   «1000 kg/m³» (§5.0)
```

##### V · **`α* = 1 + 1/(4π)`** — dimensionless, из tick budget

**§7.1:** **`hT·ω = 2π`** — полный фазовый цикл вакуума за M-тик. **Vacuum residue** на tick:

```
α* − 1 = 1/(4π)     ⟺     α* = 1 + 1/(4π)
```

**Не** ширина **`N_ring`** (512) — это **информационный** бюджет (§3.12.6). **`α*`** — **фазовый объём** gate numerator после **`ρ→u_P`**.

##### VI · Замыкание лестницы — все оставшиеся кванты (§5.2.3)

**Постулат:** на M **нет** mechanical float-knobs. Всё ниже — **следствие** **`s₀`**, **`l_P`**, **`hT`**, **`|N₄|`**, **`N_ring`**, **`N_φ`**, **`frac_bits`**, **`ρ_max`**. CODATA — **только** там, где T якорит измерение (**`e₀`**, **`m_e`**, **`c`**).

| символ | natural / disc | формула | SI (если нужен) |
|--------|----------------|---------|-----------------|
| **`a_Q`** | **`2^{−F}`** | **`a_Q = 2^{−frac_bits}`** | min \|z\| на Q-сетке |
| **`ρ_Q`** | **`2^{−2F}`** | **`ρ_Q = a_Q²`** | min **ρ_field** quanta |
| **`δφ_ring`** | **1 tick** | **`2π/N_ring`** | — |
| **`Δφ_min`** | **41 ticks** | **`⌊N_ring·s₀/(2πℏ)⌋`**, **`s₀=ℏ/2`** | **0.5 rad** |
| **`sync_strength`** | **10 ticks / unit pull** | **`⌊Δφ_disc/4⌋ = ⌊Δφ_disc·κ_link⌋`** | **`κ_link·Δφ_min = 1/8 rad`** |
| **`pauli_kick`** | **256 ticks** | **`N_ring/2 = π rad`** | SU(2) exchange **π** |
| **`pauli_ρ_min`** | **½** | **`ρ_max/2`** | **`u_P/2`** [J/m³] per component |
| **`pauli_cos∥`** | **cos(½)** | **`cos(Δφ_min)`** | Heisenberg parallel cone |
| **`n_E`** | **ℤ** | **`|Φ_kick| / Δφ_disc`** | **`n_E·E₀`**, **`E₀=E_P/√2`** |
| **`b`** | **{0,1}** | **`min(1,|n_∂|)`** | **`m_cell = b·m_P`** |
| **`Q`** | **n·e₀** | **`n∈ℤ`**, **`e₀=e` CODATA** | **`α_fs`** из gate §8.2 |
| **`macro_ρ`** | **1** | **`ρ_max`** | **`u_P`** для **`w(ρ)`** A8 |

**Holomorphy sync (§3.9):** **`φ_sync = sync_strength · wrapped(ζ)`** — та же **κ_link**-доля Heisenberg-шага, что и **`cr_strength`**, но на **глобальную** CR-сшивку tick.

**Pauli (§3.10.4):** parallel **`z₁ ∥ z₂`** при **`ρ_i ≥ ρ_max/2`** (два fermion на **`v_p`** делят потолок A7) и **`|cos θ| > cos(Δφ_min)`** → extra **`Φ += pauli_kick`** (**π** = **`N_ring/2`** ticks).

**Energy ledger (§3.12):** saturating **`Φ`** [ticks] → **`n_E = ⌊|Φ|/Δφ_disc⌋`**; локально **`Σ_{N₄} n_E·E₀ ≡ 0 (mod E₀)`**.

**Audit / verify:** [`DEVLOG.md` §2](DEVLOG.md#§2-audit--gap-tracker) · **`elementary_quanta_row()`** · **`MConfig`** defaults.

### 5.3 Макро-газ: нет «пустого пространства»

**Онтология (не billiard balls):**

| объект | что это в M | что **не** это |
|--------|-------------|----------------|
| **Среда / вакуум** | **непрерывно кипящая** несжимаемая **комплексная спинорная** жидкость **`z∈ℂ²`**; **`K_P ≈ 4.6×10¹¹³` Pa** (§5.1.1) | «пустота между молекулами» |
| **Молекула газа** | **фрактальный macro-узел** (soliton) из **миллиардов** pra-вихрей **`hV`** | шарик в пустоте |
| **Макро-газ** | **облако** таких macro-узлов на решётке | сжатие «самой» планковской жидкости |

**Ключ:** при macro-расширении / сжатии **планковская жидкость под облаком не сжимается ни на йоту** (A3, §5.1). Меняется только **плотность распределения macro-вихрей** на **`Λ`**. Молекулы **«плывут»** сквозь **цифровой океан** вакуума.

#### 5.3.1 Уравнение состояния идеального газа **`PV = nRT`**

Macro-молекулы окружены **каузальным фазовым полем** соседей **N₄**. При сближении — **не механический удар**, а **интерференция Гейзенберговских хвостов** кипения (§3.7, A5).

| macro | M→T смысл |
|-------|-----------|
| **P** | суммарный избыток **`Θ_flow`** / **`Δφ_N₄`** на границе замкнутого объёма (тор); среда **выталкивает** вихри к гармоническому равновесию |
| **T** | средняя **кинетическая энергия зигзага** macro-вихрей (§5); выше скорость → чаще micro-соударения |
| **V** | число **`hV`**, занятых облаком (не «сжатие жидкости») |

При macro-сжатии (**`V↓`**) вихри сближаются → **Гейзенберг-триггеры** перекрываются → **`Arg(⟨z⟩/z)`** между соседями растёт → **фазовое давление** **`P`** растёт **∝ плотности упаковки**:

```
P = (N_вихрей / V) · k_B · T     ⟹     PV = nRT
```

**Менделеев–Клапейрон** — **статистическое следствие** дискретного Коши–Римана + gate (§3.9), **не** отдельная аксиoma M.

**Sim leaf:** EOS / **`P(ρ)`** readout — [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

#### 5.3.2 Две скорости «звука»: свет **`c`** vs газ **`v_s`**

В одной модели живут **две разные** гидродинамики:

| волна | скорость | смысл |
|-------|----------|--------|
| **давление в вакууме (свет)** | **`c = κ·c₀ = c₀/√2`** (CODATA, §1.1) | предел волны **среды** — модуль **`K_P`**, **`n=0`** photon sector |
| **сжатие в macro-газе** | **`v_s = √(γ·R·T/M_газа)`** | передача **механического** импульса **между macro-вихрями** через их соударения |

**Инвариант:** **`v_s ≪ c`** — средняя скорость теплового зигзага macro-вихрей **строго меньше** скорости света из-за **внутренней инерции** зигзага (§5). Звук в газе **ползёт** относительно **света в вакууме**.

**Sim leaf:** численный **`v_s`** на T-readout — [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

#### 5.3.3 Уравнение Ван-дер-Ваальса из геометрии ДА (не fitted)

Классический **реальный** газ:

```
(P + a/V²) · (V − b) = nRT
```

В M **`a`** и **`b`** — **не эмпирика**, а следствия **пиксельной геометрии**, **`K_P`**, **Гейзенберга**, **Паули**, **`α_fs`**.

**1 · Поправка `b` (собственный объём, Heisenberg + Pauli)**

**`b`** — не «объём шариков», а **зона каузального отсечения** **`Δφ ≥ ½`** (§3.7, A16).

Два topo-вихря на **N₄** не сближаются бесконечно: при **`R < R_min`** фазовый градиент упирается в **`½` рад** → **`K_P`** + anti-smear → **бесконечное фазовое давление** (самофокусировка), ядра **расталкиваются**.

Минимальный объём локализации pra-вихря: **`hV = l_P³`**. С **Pauli** (§3.10.4) и **N₄**-ядром:

```
b₀ ≈ 4 · hV = 4 · l_P³
```

Для **`n`** молей, каждая молекула — конгломерат **`N_вихрей`** pra-дефектов:

```
b = n · N_A · N_вихрей · 4 · l_P³
```

Сетка **физически** не даёт упаковать macro-вихри плотнее этого предела.

**2 · Поправка `a` (притяжение, фазовая интерференция)**

**`a`** — **минимизация фазового натяжения**: на больших расстояниях **Гейзенберг-хвосты** двух macro-вихрей **интерферируют**; среда тянет дефекты к **`Δ_disc z → 0`** (§3.9). Сила **∝ (N/V)²**, масштаб — **`K_P`**:

```
a = N_A² · N_вихрей² · K_P · l_P⁶ · α_fs
```

**`α_fs`** — константа тонкой структуры (фазовое сопряжение с вакуумом, §8.2 · `si_constants.alpha_fs`).

**3 · Финальное уравнение в терминах автомата**

```
( P + n² N_A² N_вихрей² K_P l_P⁶ α_fs / V² ) · ( V − 4 n N_A N_вихрей l_P³ ) = nRT
```

**Симулятор (GPU DoD, open):** плотное облако **`n=±1`** vortex на торе; **сжатие границ** **`V↓`**:

1. далеко — **притяжение** хвостов (**`a`**, облако «стягивается»);
2. близко — **Heisenberg floor** (**`b`**, дальнейшее сближение **заблокировано**);
3. **нет** NaN / сингулярности — только **максимальное фазовое сопротивление** **`K_P`**.

**4 · Связь с бинарной плотностью §5.0**

**`b`** в continuum — **приближение**. В M — **абсолютный предел заполнения решётки**: macro-газ **не сжимается плотнее **`ρ₀ = ρ_P`**. Когда все **`hV`** в области заняты pra-вихрями → **Планковский монолит** → **`K_P → ∞`** (gate) + **Heisenberg floor** → фаза **заблокирована**.

**«Сингулярность» ЧД на M:** не **`ρ → ∞`**, а macro-область, где **каждый пиксель = 1** (вещество). BH = **упакованный монолит **`ρ_P`**, не математический pole плотности.

**Код / verify:** `si_constants.vdw_*` · **`VdW_algebra`** — [`DEVLOG.md` §4](DEVLOG.md#§5-matter--quantization--gas) · T compression — [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс).

---

## §6. Иерархия M → SM (масштабная лестница, часть модели)

| Уровень | Масштаб | Объект |
|---------|---------|--------|
| 0 | 1·dV = (dl)³ | **pra-дефект** в одном dV, заряд `e₀`, `m₀` |
| 1 | 10³–10⁵·dl | пре-резонансы / лептонные узлы (составные) |
| 2 | ~10¹⁵·dl | кварковые фокусы (составные) |
| 3 | ~10²⁰·dl | адроны (протон), SM |

Конфайнмент: «вытащить кварк» = разорвать топологический узел → **пара pra-дефектов**, не один dV.

---

## §7. SI-мост (размерные линейки)

```
dl = hL = l_P = √(ℏG/c³)
hT = t_P/√2 ,   t_P = l_P/c   (учебниковый; hT — SSOT M-тик)
dV = dl³
|z|² ↔ ρ_E/u_P ,   u_P = K_P = c⁷/(ℏG²) ,   μ_P = c⁵/(ℏG²)
(Δ z)/dl² ↔ Δ₄ z
κ = 1/√2 ,   c₀ = l_P/hT = √2·c ,   c = κ·c₀ ,   c² = K_P/μ_P
```

### 7.1 Константы `g`: SI → код (монолит, без λ)

**Шаги решётки (CODATA 2018 + N₄ κ):**

| символ | формула | значение (СИ) | код |
|--------|---------|---------------|-----|
| `DX` / `hL` | `√(ℏG/c³)` | `1.616255×10⁻³⁵` м | `SI.l_P` |
| `t_P` (conv.) | `l_P/c` | `5.391247×10⁻⁴⁴` с | `SI.t_P` |
| **`DT` / `hT`** | **`t_P/√2`** | **`3.812187×10⁻⁴⁴` с** | **`SI.hT`** |
| `c₀` | `l_P/hT` | `4.239706×10⁸` м/с | `SI.c0` |
| `c` (macro) | `κ·c₀` | `2.997925×10⁸` м/с | `SI.c` |
| `μ_P` | `c⁵/(ℏG²)` | `5.155×10⁹⁶` kg/m³ | `SI.mu_P` |
| **`K_P` = `β` = `u_P`** | **`μ_P·c² = c⁷/(ℏG²)`** | **`4.633×10¹¹³` Pa** | **`SI.K_P` / `SI.u_P`** |

**Fluid:** `c² = K_P/μ_P` — macro `c` = скорость волны давления несжимаемой среды (§5.1.1).

**ω — не knob:** один полный оборот вакуумной фазы за **M-тик** `hT`:

```
ω = 2π / hT ≈ 1.648×10⁴⁴ рад/с
hT·ω = 2π
```

**Размерный gate (до нормировки):** λ **убран** — его роль = `hT` (размерность времени):

```
z' = z + l_P²·Δ_disc z + i·hT·( α_SI/(ρ_E + β) − ω )·z
β = K_P = u_P = c⁷/(ℏG²) ≈ 4.633×10¹¹³ Дж/м³
α_SI = (2π + ½)·u_P ≈ 3.143×10¹¹⁴ Дж/м³
```

**Схлопывание в natural (симуляция):** `|z|² = ρ_E/u_P`, подставить `hT·ω=2π`:

```
φ = hT·ω · ( α*/(|z|² + ε) − 1 ) = 2π · ( α*/(|z|² + 1) − 1 )
α* = 1 + 1/(4π) ≈ 1.0795774715
ε = 1
```

| буква диалога | СИ | natural (код) | число |
|---------------|-----|---------------|-------|
| `β` | `u_P` [Дж/м³] | `ε = 1` | `4.633×10¹¹³` |
| `λ` | `hT` [с] | **нет** — внутри `hT·ω` | `3.812×10⁻⁴⁴` |
| `α` | `(2π+½)u_P` [Дж/м³] | `α* = 1+1/(4π)` | `1.079577…` |
| `ω` | `2π/hT` [рад/с] | `phase_scale=2π` | `1.648×10⁴⁴` |
| `γ` | — | `gamma=0.25` | **калибровка dispersion (T)**, не из SI |

**Python (SSOT):** `mt_ca/si_constants.py` → `as_code_dict()`.  
**Рантайм gate:** `mt_ca/update.py` — `phase_scale * (alpha_factor/(ρ+ε)−1)`.

**Зигзаг → масса (T):** при локализованном vortex gate отражает фазу в ε-окрестности одного `v_p`; macro readout видит субсветовой зигзаг → `m_rest` (§5).

---

## §8. SM-константы (модель, не meta)

### 8.1 `v_p`-частица: Planck-арифметика (**уже сейчас**, не claim модели)

| Параметр | Формула | Статус |
|----------|---------|--------|
| объём | `v_p = (dl)³` | §5, §7 |
| масса | `m₀ = m_P` | **считается сейчас** из `ℏ,G,c` |
| заряд | `e₀ = e` | **измерено**; U(1) — один квант на дефект |
| частота | `ν₀ = 1/hT` | **считается сейчас** (~2.62×10⁴³ Hz) |
| энергоплотность при `\|z\|²=1` | `u_P = K_P` | **считается сейчас** |

**Не путать:** Planck-строки — размерный факт. **Идентификация модели:** элементар = один `v_p`. **§8.2:** α_fs и массы SM — **алгебра из `g` + топологии**, считаются сейчас (код: `si_constants.py`).

### 8.2 SM-константы: **вычисляются** из `g` (не fitted)

#### Постоянная тонкой структуры α

Из gate (§7.1): `α* = 1 + 1/(4π)` — vacuum residue `(α*−1) = 1/(4π)` на тик.  
T-слой: EM-связность = **фазовый объём** emergent 3D readout над циклом `2π`:

```
α_fs⁻¹ = 4π³ + π² + π
α_fs   = 1 / (4π³ + π² + π) ≈ 0.0072973525
```

| | Модель | CODATA 2018 |
|---|--------|-------------|
| `α_fs` | `0.007297352569…` | `0.0072973525693…` |
| `α_fs⁻¹` | `137.036303776…` | `137.035999084…` |

**Считается сейчас** — подставь `π`. Не sim, не knob.

#### Массы lepton / baryon (структура + топология узла)

```
m_e = m_P · α_fs² · f_геометрия(n, N₄)
m_p = m_P · (α_fs / 3) · f_геометрия(n, N₄)
```

**`f_геометрия`** — упаковка pra-дефектов в составном узле на выбранной ε (§1.3). Для **`N₄`**: winding `n` + число `v_p` в узле → **число**, не knob.  
Check электрона (§4.0.1):

```
f_геометрия(e) = m_e / (m_P · α_fs²) ≈ 7.86×10⁻¹⁹     (CODATA → check topology)
```

#### Таблица «считается сейчас»

| Константа | Формула | Статус |
|-----------|---------|--------|
| `l_P`, `t_P`, `hT`, `m_P`, `μ_P`, `u_P`, `K_P` | Planck + fluid | §8.1 |
| `α*`, `ε`, `ω` | gate §7.1 | §7.1 |
| **`α_fs`** | **`1/(4π³+π²+π)`** | **считается** |
| `m_e`, `m_p` | `m_P · α_fs^… · f_геометрия(n,N₄)` | **f из ε + топологии** |

**Проверка M→T (§4.0.1):** sim + electron node geometry → `F_lepton` → `m_e` **без** нового knob. **Impl / sim:** [`DEVLOG.md` §4](DEVLOG.md#§8-sm--higgs).

**Не meta:** электрон, протон, α, иерархия кварков — **целевые observables модели M→T→SM**.

### 8.3 Higgs: T-пена, не M-первопричина массы

**Стандартная картина (macro):** scalar **Higgs** → Yukawa → **`m`** частиц.

**Наша M→T иерархия (§5.0.1):**

```
M   Arg(⟨z⟩/z) + K_P + зигзаг в hV     — истинный механизм инерции
      ↓ coarse + resonance
T   m_rest, effective scalar «Higgs-like» fluctuation
      ↓ accelerator readout
SM  125 GeV Higgs boson — macro wave packet, not micro root
```

**125 GeV** — энергия **macro-возбуждения** цифрового океана (конгломерат **`Arg`-квантов**), не «создание mass ex nihilo». Коллайдер **раскачивает T-слой** и регистрирует **волну пены**; под ней — детерминированный **планковский фазовый затвор** + **`ρ ∈ {0, ρ_P}`**.

**Энергетическая иерархия (§5.0.2):** один **`Arg`-квант** несёт **`E₀ ≈ 8.6×10²⁷ eV`** — на **~16 порядков** выше **Higgs ~1.25×10¹¹ eV**. LHC видит **T-конгломерат**; M-переносчик — **`s₀=ℏ/2`** на каждом **`hT`**.

**Следствие для модели:** **`m_e`, `m_p`** считаем через **`m_P · α_fs · f_геом`** (§8.2) и **zigzag/topology** — **без** fitted Higgs VEV как axiom M. Higgs sector = **emergent T** (open: correlate **`m_H`** with **`N_вихрей`** pack). **Sim leaf:** [`DEVLOG.md` §3](DEVLOG.md#§3-open-leaves-индекс) · **не refute** LHC — **переинтерпретация** слоя.

---

## §9. Космология (→ [`META.md` §3](META.md#§3-космология-логика-закрыта))

**Не SSOT.** Циклы, BB-narrative, бесконечность, генезис **наблюдаемой** Вселенной, baryon-asymmetry **story** — только **[`META.md`](META.md) §3**. Следуют из §0–§8 **без новых knobs** в `g`.

**Остаётся в MODEL:** тор (§10.1), A3/A5/A6, антиматерия **`n→−n`** (§5.0.3), seed-топология ниже.

### 9.7 Поиск зародышей: **не brute force**, ~**10–20** прогонов

**Raw brute force** отсечён **до старта:** A9 (CR) + A4 (U(1)) → случайные поля = мусор на 1-м тике.

**Три сжатия:** (1) моды Δ₄ / **(k, φ)** · 	_analysis.py · (2) только winding **n ∈ ℤ** · (3) inverse coarse-grain из T (опция).

**4 топокласса на N₄:**

| n | физика | SeedClass | test |
|---|--------|-------------|------|
| 0 | безмассовая garmonika | PLANE_WAVE | T1 |
| +1 | pra-vortex 
_p | VORTEX_P | T2, A11 |
| −1 | antivortex | VORTEX_M | T2 collision · §5.0.3 |
| +2 | двойной узел | VORTEX_N2 | persistence |

**2 параметра на класс:** r = 1…5 ячеек; импульс/φ₀ → **~10–20 runs**. seeds.py · 
alidate_mt.py.

**ε = N₄** (Moore снят). Не наш BB; да — stable macro + **
_геометрия(n,N₄)**.

---

## §10. GPU / численная реализация (инженерный контракт)

Физика §0–§8 замкнута; ниже — **три предохранителя**, без которых GPU-код падает или «молчит».

### 10.1 Границы: тор (periodic BC)

Конечная сетка `N×N` (MVP 2D). На краях **`g` не обрывается** — **periodic wrap**:

```
x+N ≡ x ,   y+N ≡ y
N₄ на краю сшивает правый↔левый, верхний↔нижний
```

**Код:** `laplacian.von_neumann_laplacian` — `torch.roll`; `linear_step_local_ca` — wrap в bond-sweeps.  
**Зачем:** глобальная норма без стенки-поглощателя; унитарность на торе.

### 10.2 Инициализация: «первичный бульон», не `z≡0`

При `z=0` gate даёт `φ = 2π/(4π)`, но **`z·exp(iφ)=0`** — поле заморожено, кипение не стартует (A5).

**t=0:** комплексный фазовый шум, **`|z| ~ vacuum_amplitude`**:

```
vacuum_amplitude = 1e-6     (default; config MConfig.vacuum_amplitude)
SeedClass.VACUUM → N(0, amp) + i·N(0, amp)
```

**Код:** `seeds.make_seed(VACUUM)` · `simulator.reset()`.

### 10.3 Алгебра шага: `exp(iφ)` / `ω^Φ`, не Euler `z += iφz`

Нелинейность **только** модуль-сохраняющий доворот (A3, A4). Linear — product локальных 2×2 unitaries, не raw add.

**Canonical M (§3.12.5):** **`R(Φ)=ω^Φ`** на **`Z_N[i]`** — дискретный аналог **`exp(iφ)`**; float **`exp`** только decode/probe.

```
z* = local_ca(z; γ)
z' = z* · exp(i·φ)          ← T-нотация; на M: R(Φ_disc) via Rot_LUT
φ  = 2π · (α*/(|z*|²+ε)−1) · w(ρ)
```

**Код:** `update.micro_step` · `linear_step_local_ca`.  
**Float drift:** bond-unitaries + `exp(iφ)`; контроль — `verify_principles` A3 (`norm_drift < 1e-4`).

### 10.4 GPU literals (SI anchor · `si_constants.as_code_dict()`)

В natural sim **`ε=1`**, `|z|²↔ρ_E/u_P`; ниже — **размерный якорь** для отчётов / будущих SI-kernels:

```
DX            = 1.616255e-35   # l_P  [m]
DT            = 3.812187e-44   # hT = t_P/√2  [s]  (не textbook t_P)
K_P           = 4.632947e+113 # bulk modulus [Pa = J/m³]
ALPHA_FACTOR  = 1.07957747     # α* = 1 + 1/(4π)
```

**Код:** `python -c "from mt_ca.si_constants import as_code_dict; print(as_code_dict())"`.  
Расхождение с округлёнными paste (~`KP 4.622584e+113`, `DT 3.811984e-44`) — **CODATA/округление**, не другая физика.

**4 топокласса §9.7:** `VACUUM`, `PLANE_WAVE`, `VORTEX_P/M/N2` · `seeds.py` · 4070 MVP 512² — секунды на прогон.

---

## §11. Код (M-only slice)

`mt_ca/`: M = `local_ca` + gate. T = `t_analysis.py` + `validate_mt.py`. Протокол: `BUILD.md` · константы: `si_constants.py` · §10 — GPU-контракт.
