\begin{tikzpicture}[carrier panel, x=0.32cm, y=0.32cm]
  \begin{scope}
    \foreach \r in {0,...,15} {
      \foreach \c in {0,...,15} {
        \pgfmathsetmacro{\v}{cos(12*\r + 7*\c)}
        \pgfmathsetmacro{\g}{int(50 + 40*\v)}
        \fill[gray!\g] (\c,15-\r) rectangle ++(1,1);
      }
    }
    \node[anchor=north, font=\footnotesize] at (8,-1) {$\mathcal{M}$: детерминированный $z(x,t)$};
  \end{scope}
  \begin{scope}[shift={(6.2,0)}]
    \foreach \r in {0,...,15} {
      \foreach \c in {0,...,15} {
        \pgfmathsetmacro{\v}{0.6*cos(12*\r + 7*\c) + 0.25*cos(3*\r) + 0.2*cos(3*\c)}
        \pgfmathsetmacro{\g}{int(50 + 35*\v)}
        \fill[gray!\g] (\c,15-\r) rectangle ++(1,1);
      }
    }
    \node[anchor=north, font=\footnotesize] at (8,-1) {$\mathcal{T}$: $\Phi=\mathcal{B}z$};
  \end{scope}
\end{tikzpicture}
