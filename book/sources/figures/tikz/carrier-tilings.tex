\begin{tikzpicture}[carrier panel, scale=0.95]
  \begin{scope}[shift={(-3.2,0)}]
    \foreach \i in {-2,...,2} {
      \foreach \j in {-2,...,2} {
        \pgfmathsetmacro{\x}{\i*0.55 + mod(\j,2)*0.275}
        \pgfmathsetmacro{\y}{\j*0.48}
        \draw[carrier object, carrier fill]
          (\x,\y) -- ++(1,0) -- ++(-0.5,0.866) -- cycle;
      }
    }
    \draw[carrier object, carrier hifill] (0,0) -- (1,0) -- (0.5,0.866) -- cycle;
    \node[anchor=north, font=\footnotesize] at (0.5,-0.35) {треугольники};
  \end{scope}
  \begin{scope}[shift={(0,0)}]
    \foreach \i in {-2,...,2} \foreach \j in {-2,...,2} {
      \draw[carrier object, carrier fill] (\i,\j) rectangle ++(1,1);
    }
    \draw[carrier object, carrier hifill] (0,0) rectangle (1,1);
    \CarrierDimH{0}{1}{-0.35}{$a$}
    \node[anchor=north, font=\footnotesize] at (0.5,-0.55) {квадраты};
  \end{scope}
  \begin{scope}[shift={(3.2,0)}]
    \foreach \r in {-2,...,2} \foreach \c in {-2,...,2} {
      \pgfmathsetmacro{\cx}{\c*0.75 + mod(\r,2)*0.375}
      \pgfmathsetmacro{\cy}{\r*0.65}
      \CarrierHex{0.35}{(\cx,\cy)}
    }
    \draw[carrier object, carrier hifill]
      (30:0.35) \foreach \a in {90,150,...,330} { -- (\a:0.35) } -- cycle;
    \node[anchor=north, font=\footnotesize] at (0,-1.15) {шестиугольники};
  \end{scope}
\end{tikzpicture}
