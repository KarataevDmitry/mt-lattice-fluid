#!/usr/bin/env python3
"""REJECTED family — do not use for seed search.

Was: tick=(y+x)·step mod N_ring over 512 steps. That is a free plane-wave
ramp on N_ring, not Heisenberg bricks (N_φ=13). Canon seeker:
scripts/run_seed_brick_scan.py · vacuum_boil_fixed (class_dy/dx/offset).
"""

raise SystemExit(
    "rejected: use scripts/run_seed_brick_scan.py "
    "(filled N_φ bricks, not N_ring ramp)"
)
